# R-395 · A2 阶段二：gold 清洁账本发布与 0608 更正事件并入核验（阻塞终态报告）

- **任务号**：task-0612（A2 阶段二；并入 task-0608 更正事件追加）
- **日期**：2026-09-02（北京 16:06–16:15 执行窗口）
- **终态**：**阻塞（前置自检不过）——全程零写入，发布与并入未执行**
- **执行人**：spawn 子 agent（spawn-gold-a2-phase2，重派窗口第 2 次尝试）

## 背景与目标

gold 引擎 NaN 修复 A2（task-0610，R-394）已在 staging 产出全历史清洁重算账本，本次任务为目标的生产发布环节：

1. **生产发布**：HP `~/quant-evolve/results/engines/gold/shadow_nav.csv` 版本化存档后，用 staging 清洁账本（`output/staging_gold_a2/shadow_nav_a2fixed.csv`）覆盖，并做 diff 校验。
2. **task-0608 更正事件并入**：向 gold paper 账本 audit 追加 NaN 更正事件（asof=2026-08-31 收盘 **9.135**，R-393 已证；9.475=08-28 收盘，禁用），audit_len 预期 2→3。
3. **BFF 刷新核验**：手动触发 auto-sync（HP→VPS 镜像→BFF），核验 agent-dashboard:8055 `/api/quant/engines/gold_trend_sma200/shadow-nav` 返回新账本末点（预期 nav≈3.17068）。
4. **0609 清洁链基线通知项**：按原始任务书范围执行。

**硬性前置自检（任务书红线，任一不满足即零写入阻塞退出）**：HP gold 引擎须处于 `open.month=2026-09`、`last_signal=2026-08-31`、`audit_len=2`（即 09-02 15:40 北京 cron 已完成 close_and_roll）。

## 方法与数据来源

- 前置自检：SSH 直读 HP `paper_state.json`（sha256 + 字段抽取），shadow_nav.csv / staging 文件 sha256 与行数实测。
- 根因取证：HP `logs/paper_gold_daily.log`（mtime + 末 22 行 + 业务行过滤）；HP crontab 只读核对。
- 数据来源：全部为本机与 HP 实测（sha256sum / python json 直读 / 日志 tail），未引用缓存或推测值；过程证据见 `shared/results/work/task-0612-notes.md` 与 `task-0608-notes.md`。

## 核心发现

### 1. 前置自检三项全不满足（2026-09-02 16:07 实测）

| 自检项 | 要求 | 实测 | 结果 |
|---|---|---|---|
| open.month | 2026-09 | **2026-08** | ✗ |
| last_signal.month_end | 2026-08-31 | **2026-07-31** | ✗ |
| audit_len | 2 | **1**（仅 init） | ✗ |

paper_state.json sha256=`be9e30f1...adeb6`，与 09-01 基线逐字一致；`updated_at=2026-09-01T07:40:03+00:00`（09-02 run 未写入任何状态）。

### 2. 根因（本次与 09-01 阻塞不同）：cron 准点触发，但 run 因数据源超时失败

- log mtime=`2026-09-02 07:40:32 UTC`（=北京 15:40，crontab `40 7 * * 1-5` 准点触发）。
- log 末尾 Traceback：`urllib/request.py → http/client.py → ssl.py read → TimeoutError: The read operation timed out`——数据源 HTTPS 拉取超时，run 异常终止于**拉数阶段**。
- 业务日志末行仍为 09-01 run 写入的 `mark 2026-08-31: px=9.1350`；09-02 run 未产出任何业务行，**未走到 close_and_roll**，9 月信号（NaN 路径 w=0）仍未落盘。
- 影响：①发布窗口条件不成立（发布应在引擎状态一致窗口进行）；②更正事件无对象（9 月 NaN 记录尚未存在于 audit）。

### 3. 零写入清单（全部实测核验）

- 生产 `shadow_nav.csv`：sha256=`1bec2035...49a2814bd`，未动；
- `paper_state.json`：sha256=`be9e30f1...adeb6`，未动；
- staging `shadow_nav_a2fixed.csv`：sha256=`50afd135...a459ead`，就绪未动；
- registry / paper_engine 分配 / HP crontab：未动；无备份文件产生；BFF 与 VPS 镜像未触发刷新。

### 4. 附带校准信息（供重派执行）

- 行数口径：本次实测生产与 staging 均 158 行（python splitlines），此前笔记「生产 157 行」疑为 wc -l 末行换行符口径差异；重派时 diff 校验以实际 diff 输出为准。
- 发布 diff 预期不变：wdiff 34 行（33 语义月+1 舍入行 2014-07-31）+ 2026-08 stub 行 gold_ret 13.412%→8.324%（px 9.564→9.135）。
- 更正事件追加时仍须先完成同源核验：确认 9.135 与 cron 实际消费的 asof 收盘同源（日志 `mark 2026-08-31: px=9.1350` 已两次佐证同源语义正常）。

## 结论

**阻塞终态（第二次），全程零写入。** A2 生产发布、task-0608 更正事件并入、BFF 刷新核验均未执行，待前置条件恢复后按原任务书重派。解除条件：数据源恢复使 daily run 成功拉到 9 月 bar 且 close_and_roll 完成（open.month=2026-09、last_signal=2026-08-31、audit_len=2）。**重派建议**：北京 09-03（周四）15:40 cron 后；因本次失败根因是数据源网络超时（非引擎逻辑），建议重派前先做只读连通性探测；若需手动跑一次 `--action daily` 属引擎写入动作，须用户批准后另行派发。0609 清洁链基线通知项：笔记与任务书中仅指向「quant-bff vC-0 权威曲线 gold 列与考核链仍消费旧数据、重建成独立审批事项」的既有结论，本次无新增动作，已在 BFF 链路勘察中留档。

## 来源清单

- `shared/results/work/task-0612-notes.md`（两次阻塞实测记录、BFF 链路勘察、零写入清单）
- `shared/results/work/task-0608-notes.md`（更正事件方案二四点、asof 语义、09:09 同源核验方法）
- HP 实测：`results/engines/gold/paper_state.json`（sha+字段）、`results/engines/gold/shadow_nav.csv`（sha）、`output/staging_gold_a2/shadow_nav_a2fixed.csv`（sha）、`logs/paper_gold_daily.log`（mtime+Traceback）、crontab
- R-393（asof 收盘 9.135 定价）、R-394 / task-0610（A2 staging 产物与发布 diff 预期）
