# R-286 T4 影子叠加启动实施（批准点④执行报告）

- 任务号：task-0464｜日期：2026-08-23｜类型：影子叠加启动实施（纯新增文件，零在役改动）
- 批准链：用户 2026-08-23 01:24 拍板「都推进」= R-267/R-282 **批准点④（影子叠加启动）** → 本任务实施。前提就绪：task-0458 验收通过（a14_crowdf2 获 `shadow_overlay_eligible`，O1b/O2/O3 全过，冻结配置 `model/scoring_v12_frozen.json` sha256 315ee82d…）。
- 上游：R-282（§六启动条件与 A2 条目草案）、R-259（§4.2 schema / §5.2 接入四条件 / §5.3 数据管道 / §5.4 月度治理）、R-273（T4 低拥挤时代行为≡在役的影子期判读规则）
- **报告编号说明**：任务书预定 R-285，但 R-285 已被「可转债轮动E2预注册」（01:20 创建）占用，本报告改用 **R-286**。
- 硬红线执行情况：**纯新增**（engines.json + engines_shadow_evaluate.py + engines_shadow_nav_append.py + shadow_nav.csv 初始基线段 + cron 待装文件）。evolution_pipeline.py / registry 在役条目 / scoring 冻结文件 / paper_engine / HP crontab **零改动**（cron 只出待装文件，未安装）。

## 一、实施清单（四件套）

| 件 | 位置（HP ~/quant-evolve） | 说明 |
|---|---|---|
| 1. engines.json | `model/registry/engines.json`（新建，schema_version=1） | A（active，自 a13_rsraw_e1f10dz 抽取）+ A2（shadow cross_engine，R-282 §六草案落地）两条目 |
| 2. 影子 NAV 管道 | `results/engines/a2/shadow_nav.csv` + `scripts/engines_shadow_nav_append.py` | 初始=task-0458 overlay_combo_a14_w050_nav.csv 历史基线段（4491 行，2006-01-04→2024-06-28）；脚本幂等 init + append 逐月更新 |
| 3. 月度 evaluate | `scripts/engines_shadow_evaluate.py` | 读 A2 影子 NAV + A 在役 NAV → ann/MDD/Calmar/corr(A,A2)/滚动12月 → append shadow.evals[] → clean_evals 计数；`--mode baseline` 首跑 |
| 4. 待装 cron | VPS `hp-cron-pending/a2_shadow_evaluate.cron` + `INSTALL.md` | 每月 3 日 09:35（R-259 §5.4）；**未安装**，安装属不可自动清单 |

回滚路径：registry tar 备份 `model/registry_backup_task0464_20260823.tar.gz`（任务前置，67 项 registry 全量）→ 删 engines.json 条目 + 删两脚本 + 删 results/engines/a2/ 即完全回退；在役 A 引擎零依赖新文件（engines.json 缺失时按 R-259 §4.1 降级为硬编码单引擎 A）。

## 二、首月基线快照（手动首跑，不入 cron）

`--mode baseline` 首跑验证（ts 2026-08-22T17:33:19+08:00，HP 本地时区）：

| 指标 | 值 | 校验 |
|---|---|---|
| nav_period | 2006-01-04→2024-06-28 | 与 R-282 §三 locked 窗一致（4491 行） |
| ann | 0.21920118 | **逐位**复现 R-282 o2.combo_metrics ann 0.21920118 ✓ |
| mdd | −0.33554161 | 逐位复现 R-282 ✓ |
| calmar | 0.65327569 | 逐位复现 R-282 ✓ |
| corr(A,A2) | 0.999851 | 月频相关 n=221（在役 a13dz vs A2 叠加臂） |
| rolling12_ann | −0.02721 | 基线段末段 12 月（锁定期尾部） |
| clean_evals | **0** | 影子期自 2026-08-23 起尚未完成月度点，0 为正确起点 ✓ |
| evals[] | 1（基线） | mode=baseline，clean_eval=false |

## 三、engines.json A2 条目要点（R-282 §六草案落地）

- `engine_id=A2, parent=A, type=sub_engine_overlay, status=shadow, registry_ref=a14_crowdf2`
- `overlay.w=0.5, w_source=model/scoring_v12_frozen.json migration（R-267 §4.2 组件型建议值）`
- `shadow.mode=cross_engine, started_at=2026-08-23, min_months=3, max_months=6`
- `shadow.nav_path=results/engines/a2/shadow_nav.csv, required_clean_evals{min:3, max:6, rule=clean_evals 剔除低拥挤零区分月份；月度实测两腿换手叠加与拉回成本}`
- `adjudication{verdict=shadow_overlay_eligible, verdict_file=results/overlay_verdict_a14_crowdf2.json, task=task-0458}`
- `promotion=真金权重分配=用户人工门（批准点⑤），永不自动化`
- `audit.changes[]`：用户 01:24 批准 + task-0464 登记，两次变更留痕

## 四、终止判据四条（R-259 §5.4 框架，落位 engines.json shadow.termination）

① 连续 N 月低于门槛（N 待 R-282 冻结 + 用户确认时点）② corr(A,A2) 超独立性上限（E1 实测 0.135，上限待影子期数据到位后定）③ 数据断供 ≥K 月（K 待确认时点）④ 用户手动终止。数值均为占位并标注「R-282 冻结+用户确认时点」，不做预判。

## 五、观察判据（R-273 适配，落位 shadow.note）

T4 自 2025-09 起在低拥挤时代行为上≡在役（nav 比值极差 1.3e-04，稳定 0.958）——**低拥挤时段两腿区分度恒为零**，因此：
- p<40 时 A2≈A 属预期，**不算跟踪失败**；
- clean_evals 须剔除无区分月份，或在 3-6 月基础上按高位月（p≥40）出现数延长影子期；
- 月度 eval 须实测两腿换手叠加与再平衡拉回成本（R-267 §八.7）。

## 六、验收与可复核命令

```bash
# HP ~/quant-evolve
ls -la model/registry/engines.json scripts/engines_shadow_evaluate.py scripts/engines_shadow_nav_append.py
head -c 1200 results/engines/a2/shadow_nav.csv            # 基线段 4491 行
/home/noname/miniconda3/envs/quant/bin/python scripts/engines_shadow_evaluate.py \
  --mode baseline --engines model/registry/engines.json --engine A2   # 可重放，输出合法 JSON
tar -tzf model/registry_backup_task0464_20260823.tar.gz | wc -l      # 67
```
首跑 JSON 已实读（见 §二），干净 JSON、clean_evals 起点正确。

## 七、产物清单

- HP `model/registry/engines.json`（A+A2，纯新增）+ `model/registry_backup_task0464_20260823.tar.gz`
- HP `scripts/engines_shadow_evaluate.py`、`scripts/engines_shadow_nav_append.py`（纯新增，py_compile 通过）
- HP `results/engines/a2/shadow_nav.csv`（历史基线段 4491 行）
- VPS `hp-cron-pending/a2_shadow_evaluate.cron` + `INSTALL.md`（**待装，未安装**）
- VPS `work/task-0464-notes.md`（过程笔记/恢复点）

## 来源

R-282（§六 A2 草案与启动条件、§三 locked 窗与 O2 口径）、R-259（§4.1-4.3 schema 与迁移、§5.2-5.4 影子契约/管道/治理）、R-273（低拥挤期判读规则）、R-267（§4.2 组件型权重、§八.7 换手/拉回成本）；全部数字可由 §六 命令重放。

过程笔记：work/task-0464-notes.md
