# R-306 黄金趋势引擎影子观察登记实施（task-0484）

- 任务号：task-0484｜日期：2026-08-24｜类型：影子观察登记实施（registry 仅新增条目，A/A2 逐字不变，零在役改动）
- 批准链：用户 2026-08-24 22:54（微信会话）对黄金 E2 裁决**选①**「认可『风险调整+分散』定位」→ 影子观察登记。上游：R-303（E1 画像）→ R-304/R-305（task-0483 E2 预注册+执行，6/7 门 PASS，G3 前段 2013-19 Calmar 0.902<1.0 FAIL、牛市依赖 2020+ 贡献 65.2% 净对数收益，verdict=PARTIAL_DISCLOSURE_USER_ADJUDICATION）。**激活/接入层2 ERC/真金分配仍为用户人工门，本次不含。**
- 先例：R-286（task-0464）T4 影子叠加启动四件套结构（engines.json 新增 + 影子 NAV 管道 + 月度 evaluate + cron 待装包）。
- 硬红线执行：**registry 仅新增 B 条目**（tar 备份 + 文本拼接纯插入 + 字节/深度双重校验）；引擎文件/evolution_pipeline.py/paper_engine/HP crontab 零改动（cron 只落 pending 未安装，`crontab -l | grep -c gold`=0）。

## 一、四件套实施清单

| 件 | 位置 | 说明 |
|---|---|---|
| 1. engines.json B 条目 | HP `model/registry/engines.json`（8dc8b92c… → 08b045e7… → baseline 后 83c47f3a…） | `gold_trend_sma200`，slot=B，type=standalone_shadow，status=shadow；evidence 段写全判门/裁决链；shadow.started_at=2026-08-24，min/max 3/6 月 |
| 2. 影子 NAV 管道 | HP `results/engines/gold/shadow_nav.csv`（157 月 seed，2013-08-31..2026-08-31）+ `shadow_nav_seed.csv` + `mmf_monthly_push.csv`（157 月货基腿）+ `scripts/engines_shadow_nav_gold.py`（幂等 init+append+--dry-run） | seed=r483 e2_nav_monthly.csv 转制（w/gold/mmf/gross/net/nav）；append=腾讯源自采集→E2 冻结信号复刻→只追加完整月 |
| 3. 月度 evaluate | HP `scripts/engines_shadow_evaluate_gold.py` | 月频快照 ann/MDD/Calmar（月复利口径）/corr(A,gold)/trailing12 corr/rolling12 → append B.shadow.evals[]；monitoring 计数连续 corr>0.15；baseline 已首跑（evals=1，clean_evals=0 正确起点） |
| 4. cron 待装包 | VPS `hp-cron-pending/gold_shadow_evaluate.cron`（3 日 09:38 append + 09:40 evaluate，**5 字段标准格式**）+ `gold_mmf_push_vps.cron` + `gold_mmf_push.py`（VPS 侧 MMF 推送助手）+ INSTALL.md 追加章节 | **未安装**（安装属不可自动清单） |

回滚路径：HP `model/registry_backup_task0484_20260824.tar.gz`（67 项 registry 全量，sha256 076eeaea3e736024…）→ 删 engines.json B 条目 + 删两脚本 + 删 results/engines/gold/ 即完全回退；A/A2 引擎零依赖新文件。

## 二、数据源决策（HP 自包含优先的实测依据）

- **黄金 ETF 518880 日线：HP 自包含（选定）**。腾讯 `web.ifzq.gtimg.cn` fqkline 在 HP 实测可用（socket+HTTP 双通，全史 3179 根 2013-07-29..2026-08-24，回溯分页每窗 ≤640 根）；eastmoney 全系在 HP HTTP 层 RemoteDisconnected（带 UA 亦断）故弃用 akshare eastmoney 族。**与 r482 冻结源逐月末收盘核对 158/158 完全一致（maxdiff=0）**；E2 冻结信号复刻对照 156 完整月：w maxdiff 4.6e-05（vol60 舍入级）、gold_ret maxdiff 4.6e-13、net maxdiff 1.1e-06——方法论移植成立。
- **MMF 000198 万份收益：VPS 月推（HP 无免费稳定源）**。实测：sina CaihuiFundInfoService.getNav 返回 0 行、腾讯 fund API 404/未定义方法、eastmoney 断连；VPS 侧 eastmoney f10 lsjz 实测可用，`gold_mmf_push.py` dry-run 2026-07 月收益 0.000718079382561 与冻结 seed 逐位一致（r483 同为 0.0007180793825602994）。断供 ≥2 月 = 终止判据 c3。

## 三、A/A2 不变性证明（「不影响在役」判定基准）

1. 前置 tar 备份 67 项全量（见上）。
2. B 条目以**文本拼接**插入 engines 数组闭合前：改动前后 A/A2 前缀（前 5992 字节）**逐字节不变**（`new[:cut]==old[:cut]` True，cut=old.rindex('\n  ]\n}')）。
3. baseline evaluate 真跑（json 重写整文件）后：**A、A2 parsed deep-equal 均 True**（写前/写后比对，脚本内置 engine[i]≠B 变化即中止的断言）。
4. sha256 链：before 8dc8b92c5ea9af3b（5999B）→ splice 后 08b045e71f8b9bd4（10404B）→ baseline 后 83c47f3a6529a574（12908B，JSON 合法）。

## 四、baseline 首跑快照（HP 实测，2026-08-24T15:15:04+08:00 HP 本地）

| 指标 | 值 | 校验 |
|---|---|---|
| nav_period / n_months | 2013-08-31..2026-08-31 / 157 | = r483 基线段 ✓ |
| ann | 0.07590979342570381 | 与 E2 V1_net 0.07590979342573179 差 <1e-12（seed CSV 12 位浮点序列化所致）✓ |
| mdd / calmar | −0.05901780820812119 / 1.286218443728281 | 同上逐位复现 E2 ✓ |
| corr(A,gold) | −0.0400133462997406 | **逐位复现 E2 G6 −0.04001334629967728**（差 <1e-12），n=131，窗 2013-08-31..2024-06-30 ✓ |
| rolling12_ann | 0.135009 | 近 12 月（2025-09..2026-08，尾月部分月） |
| trailing12 corr | 0.2327（历史尾窗 2023-07..2024-06） | **baseline 不计入连续计数**（防 a13 锁定 NAV 历史尾窗污染；月度模式优先活 NAV results/paper-nav.csv，不可用则 corr_stale=true 拒计数） |
| clean_evals | **0** | 影子期自 2026-08-24 起，首全月 eval=2026-09，0 为正确起点 ✓ |

## 五、B 条目判据要点（engines.json shadow 段落位）

- 终止四条沿 R-273/R-259 §5.4 框架 + 黄金特有，数值标注「R-304 冻结+用户确认时点」：c1 连续低于门槛（N 默认 3 待确认）；**c2 独立性：G6 实测 −0.0400（n=131）为基线，复看线 trailing12 corr>0.15 连续 6 月（R-305 结合表观察线，monitoring 计数器落地），STOP 上限占位 0.20**；c3 数据断供 ≥K 月（默认 2，含 MMF 推送断供）；c4 用户手动。
- 牛市依赖监控（G3 FAIL 披露延续）：rolling12_ann vs 全样本 ann 偏离 + 段 Calmar（2013-19 Calmar 0.902 vs 2020+ 2.296）；定位=风险调整+分散（用户选①原文入 audit.changes）。
- promotion=激活/层2 ERC/真金分配=用户人工门，永不自动化。

## 六、发现与遗留

1. **A2 旧待装 cron 文件为 6 字段格式，HP crontab 实为标准 5 字段**（实测 `30 16 * * 1-5`）——安装时会整行失效，INSTALL.md 已记修正提示（未改旧文件，非本任务范围）。
2. seed 尾月 2026-08 为冻结日部分月收益（至 2026-08-24），append 自 2026-09 起全月口径（engines.json base_segment 已标注）。
3. corr(A,gold) 月度模式依赖活 NAV：results/paper-nav.csv 存在（A 在役 paper engine）；若停更则 corr_stale 拒计数并标注。

## 七、验收与可复核命令

```bash
# HP ~/quant-evolve
/home/noname/miniconda3/envs/quant/bin/python -c "import json;d=json.load(open('model/registry/engines.json'));print([e['engine_id'] for e in d['engines']],d['engines'][2]['slot'])"
wc -l results/engines/gold/shadow_nav.csv          # 158（头+157）
/home/noname/miniconda3/envs/quant/bin/python scripts/engines_shadow_evaluate_gold.py --mode baseline --dry-run --engines model/registry/engines.json
/home/noname/miniconda3/envs/quant/bin/python scripts/engines_shadow_nav_gold.py append --dry-run --out results/engines/gold/shadow_nav.csv --mmf-file results/engines/gold/mmf_monthly_push.csv  # 幂等退出
tar -tzf model/registry_backup_task0484_20260824.tar.gz | wc -l   # 67
crontab -l | grep -c gold                          # 0（未安装）
# VPS
python3 hp-cron-pending/gold_mmf_push.py            # MMF dry-run
```

## 产物清单

- HP：engines.json（+B，A/A2 不变）+ registry_backup_task0484_20260824.tar.gz + scripts/engines_shadow_nav_gold.py + scripts/engines_shadow_evaluate_gold.py + results/engines/gold/{shadow_nav.csv, shadow_nav_seed.csv, mmf_monthly_push.csv}
- VPS：hp-cron-pending/{gold_shadow_evaluate.cron, gold_mmf_push_vps.cron, gold_mmf_push.py} + INSTALL.md 追加章节 + work/task-0484-notes.md

## 来源

R-304/R-305（task-0483：work/r483/{e2_prereg.json sha256 40ae2657…, e2_gates_result.json, e2_nav_monthly.csv, e2_backtest.py}）；R-286（task-0464 四件套先例）；R-259（§4/§5 影子契约与治理框架）；R-273（影子期判读规则）；R-303（E1 画像与形态边界）；全部判门/NAV 数字引自 r483 落盘 JSON/CSV（报告内零心算）。

过程笔记：work/task-0484-notes.md
