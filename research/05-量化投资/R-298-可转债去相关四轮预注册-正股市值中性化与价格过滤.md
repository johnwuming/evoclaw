# R-298 可转债轮动 E2 四次预注册（正股真实市值中性化 + 价格下限过滤 · V6/V7）

- 任务号：task-0479（task-0474/R-295 对症续推；用户 2026-08-24 18:19 指示「任务474继续推进」）
- 日期：2026-08-24（**本文件写定于任何 E2 四次回测计算之前**；正股市值原始数据采集为数据准备阶段，与回测计算分离，采集规则亦于本节 §四 冻结后才进入回测消费）
- 状态：**预注册完成，待执行**。全部参数与判门一经登记不可事后修改；任何追加=新预注册+独立计块+报告明示 post-hoc。
- 性质：影子研究线。零真金、零引擎改动；不动 registry/paper_engine/HP crontab，不碰 HP。激活（若有）=用户人工门。
- 链路：R-281(E1) → R-285/R-288(预注册) → R-287/R-289(执行；V2 六门全过但 corr 0.583>0.5) → R-293/R-295(三次：正股行业中性化 corr 0.622，负结果) → R-297(csad 线负结果) → **本预注册（四次）**。

## 一、动机与机制假设（依据 R-289 §6 + R-295 §3 死因核验）

1. R-289 §6 判定：V2 与在役微盘引擎 a13 的 corr 源头 =「低价/小市值」维度（流动性溢价/小市值共同驱动），不由行业介导。
2. R-295 实证：行业中性化（V3）反把选券自由度让渡给该维度使 corr 恶化至 0.622；组合级行业上限（V4）无效；仅降权低价腿（V5，0.7→0.5）力度不足（0.599）。
3. R-289 §6 ①：发行规模静态代理对「真实流通规模」表征弱。
4. **本轮对症**：① 规模中性化变量从「发行规模百分位」换成「正股总市值」（时变、逐月截面，直接表征小市值共振维度）；② 低价维度硬约束（价格下限过滤，直接砍掉与微盘共振最深的超低价券段）。

## 二、冻结试验形态（n_trials ≤ 4 独立计块）

**V2 冻结基线（全部变体继承，零修改）**：Top-30 平方根梯度权重 w=(31−r)^0.5 归一；缓冲区 BUFFER=50；信用过滤链（上市≥25日、评级 rk≤4 即 AA- 及以上、黑名单 12 券、发行规模≥2亿、close 非空）；S=0.7P+0.3D−0.1M（P/D/M 为截面百分位 rank，fillna 0.5）；成本 0.10% 单边按买卖腿数/N 计提；组合月频调仓，形成期 2018-01..2026-06，收益窗 2018-02..2026-07（102 月）。

| 试验 | 形态（相对 V2 的唯一/叠加差异） |
|---|---|
| **V6（主试验）** | 规模中性化变量从 `issue_sz` 百分位换成**正股总市值** `MC` 百分位：逐月截面 OLS 残差化 S ~ [1, MC_pct]（实现方式与 R-288 规模中性化同构，仅换变量；MC_pct 为当月过滤后 universe 内正股总市值 rank pct，缺失 fillna 0.5）。其余全部继承 V2。 |
| **V7（主试验）** | V6 + **价格下限过滤**：形成期 universe 增加 `close ≥ 105 元` 硬约束（作用于打分与入选）。 |
| S1（敏感性） | V7 但价格下限 110 元。 |
| S2（敏感性） | V6 无价格过滤 + **低价×小市值交叉降权**：S_adj = (0.7P+0.3D−0.1M) − 0.2 × P_pct × (1 − MC_pct)，再对 S_adj 做 V6 同构正股市值残差化。 |

n_trials = 4（V6、V7 计胜；S1、S2 仅披露敏感性）。V2 复现为 G0 环境对拍，不计 trials。

## 三、判门（沿 R-288/R-293 冻结口径，零放宽）

- **G0 环境对拍（先跑）**：V2 复现 corr(a13_locked) = 0.583（与 R-289/e3 V2_G0 逐位一致，|Δ|<1e-9）+ IC 均值对拍 dual_low 0.0451 / price 0.0966。G0 不过 → 停止，查环境漂移，不得进入判门。
- **G1 超额门**：主窗年化超额 vs 000832 ≥ +5.0pp。
- **G2 回撤门**：月频净值 MDD ≥ −20%。
- **G3 分段门**：前窗(2018-02..2022-07)/后窗(2022-08..2026-07) 年化超额均 > 0。
- **G4 换手门**：月均单边换手 ≤ 40%。
- **G5 容量门**：Cap_m = Σ当月全部持有券(10%×ADV)（ADV=月末前 20 交易日日均成交额，volume×close 近似），主窗月度中位数 ≥ 2000 万元；缺成交额月披露占比，>30% 判 FAIL。新选中未落盘券按 g5_fetch2.py 模式增量补采（限频 0.5s、重试≤2、300s 时限，失败登记不阻塞）。
- **G6 独立性门（判胜核心）**：V6/V7 主口径 corr(月收益, a13_rsraw_e1f10dz_locked_nav.csv)（重叠窗，与 V2 对拍同窗）**< 0.5**；另披露 102 月全窗 vs i3_base、i3_abs_s4。
- **裁决**：V6/V7 任一 G0–G6 全 PASS → 达标形态；G1–G5 过但 G6 FAIL → 负结果；部分改善（0.5≤corr<0.55）如实披露为「影子期观察线候选」。
- **IC 衰减监控**：信用过滤后 universe（E1 口径，与 R-289/R-295 逐位同源）dual_low / price_only 全窗均值 + 滚动 6 月；连续 6 月 IC<0 → 复看登记。

## 四、停止规则与数据披露

**停止规则**：V6/V7 若 G6 仍 >0.5 → 负结果归档，**不再扩网格翻案**。行业（V3）、规模代理（V2 发行规模）、正股真实市值+价格（本轮 V6/V7）三路径试尽后，剩余选项交用户裁决：① 影子期 corr≤0.55 观察线；② 剩余余额中性化（免费源缺，需数据采购）；③ 关闭可转债 B 引擎候选线。

**正股总市值数据（本轮新增）**：
- 源：akshare `stock_zh_valuation_baidu(indicator=总市值, period=全部)`，单位亿元；885 只正股（bond_stock_map.parquet 全集），限频 0.5s、重试≤2、失败登记不阻塞。
- 月度化规则（冻结）：取每月最后一个观测（源为中位 9-11 天间隔的准日线采样）；月内无观测则前向填充（ffill limit=3 月）；回测窗内缺失 MC_pct fillna 0.5。
- **PIT 披露**：百度总市值 = 历史收盘 × 历史总股本（时变股本内生，优于当前股本快照近似）；但月内最后观测距月末可有约 1-2 周采样误差——与 R-293 行业快照同级的近似限制，**显性声明为非严格 PIT**。落盘 `work/r474/stock_mktcap_monthly.parquet` + md5。
- 其余面板缺口继承 R-285/R-288 §四声明（强赎/下修公告缺失、摘牌月近似、评级/黑名单/行业 2026-08 快照含前视成分、票息不建模、同收盘成交近似）。

**机器可读锁**：`work/r474/e4_prereg.json`（sha256 随落盘记录于执行报告）。**先写后算取证：本 md 与锁 json 的文件系统时间戳必须早于 `e4_backtest.py` 首跑产物。**

## 五、与在役结合收益（继承用户历史诉求，R-297 范式）

若 V6/V7 任一 G6 < 0.5：补月收益线性结合表（w=0/25/50/75/100%，指标：corr vs a13、年化、MDD、Sharpe、Calmar；n=重叠月数注明）。全 FAIL 则跳过。

---
取证行：本预注册 2026-08-24 写定；执行=R-299（task-0479）。
