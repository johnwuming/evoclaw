# R-313 看板黄金引擎呈现实施（task-0491，数据同步补齐 + paper 实时小区块，最小范围）

## 背景与范围
- 用户 09:23 拍板：只做「数据接线① + paper 小区块②」，组合视图已砍（由 R-314 承接）。
- 目标：VPS 看板（agent-dashboard，`:8055`）量化 Tab「跨引擎影子」区块正确呈现黄金引擎 `gold_trend_sma200`（active）+ 其 157 月影子曲线 + paper 实时状态小卡片，且新增数据不破坏 390x844 无横向滚动约束。

## 现状缺口（实查确认）
1. VPS `/root/.openclaw/workspace-quant/results/engines.json` 为旧版（5999B，仅 A active / A2 shadow），缺 gold 条目；
2. `/root/.openclaw/workspace-quant/results/engines/` 目录为空，gold 四件套（shadow_nav.csv / paper_state.json / mmf_monthly_push.csv / shadow_nav_seed.csv）未同步；
3. 黄金 paper 实时链无 API 端点、无前端区块；
4. **根因**：VPS 侧 `auto_sync_notify.py`（cron */30 分钟 + 每日 03:00 全量）的 `MIRROR_INCLUDES` 清单缺 engines.json 与 engines/ 目录——这就是看板数据停在 8/23 的根因（非 HP 未推送，HP `sync_to_vps.sh` 本身 include *.json/*.csv 已覆盖）。

## 实施内容

### ① 数据同步补齐（VPS 侧 + 同步链根因修复）
- 备份 VPS 旧版 `results/engines.json.bak-task0491-20260825-093559`（5999B）；
- 新 `results/engines.json` = HP `model/registry/engines.json` 精确副本（md5 `3b07d833…`，14649B）：A active / A2 shadow / **gold_trend_sma200 active**（type=standalone_active，shadow.mode=standalone_b，nav_path=`results/engines/gold/shadow_nav.csv`，evals 含 ann 0.0759 / mdd -0.059 / calmar 1.286 / corr_A_gold -0.040，promotion 含激活记录）；
- 建 `results/engines/gold/`，四件套 md5 与 HP 逐一一致（shadow_nav=seed=`3654c3e8…`、mmf=`cc8de822…`、paper=`f709177f…`）；
- **同步链根因修复**：备份 `auto_sync_notify.py.bak-task0491-20260825-101326`，`MIRROR_INCLUDES` 最小 diff 补 3 行（`--include=engines.json` / `--include=engines/` / `--include=engines/**`）。dry-run 实测输出含 engines、engines/a2、engines/gold 四件套 → include 生效；
- **下次同步触发时点**：HP 数据更新后 VPS auto_sync cron 每 30 分钟自动镜像；每日 03:00 `--force-notify` 全量兜底；手动 `python3 /root/.openclaw/workspace-quant/scripts/auto_sync_notify.py`。HP 侧 `sync_to_vps.sh` 无需改动（已天然覆盖），副本备份在 `/tmp/task0491/hp/sync_to_vps.sh`。

### ② server.js（5 处最小 diff，语法 OK，服务重启 active）
1. **parseShadowNavCsv 表头驱动列定位**：gold CSV 表头 `month,w_applied,gold_ret,mmf_ret,gross,net,nav` 的 nav 在第 7 列，原通用解析器固定取第 2 列会把 w_applied 当 nav（改码前实测 157 点 nav 全错）。按表头列名定位 nav/ret，无表头保持原行为（a2 平铺 `date,nav` 兼容不变）。**属解析器增强而非映射逻辑改动，理由已记录**；
2. **新端点 `GET /api/quant/engines/:id/paper`**：由 shadow.nav_path 目录推导 `paper_state.json`；返回 status / current_weight / last_signal{px,sma200,vol60_ann} / marks(date,nav,px) / open{month,nav_open} / months_closed / activated_at(registry promotion) / updated_at；文件缺失 ok:false 优雅降级；
3. 前端 loadPaperQuant：shList filter 加 `standalone_b`，并行拉取 shadow-nav + paper，签名与传参接通；
4. **renderCrossEngineShadowCard**：standalone_b 引擎出卡（单线影子曲线，无 parent 对照）+「✅ 已激活（影子期豁免）」徽标 + eval 摘要（ann/mdd/calmar/corr(A,gold)）+ **paper 实时小区块**（激活日期 / 当前仓位 w=0（全现金 MMF）/ 信号 px/SMA200/vol60 / 最新 mark NAV / marks 数 / 已结月）。

## 验证（全部实际执行）
| 项 | 结果 |
|---|---|
| `GET /api/quant/engines` | ok:true，3 引擎，gold=active / evals=1 ✓ |
| `GET /api/quant/engines/gold_trend_sma200/shadow-nav` | source=nav_path，**157 点**（2013-08-31 nav=1.0038 … **2026-08-31 nav=2.6046**），nonzero=157 ✓ |
| `GET /api/quant/engines/gold_trend_sma200/paper` | current_weight=**0**、marks=1（2026-08-24 NAV 1.0）、months_closed=0、activated_at=2026-08-25T00:35:00+08:00 ✓ |
| 无头浏览器 390x844 截图 | `r313-quant-390x844.png`：bodyScrollW=**390**、scrollW=390、quantScrollW=370，**无横向滚动** ✓ |
| 无头浏览器 1440x900 截图 | `r313-quant-1440x900.png`：bodyScrollW=1440 ✓ |
| DOM 断言（390 档） | 跨引擎区块含 gold 引擎卡 + 已激活徽标 + eval 摘要 + paper 实时（w=0 全现金 / 信号 / 最新 mark / marks 1）+ canvasCount=3（影子曲线已绘制），9 项全 true，page errors none ✓ |
| 备份在位 | VPS `engines.json.bak-task0491-20260825-093559`、`auto_sync_notify.py.bak-task0491-20260825-101326`、HP `sync_to_vps.sh`（/tmp/task0491/hp/）✓ |

## 约束合规
- 未改 HP crontab / registry / evolution_pipeline / paper_engine* / paper_engine_gold；未删任何数据；报告数据来自 `work/task-0491-notes.md`；
- 截图与 DOM 断言均确认 390 档 bodyScrollW=390，量化 Tab 无横向滚动。

## 交付物
- 正式报告：本文档（`05-量化投资/R-313-看板黄金引擎呈现实施.md`）
- 截图：`tools/agent-dashboard/r313-quant-390x844.png`、`r313-quant-1440x900.png`
- 过程笔记：`work/task-0491-notes.md`
