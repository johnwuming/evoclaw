# R-310：paper_engine 备用行情源降级链实施报告

- **任务号**：task-0488（任务中心 127.0.0.1:8055）｜R-310
- **执行日期**：2026-08-25（HP 本地时间 2026-08-24 22:23–22:32 UTC）
- **执行者**：子 agent（主会话分派）
- **前置任务**：R-308（task-0486，fetch_spot_closes 东财单源）；R-309（task-0487，旧链退役，单引擎单链）

## 背景

R-308 已为 `scripts/paper_engine.py` 增加 `fetch_spot_closes()`：交易日 15:05 后经东财快照（akshare `stock_zh_a_spot_em`）取持仓当日真实收盘价，失败回退 parquet（T+1 回填）。近期 **HP→东财 spot 端点连接被重置**（RemoteDisconnected，重试 2 次失败），当日行持续降级为 T+1 回填——完整性不受影响，但延迟 +1 运行日。用户 2026-08-25 05:51 批准增加新浪/腾讯备用源（R-308 待决策第 3 项）。

## 方法

### 1. 源可用性实测（HP 直接 curl，2026-08-25 06:0x GMT+8，行情为 08-24 收盘数据）

| 源 | HP 连通性 | 批量 | 格式/口径依据 |
|---|---|---|---|
| 新浪 `hq.sinajs.cn/list=sh600000,...` | ✅ 需 `Referer: https://finance.sina.com.cn` | ✅ 逗号拼接 | GBK；`var hq_str_sh600000="名称,开盘,昨收,最新价,高,低,...,日期,时间";` → **逗号索引 3=最新价**，收盘后即当日收盘价（实测 600000→9.220） |
| 腾讯 `qt.gtimg.cn/q=sh600000,...` | ✅ 无需特殊头 | ✅ 逗号拼接 | GBK；`v_sh600000="1~名称~600000~最新价~昨收~开盘~..."` → **`~` 索引 3=最新价**（实测 600000→9.22，与新浪一致） |
| 东财（akshare） | ❌ RemoteDisconnected（本任务动机） | — | — |

北交所代码（4/8/92 开头）新浪/腾讯不覆盖：`_mkt_sym` 返回 None 跳过，由调用方现有"取价不全则整体弃用"门兜底，口径安全不变。

### 2. 代码改造（最小 diff，先备份）

- 备份：`cp scripts/paper_engine.py scripts/paper_engine.py.bak.r310_202608242223`（66959 bytes）
- 文件：HP `~/quant-evolve/scripts/paper_engine.py`（66959 → 70504 bytes）
- **改动函数与行号**：
  - L52：新增 `import re`（新增源解析需要）
  - L349-350：常量 `SPOT_TIMEOUT=10`（每源单请求超时秒）、`SPOT_BATCH_MAX=40`（单次批量代码上限，防请求过大）
  - L353-360 `_mkt_sym()`：6 位码→sh/sz 前缀；非沪深返回 None
  - L362-364 `_err_brief()`：异常摘要压缩至 120 字符（保证"每级失败只 log 一行"）
  - L366-392 `_spot_from_sina()`：新浪批量快照解析
  - L395-421 `_spot_from_tencent()`：腾讯批量快照解析
  - L423-469 `fetch_spot_closes()` 重写为降级链：**东财 → 新浪 → 腾讯 → 空 dict 回退 parquet**；每级失败仅一行 log（源名+异常摘要），取到价即返回
- **零改动**：其余全部函数、NAV 口径、append 门、append_nav 幂等逻辑、crontab、paper_engine_gold.py、registry、evolution_pipeline.py。diff hunk 全部落在 347-467 与 52 行区间。

### 3. 返回值口径

三源均为"最新价"字段：东财快照列 `最新价`、新浪逗号索引 3、腾讯 `~` 索引 3；15:00 收盘后最新价即当日收盘价。qfq parquet 前复权锚定最新=原始价，与 baostock qfq 当日值口径一致（R-308 已论证），本任务以实测三方比价复核（见下）。

## 核心发现与验证结果（全部 PASS）

1. **真实东财现状复现**：HP 上 `ak.stock_zh_a_spot_em()` → `('Connection aborted.', RemoteDisconnected('Remote end closed connection without response'))`——确认东财不可达是当前真实状态，非偶发。
2. **备用源直连**：8 只持仓（300824/002107/603551/000848/300009/600867/002027/601600）新浪 8/8、腾讯 8/8，**两源逐只价格完全一致**（如 600867=7.95、601600=9.50、300824=7.52）。
3. **降级链路径实测（mock 东财失败，真实网络）**：
   ```
   [22:29:44] ⚠️ spot 源1/3 东财(akshare) 失败: mocked: dongcai endpoint reset [R-310 chain test]
   [22:29:44] ℹ️ spot 源2/3 新浪 取价成功 8/8
   ```
   返回 8/8，值与新浪直连全等。
4. **三源全挂兜底分支**：mock 三源均失败 → 4 行 log 链完整（源1/2/3 失败 + `spot 全源失败，回退 parquet 口径`），返回空 dict，调用方按既有逻辑走 parquet T+1 回填。
5. **parquet 口径一致性（08-24 收盘）**：8/8 全等（parquet=新浪=腾讯，如 000848=8.43、002027=4.88），CONSISTENCY_BAD_COUNT=0，差异 0%（远优于 <0.1% 要求）。
6. **生产级真实 daily 运行（非 mock，东财真挂）**：
   ```
   [22:31:19] === 每日净值更新 ===
   [22:31:25] ⚠️ spot 源1/3 东财(akshare) 失败: ('Connection aborted.', RemoteDisconnected(...))
   [22:31:25] ℹ️ spot 源2/3 新浪 取价成功 8/8
   [22:31:27] ✅ 净值更新: 总资产 ¥98,319.00 | NAV 0.983190 | 持仓 8 只 | 模型 a13_rsraw_e1f10dz
   ```
   ——**这即是最强证据：当前东财真实故障下，daily 已自动经新浪取到全量当日价，当日行不再依赖 T+1 回填。**
7. **幂等冒烟（daily 连跑 2 次）**：
   - nav 行数 8 → 8（不变）；末行 `2026-08-24,0.98319` → 同（不变）；无重复日期（uniq -d 计数 0）
   - state：`last_daily=2026-08-24`、`model_version=a13_rsraw_e1f10dz`、`last_data_date=2026-08-24` 三项前后不变
8. **py_compile PASS**（补丁后、加 import re 后各一次）。
9. **无越界改动**：scripts/ 仅 paper_engine.py 及备份变化；results/ 4 个文件为 daily 常规重写（值不变）；crontab 未动。

## 结论

R-310 完成。fetch_spot_closes 已具备四级行情源韧性：东财（akshare）→ 新浪 hq.sinajs.cn → 腾讯 qt.gtimg.cn → parquet T+1 回填兜底。在东财当前真实故障下，HP 模拟实盘 daily 已实测自动降级至新浪并成功取价 8/8，当日行落盘延迟恢复为当日（16:30 daily 运行即写当日行），完整性与 NAV 口径零变化。每个源超时 10s、批量上限 40 只，单级失败仅一行日志，不会放大 daily 运行时长。

风险与备注：
- 新浪/腾讯为非官方免费接口，未来可能变更格式或加鉴权；解析已按最稳字段（索引 3 最新价）+ 宽松失败处理（异常→下一级），即使全部失效仍回到 R-308 之前的 parquet 兜底，不会劣化。
- 北交所持仓若未来出现，备用源不覆盖时会触发"不全则弃用"→回退 parquet（与东财缺码行为一致），口径安全。
- 建议复看点：东财端点恢复后（可用 `curl` 复测），降级链自然回到源 1，无需任何操作。

## 来源

- HP 实测命令与输出：`/tmp/r310_chain_test.py`、`/tmp/r310_allfail_test.py` 运行日志（本报告引用片段）
- daily 运行日志：HP `~/quant-evolve` 控制台输出（2026-08-24 22:31 UTC 两次）
- 改动前代码：`scripts/paper_engine.py.bak.r310_202608242223`
- 过程笔记：`shared/results/work/task-0488-notes.md`
