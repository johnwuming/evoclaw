# R-320 量化系统与 Dashboard 抽象合并精简方案

> 任务：task-0498 · 状态：方案设计（零代码改动，批准后才进入实施）
> 日期：2026-08-27 · 取证：server.js 14942 行全量端点/前端调用对照 + HP 主机只读盘点
> 红线：不影响在役引擎 / paper 链 / registry / F6 呈现；本方案阶段不改任何一行代码

## 结论先行

| 维度 | 现状 | 精简后 | 幅度 |
|---|---|---|---|
| quant API 端点 | 60 个 | 29 个 | **删 31 个（52%）**，其中 29 个纯删 + 2 个随队列出清（详见 §4） |
| server.js 死代码 | — | 删前端死岛 ~1453 行 + 后端死 handler ~1507 行 | **约 3000 行 ≈ 全文件 20%** |
| 版本类端点 | 5 端点 / 4 数据源 | 2 端点（registry + versions 统一视图） | 合并 3 个 |
| VPS 磁盘 | workspace-quant 2.1G | ~0.1G（dashboard 只读 results/ + model/） | **释放约 2.0G** |
| HP 脚本目录 | 213 个脚本 / 50387 行 | ~24 个在役留在 scripts/，其余归档 | 归档 189 个 |
| HP crontab | 24 活行 | 24 行全保留 | **零死行，不动** |

一句话：**一半的量化端点没有活消费者，最大的两块死代码是两个整页死 UI（选股模型页、旧回测页）及其独占的 16 个端点；再叠加 7 个 deprecated、8 个零调用端点。HP 侧 cron 链路健康，瘦身对象只有脚本归档与 VPS 冗余数据副本。**

---

## 一、现状盘点

### 1.1 服务端（VPS tools/agent-dashboard/server.js，14942 行）
- 端点总量 60（清单见 /tmp/r320_quant_endpoints.txt），前端内嵌同文件，统一 `api('quant/xxx')` helper（L7532）+ `post()` 包装。
- 已知 deprecated 7 个（task-0332 quantDeprecated）：L1854/1855/1856/1857、L2684/2685、L3581。
- 零调用死端点 8 个：paper 旧四件套 L2036/2053/2077/2106（新 paper/* 前端在用）、baseline/nav L2772、baseline/yearly L2794、endtoend L4190（385 行 handler，读的 timing_iter3_*.csv **已不存在**，纯僵尸）、engines/shadow-nav flat 别名 L3800。

### 1.2 前端量化 Tab（6 子 Tab，_V5_TABS L9468）
数据 data | 因子 factor | 回测 v5btlc（含 F6 模块 R-319、版本排行表） | 模型 v5model | 模拟实盘 paper | 迭代历史 v5hist。
- 各段体量：因子段 ~2400 行最大，paper 段 ~650 行，回测段 ~653 行。
- **两个整页死 UI**：`quant-page-models` div L7425 与 `quant-page-btlc` div L7426。前笔记判「无写入者」不成立，实情更严重——有完整 loader 但永不可达：
  - 死岛 A（选股模型页）：`loadModelsQuant` L11377-11885（~509 行）。唯一调用点 L11696 是 quantEnqueueAction 的 idea 回调，写入永不可见 div；switchQuantTab（L9483-9500）只派发 6 个 V5 loader。
  - 死岛 B（旧回测页）：`loadBtlcQuant` L11887-12830（~944 行）。唯一触发 `btlcOnVersionChange` L11952 仅出现在其自渲染 HTML 的 onchange（L11928）→ 自引用闭环，无引导入口。
  - 两岛全部 onclick/onchange 处理器（L11510/11614/12308/12495/12678）均只在岛内 HTML 字符串中出现，无任何活 loader 引用（逐函数 external_refs 验证）。

### 1.3 数据与外部消费方
- Dashboard 只读两处：QUANT_REPORTS_DIR=L2120 `/root/.openclaw/workspace-quant/results`、QUANT_MODEL_DIR=L3579 `.../model`。该目录另有 venv 1.1G + data 913M，dashboard 不读。
- HP 反向调用：无（HP 全部脚本 grep `api/quant` 零命中）。
- VPS 其他工具：仅 task-0286/0287/0288 历史任务文档提及，非活消费方。
- 同步链：HP `sync_to_vps.sh` + cron_paper_daily/rebalance.sh rsync → VPS shared/results/04-投资研究（239M）。

---

## 二、重复功能矩阵

### 2.1 版本列表类（重复度最高：5 端点 4 数据源 3 个活消费点）

| 端点 | 行号 | 数据源 | 状态 |
|---|---|---|---|
| version-options | L3318 | versions-manifest.json | 活（V5 模型切换器 L9666、V5 回测切换器 L9764、排行表 L10258） |
| registry | L2431 | registry/*.json + active/pending | 活（V5btlc L9777、paper L13176） |
| models | L2959 | model/registry ⊕ manifest ⊕ v0_seed ⊕ archive 四源合并（~271 行 handler） | **死岛 B 独占** |
| evolution/models | L3583 | model/main.json + sota.json + history.jsonl | 活（paper L13171） |
| history | L3384 | manifest + decisions | 活（v5hist L10416） |

**合并映射**：`models` 直接删（死岛 B 独占）；`evolution/models` 与 `history` 的版本元信息统一并入一个 `versions` 聚合端点（参数 view=switcher/history/evolution），canonical 源 = registry + manifest + model/main.json。前端 4 个消费点改为同一 helper。

### 2.2 趋势图类（删死留活后无重复）
| 图 | 数据端点 | 行号 | 状态 |
|---|---|---|---|
| 策略vs基准净值 | active/curves | v5DrawNav L10330 | 活，V5 回测主图 |
| F6 因子贡献曲线 | f6-curves | v5F6CurveHtml L10014 | 活，R-319 在役 |
| 影子净值趋势 | engines/:id/shadow-nav | L3756 | 活，paper Tab（L9771/13184 动态拼接） |
| 多版本端到端对比 | e2e-curves | btlcE2E* L12260-12445 | **死岛 B 独占，删** |
- 重复点不在数据而在渲染：内联 Chart.js 拼接代码在死岛 A（L11360-11374）与 v5DrawNav 各有一份模板。P1 抽一个共享折线图 helper，3 个活图统一调用。

### 2.3 净值/指标卡类
- paper 四件套 paper/summary+nav+portfolio+trades（L3626/3658/3911/3943）vs 旧四件套 paper-summary 等（L2036/2053/2077/2106）→ 旧的删，新的保留但 P1 合并为 1 个 `paper/state` 聚合载荷（现 4 个端点被同一 loader loadPaperQuant L13152 一次并发拉取，天然可聚合）。
- engines 与 engines/:id/paper（L3681/3809）：engines 列表 + 单引擎明细，保留；P1 可并入 paper/state 的 per-engine 视图（低优先）。
- baseline/summary（L2756）活（paper L13171 动态版本参数），保留；baseline/nav、yearly、meta 全删（后两者分别零调用/死岛独占）。

### 2.4 paper 写操作队列（机制级死代码）
POST /api/quant/action（L4053）+ GET action-queue（L4104）的设计意图是「前端入队、主 agent 心跳消费」（注释 L11668）。取证：生产者 UI（quantEnqueueAction L11685）只被死岛 A 的 4 个按钮调用；消费者侧 heartbeat.sh / HP 脚本 grep 零命中 → **无生产者无消费者的完整死机制**。当前 A2 流水线写操作实际走任务中心（AGENTS.md 量化链路）。

---

## 三、无用功能清单（每项带证据行号）

### 3.1 端点层（31 项）

**A. deprecated 一行式（7）**：summary L1854、nav L1855、factors L1856、evolution L1857、microcap/status L2684、microcap/phases L2685、evolution/summary L3581。

**B. 零调用死端点（8）**：paper-summary L2036、paper-nav L2053、paper-trades L2077、paper-portfolio L2106、baseline/nav L2772、baseline/yearly L2794、endtoend L4190（385 行，读已不存在的 TIMING_ITER3_* CSV，常量 L4126-4128 随删）、engines/shadow-nav flat L3800。

**C. 死岛 A 独占（8）**：timing-config L2417、decisions L2448、pending L2471、ideas L2525、ledger L2543、timing-matrix L4891（数据文件仅剩 shared 副本，随删标注）、action-queue GET L4104、action POST L4053（§2.4）。

**D. 死岛 B 独占（8）**：reports 列表 L2143（注意 reports/:id L2171 活，v5hist L14042/14065 在用）、q4b-contrast L2870（修正前笔记：其唯一前端调用 L12735 属死岛 B 的 loadQuantValidationLayer）、gates L2915、dsr L2943、models L2959、baseline/meta L2818、btlc L4575、e2e-curves L4689。

### 3.2 前端层
- 死岛 A：L11377-11885（~509 行）——loadModelsQuant/renderModelsQuant/决策时间线/想法池表单/quantEnqueueAction/quantRefreshQueueBadge/renderActionQueueBadge。
- 死岛 B：L11887-12830（~944 行）——loadBtlcQuant/renderBtlcPage/版本切换器/四层归因链/对比卡/净值图/年度/危机/walkforward/历代最优/报告库/btlcE2E*/loadQuantBaselineCard/loadQuantModelLayer/loadQuantValidationLayer/drawDsrCurve。
- 孤儿 div：L7425、L7426。
- loadQuantLifecycleLayer 薄壳（L12836-12848）唯一调用者是死岛 B（L12567/12607）；其渲染函数族 renderLifecycleLayer/drawLifecycleScatter 被活 V5 回测调用（L10218/10246）→ **保留渲染函数，删薄壳**。

### 3.3 数据/磁盘层
- VPS /root/.openclaw/workspace-quant：venv 1.1G + data 913M 无消费方（dashboard 只读 results 38M + model 1.4M）→ 归档后释放 ~2.0G。
- shared/results/04-投资研究/timing_matrix/：死岛 A 独占数据。
- HP ~/quant（220K qlib-verify）、~/quant-backups 9.9M、~/openclaw-backup 43M、quant-evolve/backup_paper_state_20260812 28K：归档候选。
- HP scripts/__pycache__ 46 项。

---

## 四、合并精简方案（保留/合并/删除三档）

### 删除档（P0，纯删除零行为变化）
上表 3.1.A+B+C+D 共 31 端点中，29 个纯删；POST action 与 action-queue 归入删除但附迁移注记（若未来重启远程写操作，改走任务中心 API，不复活队列）。对应死岛 A/B 前端、孤儿 div、TIMING_ITER3 常量、loadQuantLifecycleLayer 薄壳一并删除。结果：60 → 29 端点。

### 合并档（P1，行为等价重构）
1. **版本视图统一**：evolution/models + history + version-options → `GET /api/quant/versions?view=`（switcher/history/evolution），数据源收敛为 registry + manifest + model/{main,sota}.json；前端 4 消费点换 helper。29 → 27 端点。
2. **paper 聚合载荷**：paper/summary+nav+trades+portfolio → `GET /api/quant/paper/state`（一次返回），loadPaperQuant 改单请求。27 → 24 端点。
3. **共享折线图渲染 helper**：active/curves、f6-curves、shadow-nav 三图共用一个 Chart.js 模板函数，删三份内联拼接。

### 保留档（不动）
registry、engines、engines/:id/shadow-nav、engines/:id/paper、factor-catalog、factor-ic-series、active、active/pos、active/curves、f6-curves、timing、freshness、consistency、run-status、risk-status、crowding、data-health、data-assets、reports/:id、baseline/summary、history 数据并入 versions 视图后原端点退役。F6 模块、影子链、paper 实时链（R-313/315）、一致性自检（task-0359）全部原样。

---

## 五、分期改造计划

### P0 纯删除（工作量：小，半天）
- 范围：§3.1 全部 31 端点 handler + §3.2 前端死岛 + §3.3 VPS venv/data 归档（先 mv 到 staging 观察 7 天再清）。registry/paper_engine/引擎文件零改动；HP crontab 零改动。
- 验收：① 服务 `node --check` 通过且启动无报错；② F6 模块（f6-curves → v5F6CurveHtml）渲染正常；③ 影子趋势（engines/:id/shadow-nav，paper Tab）曲线正常；④ paper 实时四端点（paper/state 前的 summary/nav/trades/portfolio）返回 200 且数值与删前快照一致；⑤ v5hist 报告详情（reports/:id）正常；⑥ 任务中心（:8055）与 dashboard 完全解耦回归：任务增删查不受影响；⑦ 60 端点删后全量 curl 冒烟（删的返回 404、保留的返回 200）。
- 回滚：git 单 commit 纯删除，`git revert` 即回；VPS venv/data 用 mv 不 rm，反向 mv 恢复。

### P1 合并（工作量：中，1-2 天）
- 范围：versions 统一视图、paper/state 聚合、共享图表 helper。新增端点与旧端点并存灰度一周（旧端点 P0 已删的除外，此处指 version-options/history/evolution/models/paper 四件套的并存期），前端切换后再删旧。
- 验收：versions 三个视图返回与原三端点逐字段 diff 一致（python 脚本对比落盘）；paper Tab 视觉与数值回归快照对比；P0 全部验收项复跑。
- 回滚：并存期直接切回旧 helper；切换后保留旧端点代码两周再物理删除。

### P2 架构级（工作量：大，按需立项）
- 范围：① server.js 前后端分离（HTML/JS 抽静态文件，quant API 抽独立 router 模块）；② HP scripts/ 189 个一次性脚本移入 scripts/archive/（仅 mv，git 不跟踪的加 README 索引）；③ HP 三个备份目录合并归档到统一 archive 区；④ VPS workspace-quant 收敛为纯数据镜像目录（results+model）。
- 验收：模块拆分后端点行为全量回归（沿用 P0 冒烟清单）；HP cron 24 行逐行执行日志确认正常两轮周期；归档区有索引文件。
- 回滚：P2① 按模块拆分逐个 revert；P2②③④ 为纯 mv，反向 mv 恢复。

### 全程红线复述
- 不动 registry active 条目、不动 paper_engine/paper_engine_gold 及其 cron、不动 F6/影子/paper 实时呈现、不动 HP 数据链 6 行 cron、方案阶段零代码改动。

---
*证据索引：/root/.openclaw/workspace/shared/results/work/task-0498-notes.md（过程笔记，含全部 grep 命令与行号）；/tmp/r320_quant_endpoints.txt（60 端点清单）。*
