# R-321 前端可视化模块级精简方案（R-320 增补·纯设计，零代码改动）

- 任务号：task-0499 / 报告 R-321
- 日期：2026-08-27
- 作者：子 agent 实查产出；取证方法：对 tools/agent-dashboard/server.js（825KB/14942 行）grep/awk 定位函数边界与 innerHTML 段，逐 Tab 列模块，禁止全读
- 背景：R-320（task-0498）覆盖死代码/API/HP 侧；本报告补齐**用户可见的前端模块层面**：重复矩阵、视图合并、砍后效果图
- 结论效力：零代码改动；F6 模块（R-319）、影子趋势（R-315）、paper 实时（R-313）为近期上线在役，本报告对其只做「并入折叠/瘦身徽标」类非破坏建议，删除建议全部带证据+替代方案

---

## 一、逐 Tab 可见模块清单（活 UI，取证行号）

公共横件（量化页全局）：一致性自检点 `quantConsistDot` L7416（`consistency`，task-0359）+ 数据更新条 `quantFreshness` L7417（`freshness`）。

| Tab | # | 可见模块 | 组件函数 | 数据源端点 | 行区间 | 任务 |
|---|---|---|---|---|---|---|
| **模型 v5model** | M1 | 版本选择器（下拉） | v5VersionSelHtml | version-options | L9613-9639 | task-0349 |
| | M2 | 头卡（版本名+ACTIVE/历史徽章+数据源角标+策略行） | renderV5Model | active?v= | L9698-9723 | — |
| | M3 | 指标卡×6（年化/回撤/夏普/卡玛/月胜率/月换手）+窗口chips | v5MetricCardsHtml+v5WinChips | active.windows | L9724-9726 | — |
| | M4 | 模型解释三层卡（选股/择时/交易） | v5ExplainHtml | active.explanation | L9727-9729 | — |
| | M5 | 择时仓位趋势图（月度仓位系数线） | v5DrawPos | active/pos | L9730-9755 | — |
| **回测 v5btlc** | B1 | 引擎评估指标徽标行（每引擎年化/回撤/Calmar/corr/来源） | v5EngineEvalFrontHtml① | engines.evals / version-options.windows | L9921-9965 | R-315 |
| | B2 | 影子回测趋势对比图（A2+gold 同图，月度化，首月=1） | v5EngineEvalFrontHtml② | engines + engines/:id/shadow-nav | L9966-10011 | R-315 |
| | B3 | F6 组合回测趋势图（F6主线/A单独/A降仓/gold单独/F1/F7/沪深300+上证虚线） | v5F6CurveHtml | f6-curves?indexes=hs300,szzs&f7=1 | L10014-10066 | R-319 |
| | B4 | 版本选择器（与 M1 同组件） | v5VersionSelHtml | version-options | L9835 | task-0349 |
| | B5 | 指标卡×6+curveWin 窗口chips（与 M3 同组件同源） | v5MetricCardsHtml | active.windows / curves.strategy_metrics | L9838-9841 | — |
| | B6 | 回测趋势·策略vs基准净值图（全期/3y/1y chips；策略+hs300+微盘等权三线） | v5DrawNav | active/curves?v= | L9842-9861, L10330-10396 | — |
| | B7 | 全版本排行表（年化/回撤/夏普/卡玛 4列×N版，点击联动） | v5RankTableHtml | version-options（full 窗口） | L9862, L10265-10329 | task-0382 |
| | B8 | 引擎级生命周期折叠面板（默认折叠） | r315LcPanelHtml | engines+lifecycle+registry | L9863-9867, L9887-9908 | R-315 |
| | —B8内 | 引擎切换器 | v5EngineSwitcherHtml | engines | L10187-10203 | task-0473 |
| | —B8内 | 引擎因子/模型卡（registry entry+因子权重+selection/timing/overlay） | v5EngineFactorModelBlock | registry | L10139-10186 | task-0473 |
| | —B8内 | 生命周期层：管线/影子观察/决策时间线/实验台账/迭代轨迹散点 | renderLifecycleLayer→qLifecyclePipeline/Shadow/Timeline/LedgerTable/ScatterSection | lifecycle | L12891-13151（Shadow L12952） | R-315 |
| **迭代历史 v5hist** | H1 | 迭代历史分页列表（行内 locked 年化/回撤/夏普+features+最近决策） | renderV5HistList | history?page | L10435-10479 | task-0388 |
| | H2 | legacy 旧周期隐藏开关 | v5ToggleLegacy | — | L10400-10409 | task-0359 |
| | H3 | 详情抽屉（报告式弹层：选股参数/Gate评估(verdict+holdout段指标)/Locked指标卡/Full指标卡/机制解释/决策记录） | v5OpenHist+v5LoadHistDetail+renderV5HistDetail | history/:vid | L10468-10602 | task-0388/R-313 |
| **数据 data** | D1 | 数据健康校验卡组（checks PASS/FAIL+财务/估值/PIT/幸存者偏差 4 卡，缓存回退灰卡） | renderDataQuant a) | data-health | L10637-10694 | R-206 M1.1/task-0287 |
| | D2 | 数据资产盘点表 | renderDataQuant b) | data-assets | L10696-10748 | R-206 M1.6 |
| **因子 factor** | F1 | 类型 Tab 栏（9类+全部，角标 n达标/n测试） | renderFactorTypeTabs | factor-catalog | L10874-10892 | M1.2 |
| | F2 | 因子注册表（分组折叠 14 列表，行展开 36 月 IC 图） | renderFactorRegistry+onFactorExpand | factor-catalog+factor-ic-series | L10893-10982, L11000 | M1.2 |
| | F3 | 在役因子 IC 监控（多线+模式切换） | renderFactorIcMonitor+loadIcMonitorChart | factor-ic-series×n+data-health | L11055-11131, L11119 | M1.3 |
| | F4 | 因子相关性簇（折叠卡） | renderFactorClusters | factor-catalog | L11132-11183 | M1.4 |
| **模拟实盘 paper** | P0 | paper↔active 行为一致性徽标 | 内联 | paper/summary.rules_align | L13794-13801 | task-0352 |
| | P1 | 策略描述行（闸门动态拼装） | quantSelDescParts | registry/evolution/models | L13803-13807 | task-0387 |
| | P2 | 指标卡×6（当前净值/累计/当月/下次调仓日/运行天数/持仓数） | 内联 stat-card | paper/summary | L13810-13816 | — |
| | P3 | 运行状态条（产物 mtime 红绿灯） | renderRunStatusBar | run-status | L13237-13286 | task-0278 M4.4 |
| | P4 | 净值曲线（择时仓位双轴或纯净值） | renderNavWithPosChart/renderNavChart | paper/nav+timing | L13507-13573, L13987 | M4.1 |
| | P5 | 跨引擎影子卡（每影子引擎一张：影子NAV vs parent在役NAV 双线；gold 单线+激活徽章+paper 实时小区块+clean_evals 进度） | renderCrossEngineShadowCard | engines+engines/:id/shadow-nav+engines/:id/paper+active/curves | L13574-13743 | R-259/R-313/R-315 |
| | P6 | 当前持仓可解释表（入选理由展开+组合概览） | renderHoldingsExplainable | paper/portfolio+summary | L13294-13378 | task-0375 |
| | P7 | 最近交易记录表 | 内联 | paper/trades | L13843-13865 | — |
| | P8 | 运行模型版本卡（registry active 一致性徽章+择时层状态） | 内联 | evolution/models+registry | L13867-13882 | — |
| | P9 | 微盘拥挤度卡 | renderCrowdingCard | crowding | L13379-13468 | task-0278 M4.7 |
| | P10 | 退出纪律卡 | renderRiskCharterCard | risk-status | L13469-13506 | task-0278 M4.8 |
| | P11 | 策略参数&采纳因子 | renderAdoptedFactors | evolution/models | L13884-13912 | A4 |

**活 UI 内死代码簇（不占视觉，占行数）**：factor Tab 旧版因子库链 `buildMergedFactorSummary` L11189 / `factorRowsFromSummary` L11211 / `factorFilterRows` L11231 / `renderFactorTable` L11247 / `onFactorSearch` L11322 / `onFactorPage` L11327 / `factorTableRoot` L11317（依赖 DOM id=`factorTableRoot`，新版 DOM 是 `factorRegistryRoot` → 整链不可达，~190 行）+ `renderFactorIcChart` L11348-11375 零调用死函数。**附带 bug**：新版分组头 onclick=`onFactorGroupToggle`（L10927）→ 该函数只更新不存在的 factorTableRoot → 新版分组折叠点击无效果（静默失效），P0 修复。

---

## 二、信息类型重叠矩阵（量化「同一信息出现 N 次」）

### 2.1 趋势图类（净值曲线）

| 信息 | 出现处（活 UI） | 所在 Tab+模块 | 端点 | N |
|---|---|---|---|---|
| **A 引擎（active 现役）回测净值** | ① 策略vs基准主图（日频，全期/3y/1y）② F6 图 a_alone+a_dd 序列（月频 2013-08 起）③ paper 影子卡 parent 在役线（strategy.full 月度化 2006→今） | v5btlc B6 / v5btlc B3 / paper P5 | active/curves / f6-curves / active/curves | **3 处活**（死岛另 2 处：B'2 btlc、B'8 e2e） |
| **gold 净值（影子）** | ① 影子对比图 gold 线 ② F6 图 gold_alone ③ paper gold 卡单线+paper 实时 | v5btlc B2 / v5btlc B3 / paper P5 | shadow-nav(gold) / f6-curves / shadow-nav+engines/:id/paper | **3 处活** |
| **A2 影子净值** | ① 影子对比图 A2 线 ② paper A2 卡（影子 vs parent 双线） | v5btlc B2 / paper P5 | shadow-nav(A2) | **2 处活** |
| **指数叠加（沪深300/上证）** | ① B6 hs300 基准线 ② F6 hs300+szzs 虚线 | v5btlc B6 / v5btlc B3 | active/curves.benchmarks / f6-curves.indexes | **2 处活**（死岛另 2 处：B'3 年度表基准列、B'8 e2e 5指数） |
| paper 实际净值（模拟盘自身） | 仅 P4 一处 | paper P4 | paper/nav | 1 处（唯一） |

设计者自证分段：B2 图脚注原文「A 引擎（a13）等价长回测 nav 未并入本图，见下方『回测趋势·策略vs基准』」→ 明确知道 A 曲线应统一，但分了两张图。

### 2.2 指标卡类（年化/回撤/Sharpe/Calmar 等）

`v5MetricCardsHtml` 组件渲染点：M3、B5、H3×2（locked+full）= **4 次**。
同源指标数字（active.windows / engines.evals / version-options.windows）全量渲染点：

| # | 渲染点 | 所在 Tab+模块 |
|---|---|---|
| 1 | 指标卡×6 | v5model M3 |
| 2 | 指标卡×6（同组件同源） | v5btlc B5 |
| 3 | 引擎评估徽标行（ann/mdd/calmar/corr；A 类回退 version-options.windows 与 M3/B5 同源） | v5btlc B1 |
| 4 | F6 脚注（年化/回撤/Sharpe） | v5btlc B3 |
| 5 | 排行表 4 列×N 版本 | v5btlc B7 |
| 6 | 列表行内 locked 年化/回撤/夏普 | v5hist H1 |
| 7 | 详情 locked 指标卡×6 | v5hist H3 |
| 8 | 详情 full 指标卡×6 | v5hist H3 |
| 9 | 生命周期台账表 ann/mdd/sharpe 列 + 散点 tooltip | v5btlc B8 |

→ **同一维度指标数字在活 UI 至少 9 个渲染点、同源数据 6 处**。B1 徽标行的 A 类回退指标与 M3/B5 完全同值（active.windows），是最高优先级去重点。

### 2.3 版本/引擎列表类

| 信息 | 出现处 | N | 说明 |
|---|---|---|---|
| 版本全量列表 | M1/B4 下拉（交互控件）+ B7 排行表（version-options）+ H1 分页列表（history） | 3 | 数据源 2 个（version-options / history）；下拉是交互非展示，B7 vs H1 形态不同（对比表 vs 分页流水）——非实质重复 |
| 引擎列表 | B8 引擎切换器（生命周期视角）+ P5 影子卡遍历（对照视角） | 2 | 视角不同，保留 |
| 单值版本徽章 | M2 头卡 + P8 运行版本卡（registry 一致性） | 2 | 语义不同（展示 vs 运行态核对），保留 |

### 2.4 持仓/交易记录类

持仓仅 P6、交易仅 P7，各 1 处，**无重复**（活 UI 唯一）。

### 2.5 其余跨模块重复

- **影子观察进度（clean_evals）**：2 处 = B8 qLifecycleShadow（版本级，lifecycle.shadow_watch，L12963）+ P5 影子卡徽标（引擎级，engines.evals，L13560）。粒度不同，建议 B8 为准、P5 徽标保留（引擎卡上下文）。
- **仓位系数**：2 处 = M5（active/pos 独立图）+ P4（paper/nav+timing 双轴）。数据源不同、语义不同（模型窗口 vs 实盘运行），保留。
- **决策记录**：2 处 = B8 qLifecycleTimeline（全局）+ H3 详情（单版本）。粒度不同，保留。
- **因子展示**：3 粒度 F2 全库 / P11 在役采纳 / B8 引擎绑定，非实质重复。
- **端点跨 Tab 重复拉取**：registry（v5btlc+paper）、active/curves（B6+P5）、engines（v5btlc 2 次+paper 1+n）、version-options（v5model+v5btlc）、shadow-nav（B2+P5）、data-health（data+F3）——同会话切 Tab 重复拉取。

---

## 三、死岛复活候选评估（loadModelsQuant L11377-11885 / loadBtlcQuant L11887-12830）

### 3.1 活 UI 已覆盖 → 不复活，随死岛埋掉

| 死岛模块 | 活 UI 替代 | 判定 |
|---|---|---|
| A2 决策时间线（decisions） | B8 qLifecycleTimeline（decision-log，L12985） | 已覆盖 |
| A5 试验台账（ledger） | B8 qLifecycleLedgerTable（experiment-ledger，L13014） | 已覆盖 |
| A3 Pending 确认 | 队列无消费者（R-320 双死）；裁决已进 B8 生命周期 | 已覆盖/无效 |
| A1 作战室控制面 | M2 头卡 + P8 运行版本卡 | 已覆盖 |
| A7/A8 选股/择时模型+仓位系数 | M2/M4/M5 | 已覆盖 |
| A6 择时×SOTA 贡献矩阵（timing-matrix） | timing_matrix 数据 VPS 无消费方、timing_iter3 CSV 已不存在（R-320 僵尸）；择时当前单一（q3z×EW-MA200） | 埋掉 |
| A4 想法池（ideas） | ideas/pool.jsonl 仍被 HP runner 消化，但前端入口无消费者 | 埋入口（池保留在数据层） |
| B'1-B'6 归因链/年度/危机/WF/历代最优 | **活 UI 无对应**——见 3.2 | 见下 |
| B'7 报告库列表 | reports 仅 3 个历史 md，非活跃 | 埋掉（见 3.3） |
| B'8 e2e 多版本净值 | B6/B3 已覆盖净值；e2e-curves 读已不存在的 CSV | 埋掉 |
| B'9 基线卡 | v5btlc B5 指标卡 | 埋掉 |
| B'10 模型层版本切换器 | M1/B4/B7 | 埋掉 |
| B'12 生命周期薄壳 | 活版已迁 B8 | 埋掉 |
| B'11 q4b-contrast | Q4B 一次性研究池对比，活 UI 无对应但非持续信息 | 埋掉 |

### 3.2 独有且需评估（深度回测分析）

btlc 端点（L4575）由 `btlcResolveLayers` L4270 / `btlcBuildYearly` L4401 / `btlcBuildCrisis` L4433 / `btlcBuildGenerations` L4487 **动态计算**，读 model/main.json 真源 → 分年度收益、危机段回撤（2008/2015/2018/2021-2024）、Walk-forward 三窗 OOS、历代最优对比**有持续数据支撑**，且 v5btlc 活页未展示。
**建议**：不并入 P0。当前 v5btlc 定位为「总览页」（指标卡+净值+排行+生命周期），深度分析属「历史研究回顾」；若用户确认需要，**P2 在 v5hist 详情抽屉追加「回测深度分析」折叠区**（复用 btlcBuild* 产物，不复活整页）。不做也不损失日常信息。**本方案默认埋掉，标注为 P2 可选复活项**。

### 3.3 报告详情（openQuantReportDetail + reports/:id）

组件 L14029 全部 4 调用点（L12205/12239/12598/12599）均在死岛 B；reports 列表仅 3 个历史 md（a7-iteration-report / factor-expansion-report / R-188-quant-evolve-Phase1）。**埋掉**；v5hist 详情已是报告式抽屉（task-0388/R-313），无 md 原文刚需。若后续 HP 产出新 md 报告，P2 可把「查看报告原文」入口加进 v5hist 详情（保留组件+端点，低优先）。

### 3.4 结论

**无高优先级复活项**。唯一数据活跃的独有信息是「回测深度分析」（3.2，P2 可选）。DSR 折扣曲线（示意阶梯为硬编码假数据 L12806-12816）、五门禁面板（读 8 月 16 日 v0_seed 中性态）均为死数据——**verdict/gate 核心信息已在 H3 详情「Gate 评估」区呈现**，无需复活面板。

---

## 四、视图合并方案

目标信息架构：**「一条曲线只讲一次、一个指标只算一次、一个列表只出一份」**。对在役件（F6/影子/paper 实时）只做非破坏调整。

| 层级 | 对象 | 动作 | 证据 | 替代方案 |
|---|---|---|---|---|
| **保留** | B3 F6 图、B2 影子对比图、P5 影子卡+paper 实时、B8 生命周期折叠、H3 详情抽屉、P0-P11、M1-M5、F1-F4、D1-D2、H1-H2 | 不动 | 均为近期上线/唯一信息源 | — |
| **合并①** | B1 徽标行去指标数字（只留引擎状态徽章+来源） | 瘦身 | B1 的 A 类回退指标与 M3/B5 同源同值（active.windows）；corr/来源仅 B1 有 → 保留 | 指标看 M3/B5 指标卡；B6 曲线 meta 行（年化/回撤）已在曲线下 |
| **合并②** | B2 影子对比图移入 B8 生命周期折叠面板内 | 并入折叠 | B2 与 P5 同源 shadow-nav；影子是生命周期的「观察中」环节，语义属 B8；v5btlc 首屏 3 张净值图重叠严重 | P5 保留首屏（在役 vs 影子实际对照），B2 折叠后按需展开 |
| **合并③** | 会话级数据缓存（registry/active/curves/engines/shadow-nav/version-options/data-health，TTL 30s） | 新增前端缓存 | 跨 Tab 重复拉取同一端点（见 2.5） | 纯性能，不改变视觉 |
| **删除（并入 P0）** | 死岛 A1-A9+B'1-B'12+动作链；factor 死簇（renderFactorTable 链+renderFactorIcChart）；onFactorGroupToggle 修复为更新 factorRegistryRoot | 删 | R-320 P0 已列端点；死簇 grep 不可达 | 对应端点随 R-320 删；onFactorGroupToggle 改指向新版 DOM |
| **可选（P2，待用户拍板）** | v5hist 详情加「回测深度分析」折叠（3.2）；gate 明细折叠（H3 数据源改 registry 真源）；报告原文链接 | 新增（非必做） | 见三 | 不做则埋掉 |

### 合并后每个 Tab 模块数变化

| Tab | 前 | 后 | 变化说明 |
|---|---|---|---|
| 数据 | 2 | 2 | — |
| 因子 | 4（+~190 行死簇） | 4 | 视觉不变，去死簇+修折叠 bug |
| 模型 | 5 | 5 | — |
| 回测 | 8 | 8 | 首屏 7 块 + B2 移入 B8 折叠（折叠面板内 6→7 子块）；B1 瘦身不减块数 |
| 模拟实盘 | 12 | 12 | — |
| 迭代历史 | 3 | 3 | （可选 +1 深度分析折叠，P2） |
| 公共横件 | 2 | 2 | — |
| **合计可见模块** | **36** | **36** | 模块数不变，**首屏图密度 -1、重复指标数字 -2 渲染点、死代码 -1690 行** |

> 说明：本方案以「不破坏任何在役功能」为第一原则，合并不改模块总数，靠「折叠化+瘦身+去重」降低首屏信息密度与代码体积。真正的砍（-9 个死岛模块群、-1690 行死前端、-29 死端点）已在 R-320 P0 消化，用户视觉上表现为「页面更短、无不可达页、加载更快」。

---

## 五、页面效果图（文字版·合并后逐 Tab 自上而下）

**数据 Tab**（不变）
1. 数据健康校验卡组（通过 n/m · 更新时间；checks 卡 + 财务/估值/PIT/幸存者偏差 4 卡）
2. 数据资产盘点表

**因子 Tab**（视觉不变，内部清死码+修折叠）
1. 类型 Tab 栏（9 类+全部）
2. 因子注册表（分组折叠 14 列表 · 行展开 36 月 IC）
3. 在役因子 IC 监控（多线图）
4. 因子相关性簇（折叠卡）

**模型 Tab**（不变）
1. 版本选择器
2. 头卡：版本 + ACTIVE/历史徽章 + 数据源角标 + 策略行
3. 指标卡×6 + 窗口 chips（锁定/全窗口）
4. 模型解释三层卡（选股/择时/交易）
5. 择时仓位趋势图

**回测 Tab**（首屏 6 块，B2 进折叠、B1 瘦身）
1. 引擎评估徽标行（纯状态徽章：A ✅在役 · A2 👁影子 · gold 👁影子，来源标注；无重复指标数字）
2. 版本选择器
3. 最新版回测标题 + 指标卡×6 + 窗口 chips
4. 回测趋势·策略vs基准净值图（全期/近3年/近1年；策略+沪深300+微盘等权）
5. F6 组合回测趋势图（R-319 保持）
6. 全版本排行表（4 指标排序 · 点击联动）
7. 🧭 引擎级生命周期视图（默认折叠）：
   - 引擎切换器 → 引擎因子/模型卡
   - 📈 影子回测趋势对比图（A2+gold，自首屏移入）
   - 生命周期层：管线 → 影子观察中 → 决策时间线 → 实验台账 → 迭代轨迹散点

**模拟实盘 Tab**（不变）
1. ✅ paper↔active 行为一致性徽标
2. 策略描述行
3. 指标卡×6（净值/收益/调仓日/运行天数/持仓数）
4. 运行状态条（产物新鲜度红绿灯）
5. 净值曲线（择时仓位双轴）
6. 跨引擎影子卡（A2 双线 / gold 单线+paper 实时）
7. 当前持仓可解释表
8. 最近交易记录表
9. 运行模型版本卡（registry 一致性）
10. 微盘拥挤度卡
11. 退出纪律卡
12. 策略参数 & 采纳因子

**迭代历史 Tab**（不变，可选 P2 加深度分析）
1. 迭代历史分页列表（legacy 隐藏开关）
2. 详情抽屉：选股参数 → Gate 评估 → Locked 指标卡 → Full 指标卡 → 机制解释 → 决策记录

---

## 六、实施衔接

### 6.1 与 R-320 分期关系

- **并入 R-320 P0（清死码，同 1 个任务）**：死岛 A/B 前端模块（A1-A9、B'1-B'12、动作链）删除 + 对应 29 死端点删除 + factor 死簇（~190 行）删除 + `onFactorGroupToggle` 修复（改更新 factorRegistryRoot）。P0 验收补充：`grep -c "renderFactorTable\|renderFactorIcChart\|loadModelsQuant\|loadBtlcQuant\|quantEnqueueAction" server.js` = 0。
- **新 P1.5（前端合并期，1 个独立任务，~0.5 天）**：合并①B1 徽标瘦身 + 合并②B2 移入 B8 折叠 + 合并③会话级缓存 TTL 30s。**这是 R-320 没有的前端去重任务，属本报告新增**。
- **P2（保持 R-320 计划）**：组件化（图表统一 v5AlignSeries 工具已存在 L9803，可复用）+ 可选复活项（深度分析/gate 明细/报告原文，待用户拍板）。

### 6.2 验收标准

1. 六 Tab 渲染回归：无头浏览器 390x844 逐 Tab 截图，quantHScrollGuard（L9521）生效、无横向溢出；六子 Tab 数据均正常显示。
2. 关键模块数据一致性：同 active 版本同窗口下，M3 指标卡数字 == B5 指标卡数字 == H3 详情 locked 指标卡 == B7 排行表 active 行；B6 曲线 meta 行年化/回撤与排行表一致。
3. `node --check server.js` 通过；dashboard 服务 active；`curl -s localhost:8055/api/quant/registry | head -c 200` 返回 ok。
4. 死代码 grep = 0（见 6.1 P0 验收）。
5. registry active 条目 sha256 与改造前一致（不动在役判定基准，沿用 R-320）。

### 6.3 回滚方式

P0/P1.5 各自独立 git commit（evoclaw 仓库）；执行前 `cp server.js server.js.bak-task0499` 快照。回滚 = 还原文件 + 重启服务。缓存为前端会话态，无持久副作用。

---

## 来源清单

- server.js：loader 行区间 L9658/9756/10410/10603/10750/13152；组件函数行号见第一/二部分逐条
- 死岛行区间 L11377-11885 / L11887-12830；btlc 动态计算函数 L4270/4401/4433/4487
- 数据源活跃度：QUANT_MODEL_DIR L3579、MODEL_REGISTRY_DIR L2853、QUANT_REPORTS_DIR L2120；results/*.md 实测 3 个
- 过程笔记 task-0499-notes.md（T1-T14 原始证据）
