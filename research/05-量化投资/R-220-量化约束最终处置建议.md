# R-220 量化约束最终处置建议（R-219 修订版）

- 任务号：task-0344（续）
- 日期：2026-08-17 13:40 初版 / 13:58 用户确认修订
- 性质：基于 R-219 全量审计（47 条）+ 主 agent 逐条评审 + **用户 13:58 确认修订**的最终版，已获授权执行。R-219 保持冻结作为底稿。
- 总原则（用户 2026-08-17 13:10 决策）：门禁机制改权重机制，不设强制通过门禁，遵循最原始数学模型带来的收益，不加入人工约束规则。

## 一、两个输入源的冲突裁决原则

R-219 审计偏保守（证据型约束倾向保留），13:15 评审偏用户方向（可权重化面更大）。裁决规则：
1. **物理现实与防作弊基建**（C/D 类）：不可权重化，全部保留——这不是"人工约束"，是评估有效性的前提
2. **统计科学性内核**（g2 OOS 检验/g4 DSR）：不搞一票否决，但保留为**最高权重评分项 + 人工复核警示线**——去掉后所有回测数字不可信
3. **宇宙定义类**（四闸门/次新/ST）：权重化=换策略，**必须实验先行**（A9 原始宇宙对照），以新血统并行而非直接动现役
4. **连续参数类**（地板/MA 窗口/系数）：天然权重制对象，直接网格化让数据选

## 二、三档处置总表（最终建议，按 R-219 编号）

### 第一档·立即权重化（低风险，机制自然）

| # | 约束 | 最终建议 | 说明 |
|---|---|---|---|
| 7 | 一票否决制 | **改综合评分制**（核心改造点） | verdict→加权评分，REJECT→低分排序；v5i 型"三项优于现役被拒"不再发生 |
| 2 | g2 OOS 衰减检验 | **最高权重评分项 + 警示线** | p<0.01 或指标显著劣化→"统计警示"标签，上位需人工复核（不自动否决） |
| 4 | g4 DSR | **最高权重评分项 + 警示线** | DSR<0.90→人工复核标签；保 N 计数口径不变 |
| 1/3/5 | g1/g3/g5 | 低权重评分项 | 从未实际卡人，形式化转评分 |
| 11 | OOS split 2021-01 | 评分制内可调参数 | 可做分段敏感性 |
| 18 | E1 护栏 | **改惩罚分（首批最佳对象）** | 已是半软机制；跌越深惩罚越重，斜率可调 |
| 21 | 日历降仓 | 系数网格化 | 0.5→{0, 0.3, 0.5, 0.7} 让数据选 |
| 29 | 排序权重 | **改权重核心对象，与 A8 合流** | 过滤制→全池打分制的第一站；rank-sum vs zscore vs 分桶归因后定 |
| 34 | WF 选参网格 | 可扩格 | 搜索范围限制放开 |

### 第二档·A9 实验裁决后切换（宇宙级变更，不动现役 v5h）

> 用户 13:58 授权：**本档验证后自动决策推进**，不再逐条确认。

| # | 约束 | 最终建议 | 裁决实验 |
|---|---|---|---|
| 14-17 | 四道闸门 | **改质量分进排序（ROE 降权 0.3-0.5），A9 实测后切换** | 原始宇宙 vs 质量宇宙对照回测；若原始宇宙前沿占优→新血统 v6 并行（闸门版继续跑），不直接改现役 |
| 19 | 次新剔除 | **保留硬剔除**（+3.32pp 实测最强增益，去掉=清零 A7 成果）；惩罚分仅作预案 | 已裁决（A7 实测），无需再验 |
| 20 | 涨停剔除（+2.32pp 未上岗） | A9 顺带组合测试 v5h_xsub+limup 复合版 | 证据型规则同次新处理：实测增益则在评分中占位 |
| 23/24 | EW-MA200 系数/择时地板 | **全参数网格实测**：MA{15,20,50,100,200}×floor{0,10,18,30}%×q3z{开,关} | 裁决外部 MA15+空仓建议 vs A7b"MA150 恶化"矛盾；地板含 0% 档 |
| 26/27 | n_hold=20/月度调仓 | 组合构造参数敏感性（{10,20,30}/{月,双月}），低优先 | 非人为约束，网格顺手带过 |
| 30 | min_amt 流动性过滤 | 软化为流动性惩罚项（当前=0 未启用，无紧迫性） | 低危 |
| 39 | ST 剔除 | A9 顺带对照：ST 惩罚分 vs 硬剔除 | ST 一半风控一半收益源，数据说了算 |

### 第三档·保留不动（13:58 修订：原 #8/#13 已移除）

> 用户 13:58 决策：#8 activate 人工确认制**删除**（从未实际使用，改自动上岗）；#13 legacy-grandfathered 豁免 + override TTL **删除**（非迭代评判标准）。

| # | 约束 | 理由 |
|---|---|---|
| 9 | rollback 字节快照冻结 | 安全网，非约束 |
| 10 | locked 窗口≤2024-06-30 | 保留为评分口径（防过拟合基建）；战役目标摘除淘汰线后它承担"可信评估"职责 |
| 22/25 | q3z 及附属参数 | 本质已是连续函数=天然权重制 |
| 28/31/32/33 | 等权/G1 加分/备选池/capital_base | 稳健默认或非策略约束 |
| 35-38 | C类全部（一字板/成本/停牌/执行时点） | 物理现实 |
| 40-47 | D类全部 | 防作弊基建，排除在改动外 |

### 特殊处置

| # | 约束 | 最终建议 |
|---|---|---|
| 12 | 战役目标 25%/-20%/1.2 | **摘除淘汰线属性→前沿展示 + 档位重设**（从未硬淘汰过版本，属方向标）；A8/A9 出数后定新档位（暂定候选 18~20%/-22%~-25%/1.1~1.2，或维持 25 档作为长期愿景分档展示） |
| 37 | paper 月末 vs 回测月首口径不一致 | **独立修复项（bug 级）**，不属权重机制范畴，单独立即修 |
| — | HP CLAUDE.md 文档债 | 顺手清理（一行事实），防误导后续子 agent |
| ~~8~~ | ~~activate 人工确认制~~ | **删除**（13:58 用户：从未使用，改自动上岗；评分第一即自动 activate） |
| ~~13~~ | ~~legacy 豁免 + override TTL~~ | **删除**（13:58 用户：非迭代评判标准） |

## 三、综合评分公式草案（示例权重，动工前可调）

```
综合分 = 0.35 × S_stat    # g2 p值映射 + g4 DSR 映射（科学性内核，最高权重）
       + 0.25 × S_oos     # OOS 段 locked 指标增量（Calmar/Sharpe 归一）
       + 0.15 × S_is      # locked 全窗口指标质量
       + 0.10 × S_dd      # g6 回撤恶化惩罚映射
       + 0.10 × S_corr    # g3 相关性映射
       + 0.05 × S_logic   # g5 逻辑完整性

统计警示线（人工复核标签，非否决）：g2 p<0.01 或 DSR<0.90 → 上位需人工确认
activate 人工确认制保留：综合分第一 ≠ 自动上岗
```

## 四、与 A8/A9 合流执行路线图

1. **[立即] #7 verdict 改造**：evolution_pipeline.py verdict 合成改评分制 + registry 评分字段扩展；回填 47 条历史决策验证排序合理性（等价校验口径不变）
2. **[合流 A8] #29 排序权重**：rank-sum 四因子（对数市值 1.0 + 非流动性 1.0 + PB 倒数 0.7 + ROE 0.3）× {质量宇宙, 原始宇宙} 双版本
3. **[合流 A9] 宇宙与择时裁决**：原始宇宙对照（#14-17）+ 择时网格 MA×floor（#23/24，30 组）+ ST/涨停顺带（#39/20）
4. **[后续] B 类逐条软化**：E1 惩罚分（#18）→ 日历系数（#21）→ min_amt（#30）。第二档自动决策推进（用户 13:58 授权）
5. **[独立] #37 paper 口径修复 + 文档债清理****

每步：registry 版本化 + 等价校验 + decision-log 记录。

## 五、差异说明（R-219 原建议 → 最终建议，仅列变化项）

| # | R-219 建议 | 最终建议 | 变化理由 |
|---|---|---|---|
| 14-17 | 保留（或仅小幅权重化） | 改权重·A9 实测裁决·新血统并行 | 用户方向明确 + 外部诊断指出闸门砍弹性 + A9 已排队；实验先行对冲"换策略"风险 |
| 2/4 | 改权重（高危高权重） | 最高权重 + 人工复核警示线 | 融合"DSR/OOS 不可弃"与"无一票否决"：不否决但打标签 |
| 24 | 保留（地板值可调） | 地板与 MA 窗口进 A9 网格实测（含 0% 档） | 连续参数天然网格化；外部 MA15 建议需实测裁决 |
| 12 | 改权重（重设目标档位） | 摘除淘汰线→前沿展示 + 档位重设 | 实际从未用它淘汰版本，明确其"方向标"本义 |
| 26/27 | 保留（可测敏感性） | 网格敏感性（低优先） | 组合构造参数，非人为约束 |
| 20 | 保留备选 | A9 顺带组合测试 | +2.32pp 证据在手，值得一测 |
| 37 | 保留+修口径 | 独立修复项（bug 级） | 升级优先级 |

## 六、来源

- R-219 全量审计（task-0344，47 条，本文件底稿）
- 主 agent 13:15 逐条评审（背景/去掉影响，含实测引用：E1 ±、次新 +3.32pp、A7b 现金曲线、A7c 动态画像）
- 用户决策：2026-08-17 13:10（权重机制方向）、13:15（逐条展开要求）、13:40（修订最终建议）
- 关联实验：task-0340（A8 合成方式）、task-0342（A9 原始宇宙+择时网格）
