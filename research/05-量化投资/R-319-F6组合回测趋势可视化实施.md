# R-319：F6 组合回测趋势图可视化实施（task-0497）

- 任务：task-0497 | 日期：2026-08-26 | 执行：量化研究链路（R-319）
- 性质：看板只读呈现（VPS 侧 agent-dashboard server.js + 数据镜像），零生产改动——未触碰 HP 任何在役引擎/registry/crontab/生产文件
- 关联：R-316（F6 口径与回测结论）、R-317（F7 组合臂）、R-318（F6 层2实施方案）、R-315（影子趋势图参考实现）

## 一、背景与目标

R-316 完成了 F6（A 引擎 ddc(0.2/0.5) 降仓时黄金补位、总仓位恒满）的回测研究，结论为年化 19.1% / mdd -14.0% / Sharpe 1.197 / Calmar 1.370、13 年终值 9.72 倍；R-317（task-0495）产出 F7a/F7b 组合臂对比。但这些净值序列只存在于 CSV/JSON 产物中，量化 Dashboard 无法直观对比 F6 与独立模型（A 单独 / gold 单独）、影子引擎（A2 / gold shadow，R-315 已有独立图）和宽基指数（沪深300 等）。

本任务目标：把 F6 组合回测净值序列接入量化 Tab 回测页，在「回测趋势」区域（R-315 影子趋势图之后）新增「F6 组合回测」模块，实现 F6 主线 + A 单独 + gold 单独 + 口径1/F1/F7 + 指数叠加的同图月频对比，且数据缺失时优雅降级不报错。

## 二、方法与数据来源

### 2.1 数据源定位（重要修正）

任务书给的数据路径为 HP `~/workspace-quant/work/task-0494|0495/out/`，实际探查（ssh 10.12.192.174:2222）该路径不存在；task-0494/0495 的产物实际位于 **VPS 本地** `/root/.openclaw/workspace/work/task-0494/out/`、`task-0495/out/`（两任务在 VPS 执行），无需 HP scp，直接本地转换落盘。

### 2.2 数据落盘

新建镜像目录 `/root/.openclaw/workspace/shared/results/04-投资研究/f6_curves/`，7 个 CSV（`date,nav` 格式，对齐 e2e_curves/nav_v1.4.csv），各 156 点（2013-08..2026-07 月频）：

| 文件 | 来源 | 终值 |
|---|---|---|
| f6_nav.csv | task-0494 f6_monthly_th20_rd50.csv 的 F6 月收益累乘 | 9.7173 |
| a_alone_nav.csv | task-0495 f7_nav_curves.csv 列 A | 9.5263 |
| gold_alone_nav.csv | task-0495 f7_nav_curves.csv 列 gold | 2.6032 |
| a_dd_nav.csv | task-0494 月收益列 rA_dd 累乘（口径1 现金闲置） | 8.2376 |
| f1_nav.csv | task-0495 列 F1（等权月再平衡） | 5.2295 |
| f7a_nav.csv / f7b_nav.csv | task-0495 列 F7a / F7b | 5.2509 / 5.0451 |

### 2.3 后端 API

server.js（改前备份 server.js.bak-task0497-20260826）新增 `GET /api/quant/f6-curves`：

- 默认返回 f6 / a_alone / gold_alone / a_dd / f1 五序列；`?f7=1` 追加 f7a/f7b；`?indexes=hs300,szzs,...` 指数叠加（复用 E2E_CURVES_DIR 的 index_*.csv + E2E_INDEX_META 元数据）。
- 归一口径：所有序列统一以**窗口起点 2013-07-31 基点=1**（策略序列前插合成基点；指数取该日/之前最后收盘为基点、此后日频月末采样），保证终值倍数与 R-316 报告的 9.72 直接可比。
- 复用 e2e-curves 的 e2eLoadSeries/e2eNormalize 设施与 e2eAlign 思想，时间轴为 YYYY-MM 月度键 + 前向填充（起点前为 null）；指标 f6MetricsMonthly 按月频计算（Sharpe×√12，年化按日历跨度）。
- 空态降级：数据文件缺失 → `available:false` + note，不报错。

### 2.4 前端模块

量化 Tab → 回测页（v5btlc）「引擎评估指标 · 影子回测趋势」区块内、R-315 影子图之后新增 ③「📊 F6 组合回测（A 降仓黄金补位 · 恒满仓）vs 独立模型 vs 指数」：

- Chart.js 折线（与 R-315 同款深色主题配置），默认拉取 `?indexes=hs300,szzs&f7=1`；F6 主线加粗金色（#fbbf24），指数灰虚线区分。
- 图例 chips 含每条曲线起止区间（YYYY-MM）+ 终值倍数（×9.72 等）；图表下方脚注列各序列年化/回撤/Sharpe，并标注基点口径与 R-316 对照。
- 数据链：loadV5Btlc 的 Promise.all 增量拉取缓存 `_v5F6Curves` 并计入 quantSigOf 签名；`available:false` 时显示「F6 组合回测曲线待同步」占位。

## 三、核心发现（结论先行）

1. **上线成功且数字与 R-316 逐项一致**：API 实测 f6 终值 9.7173、年化 19.12%、mdd -13.96%、Sharpe 1.197 ——与 R-316 表格（19.1% / -14.0% / 1.197）及终值 9.72 完全吻合；a_alone 18.9%/-16.95%、gold 7.6%/-5.9%、a_dd 17.6%/-14.2%、f1 13.6%/-8.3% 亦逐项复现，链路数据无损。
2. **同窗指数对比直观化**：沪深300 以 2013-07-31 基点=1 同窗终值仅 ×2.14（mdd -40.6%），F6 的 ×9.72 与 A 单独 ×9.53、F1 ×5.23、gold ×2.60 在同一坐标系下直接可比——F6 收益端最优、F1 风险端最优的 R-316 结论获得可视化呈现。
3. **零生产改动合规**：仅新增 f6_curves 数据目录与 server.js 内追加代码（后端 API + 前端模块），既有 e2e-curves、shadow-nav 等 API 回归 200 正常。
4. 任务书数据路径（HP）与实际（VPS 本地）不一致已核实并修正，报告如实记录，避免后续任务重复走 HP scp 弯路。

## 四、验证记录（全部通过）

1. `node --check server.js` → SYNTAX_OK（改后二次执行同样通过）
2. `curl /api/quant/f6-curves` → 200，available:true，默认 5 序列、时间轴 157 月（2013-07..2026-07）；`?indexes=hs300&f7=1` 时时间轴 158 月（指数 2026-08 月初点）
3. 回归：`/api/quant/e2e-curves?versions=v1.4` → 200；`/api/quant/engines/A2/shadow-nav` → 200
4. `systemctl status agent-dashboard.service` → active (running)，restart 后 pid 1443128
5. 前端仿真（node 提取下发页面中的 v5F6CurveHtml 实跑）：有数据 → 输出含 ×9.72 与 new Chart（FRONTEND_SIM_OK）；空数据 → 显示「待同步」占位、无 chart（EMPTY_OK）
6. 首页 HTML 含新模块代码（grep「F6 组合回测」×6）

## 五、结论与建议

- F6 组合回测趋势已接入量化 Tab 回测页，与独立模型/影子引擎/宽基指数同图对比，口径与 R-316 完全对齐，可作为 R-318 三阶段实施的持续观察视图（paper 影子阶段后可把实盘 sleeve NAV 并入同图）。
- 建议：后续若 HP 侧新增组合臂回测（如 F8），只需向 f6_curves/ 落盘 `date,nav` CSV 并在 F6_SERIES_META 注册一行即可复用整条链路；层2 影子账本（R-318 阶段2）落地后建议新增独立 API 而非混入本只读回视图。

## 六、来源清单

- 数据：/root/.openclaw/workspace/work/task-0494/out/f6_monthly_th20_rd50.csv、f6_results.json（F6 月收益与指标原值）；/root/.openclaw/workspace/work/task-0495/out/f7_nav_curves.csv（A/gold/F1/F7a/F7b 净值原值）
- 落盘镜像：/root/.openclaw/workspace/shared/results/04-投资研究/f6_curves/（7 CSV）
- 指数：/root/.openclaw/workspace/shared/results/04-投资研究/e2e_curves/index_hs300.csv 等（日频收盘）
- 代码：/root/.openclaw/workspace/tools/agent-dashboard/server.js（备份 server.js.bak-task0497-20260826）
- 口径与结论：R-316-A降仓黄金补位满仓方案研究.md、R-317-等权打底降仓补位组合研究.md、R-318-F6中央风控层2实施方案.md
- 过程证据：shared/results/work/task-0497-notes.md
