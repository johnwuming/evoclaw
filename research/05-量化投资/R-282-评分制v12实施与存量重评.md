# R-282 评分制 v1.2 代码实施与存量迁移重评（批准点②③执行报告）

- 任务号：task-0458（P1）｜日期：2026-08-23｜类型：代码实施 + 存量裁决实算（零新回测）
- 批准链：用户 2026-08-23 00:14「全部推进」= 批准点①（v1.2 定稿，门槛数字冻结不可改）→ 本任务执行 **批准点②代码实施 + ③存量迁移重评**。**批准点④影子叠加启动（a14 过线后的下一步）未启动**，仅写清启动条件（§六）。
- 上游：R-267（v1.2 设计，选项B 双轨制）→ 本报告；R-253/R-254（a14 案）、R-264/R-265（a15 案）、R-259（组合层/影子基建）
- 硬红线执行情况：**纯新增文件**（scripts/overlay_adjudicate.py + scripts/combo_sim.py + model/scoring_v12_frozen.json），evolution_pipeline.py 零改动（md5 前后 9c50b188cbc9007bf2e43bc55c6570ed）；registry 仅 a14/a15 条目新增 overlay 块（tar 备份 + 全目录 md5 diff 校验，a13 条目与 main.json 逐字节不变）；零 paper_engine / crontab 改动。

## 一、定稿冻结（model/scoring_v12_frozen.json）

- sha256：`315ee82d1e096ea818f7ca9aea74033e2bcdb7078ee2e361df43a886e99c063d`（HP ~/quant-evolve/model/）
- 冻结内容：O1a/O1b 公式与门槛、O2 分臂三线（信息臂 1.005/0.2pp/−0.3pp，防御臂 0.995/+0.3pp/−0.3pp）、O3（DSR≥0.90、g2 p≥0.01、holdout 沿 v1.1）、指标口径（R-253 逐位复刻）、常态窗定义（locked 日收益剔除危机窗日收益后拼接年化）、迁移条款（a14 防御臂 w=0.5 组件型建议值；a15 信息臂 w=0.3 R-263 预注册因子权重）、trial 计账（+2，全局 97→99）。
- **危机窗清单矛盾裁决（如实披露）**：R-267 §十.4 括注「2008/2015/2018/2020/2022 段」经查 R-252/R-253 全文**不存在**该清单；R-253 E2 唯一预注册危机窗 = W-crisis（2023-09-01→2024-02-29，微盘踩踏段）。依 R-267 本文条款「危机窗清单的初值沿用 R-253 E2 预注册清单、定稿时以该清单为准冻结」，冻结为单一窗口 `2023-09-01→2024-02-29`。新增危机窗只能走 v1.3 升版。该裁决与 R-267 §4.2 自校验数字（T4 危机改善 +1.45pp 取自该窗）自洽。
- 附带口径澄清：R-267 §一表中 T4「常态年化损耗 0.8pp」实为 R-253 §3.2 W-crowd 时代损耗（13.9497%−13.2144%=0.735pp）；按 v1.2 冻结定义（locked 减危机窗）实算 T4 损耗仅 **0.217pp**（更宽松窗但同样过线，不影响任何判定）。

## 二、代码实施（批准点②验收）

| 项 | 结果 |
|---|---|
| scripts/combo_sim.py | 月频再平衡组合模拟器（R-259 层3：月初拉回、月内权重冻结阶梯函数）+ 对账三校验（分解一致性 tol 1e-6 / 端点手工重算 / 权重守恒）。sha256 480a996f… |
| scripts/overlay_adjudicate.py | O1-O3 裁决器，门槛全部读冻结配置（代码零硬编码门槛）；DSR 逐位复刻 evolution_pipeline.deflated_sharpe（只读公式复刻，不 import 避免副作用）。sha256 d9055bd8… |
| py_compile | 两文件通过 |
| 干跑一致性 | 两候选各跑两次（--at DRYRUN）输出 cmp **逐字节一致** |
| 对账三校验（实算运行） | decomp_err ≤3.3e-16、endpoint_rel_err ≤2.9e-15、weight_sum=1.0 精确 → PASS |
| 关键实现口径 | 月频再平衡=月内 buy-and-hold（非月内逐日混合）；月内逐日混合为二阶近似，会影响压线判定，冻结口径为前者 |

## 三、O2 实算（桌面推演线性近似 vs 实算）

locked 窗 2006-01-04→2024-06-28（4491 行，三序列日期索引逐位一致）；基线指标 4-6 位复现归档（a13 ann 0.220176/mdd −0.335542、a14 0.218215、a15 0.225145；a14 危机窗 MDD −0.081138 = R-253 −8.1138% 逐位）。

**a14_crowdf2（防御臂，w=0.5）——实算三线全过：**

| 线 | 门槛 | 桌面推演（R-267 §6.1） | **实算** | 判 |
|---|---|---|---|---|
| ① Calmar 比 | ≥ 0.995 | −0.47%（压线） | **0.9955742（−0.4426%）**，超线余量 +0.0005742 | ✓ 压线过 |
| ② 危机窗混合 MDD 改善 | ≥ +0.3pp | ≈+0.7pp | **+0.7826pp**（combo −0.087847 vs 在役 −0.095673） | ✓ |
| ③ 年化地板 | ≥ −0.3pp | −0.10pp | **−0.0974pp**（combo ann 0.219201） | ✓ |

O2① 压线敏感性如实标注：余量仅 5.7e-4（Calmar 比单位）。combo 全窗 MDD 与在役完全相同（−0.335542，最大回撤段未被改善，①的比值差全部来自年化项）——与 R-267 §4.2 防御臂设计依据一致。**依门槛判：未低于 −0.5%，过线，无需判负。**

**a15_csad_resid（信息臂，w=0.3）——实算判负：**

| 线 | 门槛 | 桌面推演 | **实算** | 判 |
|---|---|---|---|---|
| ① Calmar 比 | ≥ 1.005 | +0.06% | **+0.1457%** | ✗（差 0.35pp） |
| ② MDD 恶化 | ≤ 0.2pp | 0.20pp 压线 | **0.2033pp** | ✗（毫厘超线） |
| ③ 年化地板 | ≥ −0.3pp | +0.15pp | **+0.1656pp** | ✓ |

与桌面推演结论一致：任何忠实权重下混合 Calmar 改善都在噪声级，信息增量组合层不可兑现。

## 四、存量迁移重评（批准点③验收）

1. **tar 备份**：`model/registry_backup_task0458_20260823.tar.gz`（67 项：registry/ 51 json + main.json + 快照）。
2. **overlay 块注入**：a14_crowdf2.json、a15_csad_resid.json 各增 `overlay` 块（schema：scoring_version/intent/arm/o1/o2{w,metrics}/o3/verdict/trials_counted/frozen_config(sha256)/verdict_file/adjudicated{task,at}），与 verdict 文件逐字段对应。
3. **diff 校验**：全目录 52 文件 md5 前后对比，**仅 a14/a15 两文件变化**；文本 diff 纯新增 73 行、0 行删除（overlay 块追加于条目末位，原格式 indent=2 无尾换行保持一致）。
4. **a13 条目**：md5 346450f739830c78bd485ba5869117b0 前后不变；status=active、无 overlay 键。main.json 不变。
5. **排名池**：v1.1 score/rank 语义零变动（overlay 块为新增独立键，不触任何评分字段；池内其余 49 个 registry 文件逐字节不变）。
6. **DSR 计账**：全局 n_trials 97→99（迁移 2 次各计 1 trial）；n=99 复算 DSR：a14 0.9999（T=4490）、a15 0.9999——n_trials 变化对 DSR 无实质影响（方向保守）。

## 五、裁决结论

- **a14_crowdf2：O1b ✓ + O2 ✓ + O3 ✓ → `shadow_overlay_eligible`（影子叠加资格）**。O1b 实算：危机改善 +1.4536pp（R-253 +1.454pp 复现）、常态损耗 0.2167pp、locked ann −0.1960pp，三子门全过；O3：DSR 0.9999（n=99）、g2 p 0.4719、holdout PASS、无 stat_warn。R-267 §6.1「被 v1.1 冤杀者获昭雪」经实算确认成立（O2① 压线过，未触发 −0.5% 判负线）。
- **a15_csad_resid：O1a ✗（严格口径）+ O2 ✗ → `fail_archive`（判负归档）**。O1a：ΔICIR 0.0455（R-264 G2 归档）≥ 宽线 0.04476（余量 1.6%）但 < 严格线 0.06714（post-hoc ×1.5，迁移采用严格口径，R-265 §3.4.2 警示继承）；即使宽口径过 O1a，O2 ①② 亦不过。**死因（从 v1.1 的「差 0.0049 分」改写为「信息增量在组合层不可 monetizable」）**；复活路径沿 R-265 §四.3：降 w / 降换手 / 残差口径强化的全新 E2 预注册（旧档案依 R-263 停止规则不复活）。
- 两轨语义：a14 的资格**不回授替代轨**（不 claim rank1）；v1.1 排名池格局（a13 0.8781 > a15 0.8732 > a14 0.8584 > v4a 0.8088）原样保留。

## 六、批准点④（影子叠加启动）——未启动，启动条件如下

a14 获得的仅是**资格**；启动影子叠加需以下条件全部满足并经用户显式批准（R-267 §4.3、R-259 §5.2 影子契约）：

1. **用户批准点④**（本报告提交后由用户拍板）。
2. **engines.json 存在**（R-259 S2 落地前提；当前 HP 上尚无该文件——本任务未创建）。
3. **A 链路零回归**：A 引擎 active 条目、paper_engine、看板端点输出与启动前基线逐项一致。
4. **影子 NAV 双腿管道**：a14 拥挤度降权变体的月度 NAV 生产源（当前裁决基于 R-252 E2 locked 产物，影子期需月度双腿对账，R-259 §5.3）。
5. **数据管道连续 2 月度点**（R-259 影子契约第 4 条）。
6. **影子期判据适配**（依 R-273 实证）：T4 自 2025-09 起在低拥挤时代行为上≡在役（nav 比值极差 1.3e-04）——低拥挤时段两腿区分度恒为零，clean_evals 须剔除无区分月份或相应延长影子期（建议 3-6 月基础上按高位月出现数延长）；月度 eval 必须实测两腿换手叠加与再平衡拉回成本（R-267 §八.7）。

**engines.json A2 条目草案（不实施，仅供批准点④参考）：**

```json
{
  "engine_id": "A2",
  "parent": "A",
  "type": "sub_engine_overlay",
  "status": "shadow",
  "registry_ref": "a14_crowdf2",
  "overlay": {"w": 0.5, "w_source": "model/scoring_v12_frozen.json migration（R-267 §4.2 组件型）"},
  "shadow": {"mode": "cross_engine", "started_at": null, "min_months": 3, "max_months": 6,
             "eval_rule": "clean_evals 剔除低拥挤零区分月份；月度实测两腿换手叠加与拉回成本"},
  "adjudication": {"verdict": "shadow_overlay_eligible",
                   "verdict_file": "results/overlay_verdict_a14_crowdf2.json", "task": "task-0458"},
  "promotion": "真金权重分配=用户人工门（批准点⑤），永不自动化"
}
```

## 七、验收清单（可执行复核）

```bash
# HP ~/quant-evolve
sha256sum model/scoring_v12_frozen.json scripts/combo_sim.py scripts/overlay_adjudicate.py
md5sum scripts/evolution_pipeline.py model/registry/a13_rsraw_e1f10dz.json   # 9c50b188… / 346450f7…
tar -tzf model/registry_backup_task0458_20260823.tar.gz | wc -l              # 67
/home/noname/miniconda3/envs/quant/bin/python scripts/overlay_adjudicate.py \
  --config model/scoring_v12_frozen.json --candidate a14_crowdf2 --at DRYRUN --out /tmp/re.json  # 可重放
```

## 八、产物清单

- HP `model/scoring_v12_frozen.json`（冻结配置，sha256 315ee82d…）
- HP `scripts/combo_sim.py`、`scripts/overlay_adjudicate.py`（纯新增）
- HP `results/overlay_verdict_a14_crowdf2.json` / `overlay_verdict_a15_csad_resid.json`（裁决全字段）
- HP `results/overlay_o2_combo_a14_w050.json` / `overlay_o2_combo_a15_w030.json` + 两份 combo NAV csv
- HP `model/registry_backup_task0458_20260823.tar.gz`（迁移前快照）
- VPS `work/task-0458-notes.md`（过程笔记/恢复点）

## 来源

R-267（§4.2 公式门槛/§6.1-6.2 桌面推演/§七 迁移/§九 实施顺序）、R-253（窗口口径与 T4 四门数字）、R-252（E2 预注册）、R-254（a14 v1.1 死因）、R-264 L54（ΔICIR 0.0455）、R-265（a15 死因/post-hoc 警示/复活路径）、R-259（层3 组合口径/对账三校验/影子契约）、R-273（T4 低拥挤时代行为≡在役）、R-245（池格局）；全部实算数字可由 §七 命令重放。

过程笔记：work/task-0458-notes.md
