# R-344 新量化看板产品方案 PRD

- 日期：2026-08-28 ｜ 状态：已评审（二轮+三轮评审闭环，task-0533 验收通过）
- 上游依据：R-342《R-336 实施级架构设计（含 Dashboard）》第 4 章与 §3.4 API 契约；R-336《破而后立目标架构与迁移方案》§4/§6/§7/§8 治理语义
- 决策背景：用户 2026-08-28 19:05 裁决「先做产品方案，好了之后自动做技术架构文档」。本 PRD 是新看板开发的需求基线；验收通过后派发技术架构修订（task-0530）。
- 范围声明：纯产品文档，零代码零设计稿。与 R-342 冲突处一律标注「建议 R-342 修订时对齐」，不直接改 R-342。

---

## 1. 产品定位与用户场景

### 1.1 定位

**单用户产品，用户=老板本人。**

新看板不是「文件浏览器」，是**组合治理驾驶舱**：打开即答三问——现在组合什么状态、谁在管风险、迁移到哪一步了（R-342 第 4 章设计立场）。旧的 agent-dashboard 量化 Tab 是「产物文件浏览器」心智（翻报告、看曲线文件），新看板按治理模型重造：以**事件与状态**为骨架，图表为辅、数字优先。

使用形态：手机 390px 为主力视口，碎片时间打开看一眼；桌面为辅。无人值守推送——看板只负责「打开就能看见」，主动通知走既有消息渠道。

### 1.2 四个高频场景与问题清单

**场景 A：每日快查——「现在健康吗？」（每天 1-2 次，30 秒内离场）**

要答的问题：
1. 今天净值多少？比昨天涨跌多少？
2. 当前回撤落在哪一带（正常 / 提级审查 / 降仓 / 熔断）？
3. 三方对账（账本 vs 引擎持仓 vs 版本定义）是绿的吗？
4. 各引擎都活着吗？有没有引擎停更/失效？
5. 数据新不新鲜？净值是今天 mark 的吗？
6. 断路器触发了吗？有没有待处理的风险事件？

**场景 B：周度复盘——「这两周发生了什么？」（每周一次，5-10 分钟）**

要答的问题：
1. 这两周有哪些事件：版本创建/权重求解/门禁评估/晋升/降级/风控动作/对账结果？
2. 漂移四维（日盈亏偏差/Sharpe 偏差/执行率/滑点偏差）走势如何，有没有逼近带宽？
3. 两腿相关性变化？离 0.85 防御线还有多远？
4. 各引擎 IC 最新值与 3 个月趋势，有没有老化迹象？
5. 迁移推进到哪一步了？卡在什么前置上？

**场景 C：风险事件响应——「哪里出问题了？」（事件触发时，第一时间打开）**

要答的问题：
1. 是什么事件触发的（降级/对账失败/断路器/退役触发）？几时发生的？
2. 现在组合处于哪个风控带？触发了什么动作（停新仓/降仓/冻结）？
3. 影响哪个 sleeve / 哪个版本？当前在役版本变了吗？
4. 依据哪条规则触发的？实测值 vs 阈值差多少？
5. 现在需要我做什么（人工门待批 / 仅观察 / 需人工复位）？

**场景 D：审批溯源——「当时的依据是什么？」（不定期，翻旧账）**

要答的问题：
1. 这个版本当时过门禁的成绩单是什么（逐条 pass/fail + 阈值 + 实测值）？
2. 权重是怎么求出来的？用的什么求解器？有没有触发 fallback、为什么？
3. 谁批准的、什么时候批准的（G-L4 唯一人工门）？
4. 这个版本的数据切割日期（data_cut）是什么？用到哪些 sleeve、代码版本（code_hash）对应哪个？
5. 这个版本之后发生过什么（完整事件时间线）？为什么被降级/退役？

---

## 2. 页面清单与导航

### 2.1 导航结构（移动端底部 Tab，5 项封顶）

R-342 §4.5 规定底部 Tab ≤5 项，而区块有 6 个。本 PRD 的归并方案：**引擎卡片挂在总览页第二屏**（它是「组合现在健康吗」的组成部分），其余五区块各占一页。**此处为对 R-342 信息架构图（§4.2 导航直接分支六区块）的细化，建议 R-342 修订时对齐。**

| # | Tab 页 | 一句话定位 | 承载区块 |
|---|---|---|---|
| 1 | **总览**（默认首页） | 30 秒答「现在健康吗」：净值/回撤带位/对账/引擎活性 | ① 驾驶舱（首屏）+ ② 引擎卡片（下滑第二屏） |
| 2 | **风控** | 一页看全所有闸门：回撤带/波动带/相关性/漂移/断路器 | ⑤ 风控闸门 + 待处理事项（P1） |
| 3 | **版本** | 组合版本与状态机：在役是谁、怎么来的、现在什么状态 | ③ 组合版本视图 |
| 4 | **事件** | 全部治理事件的倒序时间线，可过滤可翻页 | ④ 事件流水 |
| 5 | **迁移** | Phase A-D 迁移进度与证据，A1/A2 阻塞项置顶 | ⑥ 迁移进度 |

导航层级：**Tab 页（5）→ 详情层（drawer/底部抽屉，不占 Tab）**。详情层包括：版本详情（全 schema+门禁成绩单+权重方案+求解留痕）、事件详情（payload 展开+触发规则+实测值）、引擎信号明细、对账差异明细。全部用抽屉呈现，不弹模态（R-342 §4.5 规范）。

跨页跳转关系（对齐 R-342 §4.2）：总览点引擎卡→下滑定位引擎卡；总览点在役版本卡→版本页；版本页点状态点/事件→事件页（带过滤）；风控页超带项→事件页（定位对应 risk 事件）。桌面端（>768px）Tab 转顶部导航，三列网格。

### 2.2 全局元素（每页常驻）

- **健康条**：数据新鲜度（同步滞后秒数）+ 投影校验状态。超阈值变黄，显示「数据非最新」横幅（R-342 §4.4）。
- **风险角标**：任一「待处理风险」存在（断路器触发/对账失败/漂移连续超带/退役触发中）时，风控 Tab 与健康条同时出现红点，用户不翻页也能感知。
- **角标计数口径**：角标数字=待处理事项聚合视图（§3 区块⑤ P1）的条目数；聚合字段由后端提供，字段级设计归架构文档（task-0534 承接）。
- 手动刷新：每页下拉刷新；自动轮询分频（§6.1）。

---

## 3. 六区块产品化定义

优先级口径：P0=首发必须，没有它看板不达标；P1=增强，首个调仓周期内补齐；P2=以后（Phase D 后 backlog）。

### 区块 ① 总览驾驶舱（Tab1 首屏）——P0

- **回答什么**：场景 A 全部六问的 1/2/3/5/6 问。
- **关键信息**：净值曲线（P0 带 30 日窗，P1 加 90 日/1 年切换）；日变动%；当前回撤与带位（四带：<5% 正常 / 5–10% 提级审查 / 10–15% 降仓×0.5 / >15% 熔断，阈值出处 R-336 §4.4）；在役版本卡（版本号+状态+各 sleeve 权重，390px 用堆叠条不用饼图）；三方对账徽标（绿=通过；红=对账失败，点开看差异明细——P0 只显示红绿与触发时间，明细 P1）；数据新鲜度。
- **交互**：点在役版本卡→版本页；点对账徽标→差异抽屉（P1）；曲线窗切换（P1）。
- **P0 验收锚点**：见 §4.1「一屏三问」。

### 区块 ② 引擎卡片（Tab1 第二屏）——P0

- **回答什么**：每个 sleeve 的引擎现在什么状态、还灵不灵（IC 有没有老化）。
- **关键信息**：每 sleeve 一卡：状态（shadow/paper/live/archived）；最新 IC + 3 个月趋势（联动 RET-3 老化预警：rolling IC 连续 3 月 <0 → 卡片标黄，R-336 §7.1）；ICIR_OOS；最近信号日；paper/shadow 已进行天数（自进入时间戳推导，对照 G-P1「≥1 个完整调仓周期」毕业条件）。
- **【2026-08-30 用户裁定修订·单引擎指标禁令（11:10 裁定，task-0587 落款）】**：绩效指标（回测/年化/回撤/夏普/IC/ICIR 等）**只在组合模型层展示，单引擎只允许逻辑/进展/状态**类信息；上条「最新 IC + 3 个月趋势 / ICIR_OOS」按本裁定裁撤不再接入（RET-3 老化预警如需呈现，改由组合层/事件侧承接）。该原则已编译化落地：`tools/quant-dashboard` 的 `policy.json` + `policy-lint.mjs`（task-0583；0830 实跑四项 PASS）。
- **交互**：点卡片→信号明细抽屉（P1）；点状态→版本页定位该 sleeve 所属版本（P1）。
- 依据：R-342 §4.3 区块②定义。更新频率 R-342 定为 300s 轮询；因引擎状态变化本质由事件驱动，**建议 R-342 修订时对齐为「事件驱动刷新 + 300s 兜底轮询」**。

### 区块 ③ 组合版本视图（Tab3）——P0

- **回答什么**：在役组合是谁、从哪个版本演化来、现在卡在状态机哪一段（场景 D 的 1/3/4 问）。
- **关键信息**：
  - P0：在役版本卡（高亮）+ 状态机胶囊流（首发渲染至 approved→paper→live，当前段高亮；**canary 移出首发主图**，入「未来扩展」：启用前置=先定义启用契约（期限、失效自动降级条件、G-L 阈值），契约就绪后回归主图；与 R-342 §4.3「canary 灰显」的差异见文末差异点清单）；版本列表（状态过滤）。
  - P1：版本详情抽屉全量：sleeve 清单（component_ref+code_hash+data_cut）、风险控制配置（回撤四带/波动目标 8%±2pp）、门禁成绩单（gate_report 逐条 pass/fail+阈值+实测值）、权重方案、求解留痕（solver_id+参数+是否触发 fallback 及原因）；版本树（parent_version 链）。
  - P2：两版本 diff 对比。
- **交互**：点版本→详情抽屉；详情内点门禁条目→事件页定位 gate.evaluated 事件（P1）。
- 语义锚点：「预算怎么分」进版本、「分出来的数」运行时算（R-336 §7.5.1）——版本详情展示的是**结构承诺**，权重是运行时求解结果，UI 上分开展示不得混排。
- **【2026-08-30 用户裁定落款·组合模型三层定义（10:40 裁定，task-0587）】**：组合模型=三层——①**组合构建层**：可插拔求解器（现役等波动率；风险预算/ERC 为演进向；MVO 仅对比留档，不作主口径）；②**组合层**：多腿（当前 vC-0 双腿 58.03/41.97，腿数不封顶）；③**风控层**：回撤四带/门禁成绩单/单腿风险上限。看板与汇报一律按三层主语呈现，**不得窄化为「双腿持仓」**；单腿仅为组合层的成员视图，绩效指标只在组合模型层展示（见区块② 08-30 禁令）。
- **【2026-08-30 增补·组合构建层口径演进目标（B8 方法偏离披露后对齐，task-0591；同日 21:23 用户更正红线：参数化两段式）】**：B6 批次（v1.4）早于 B8 偏离披露，上文「构建层等波动率+风控层」按以下两段式参数化理解，不再与看板现状混读——**目标态（不变）：滚动等波动率+风控层；当前供给（一行可切换）：静态 58/42 月度（提案轨迹）**。R-380 归因结论无论翻转与否只改「当前供给」行，不动「目标态」：
  - **目标态（不变）**：**滚动等波动率真解 + 风控层**。月频适配无法锚定 dryrun 解（末端失配 26.6pp，频率期限结构；hindsight 归因未排除前不称权威），随 **HP 日频 sleeve 数据接入翻转 authoritative_rolling_candidate** 后切换（policy 升版），徽标随归因与频率对齐完成后再议恢复；本段文字不随供给切换改写。
  - **当前供给（一行可切换，提案轨迹）**：构建层=**静态等波动率 58.03/41.97 月度再平衡、无风控层**（裸双腿层：无 equity DDC、无 gold sma200+vol_target、无货基层；不写死为权威）；展示曲线源 nav_curves.authoritative.csv:VC0_EQVOL_5842_M，锚=weight-solution-2026-08-28-dryrun.json solver_equal_vol_v1 定义日解；08-30 task-0590 已撤「权威口径」标签（authoritative_available=false），血缘由 policy-lint ⑥独立重算断言把守。
  - 切换前，本文其它处「等波动率/风控层」表述一律按「当前供给」口径理解：看板展示的是静态 58/42、无风控层（提案轨迹），不构成文档与看板打架。

### 区块 ④ 事件流水（Tab4）——P0

- **回答什么**：系统里发生过什么（场景 B 的 1、场景 C 的 1/4、场景 D 的 5）。
- **关键信息**：全事件倒序时间线。事件类型着色：晋升类蓝 / 风控类红 / 权重求解类绿 / 对账失败高亮红。每条显示：时间、类型、作用对象（哪个版本/sleeve）、执行者（pipeline/user/risk_layer 三种标签）、一句话摘要。事件类型全集 17 种（version.created/updated、component.registered、solver.selected、weight.solved、gate.evaluated、promotion.requested/approved/rejected/executed/downgraded、risk.action、retirement.triggered/executed、backtest.completed、reconciliation.failed、checkpoint.created，出处 R-342 §3.2）。
- **交互**：P0=按类型/执行者/对象过滤 + 分页加载（每页 50 条）；P1=点降级事件展开「触发规则+实测值 vs 阈值」、点对账失败展开差异明细、点晋升批准事件显示批准人时间；P2=事件书签/导出。
- 依据：R-342 §4.3 区块④（120s 轮询）。

### 区块 ⑤ 风控闸门（Tab2）——P0

- **回答什么**：所有风控维度现在什么状态、有没有超带、断路器什么情况（场景 A 的 2/6、场景 B 的 2/3、场景 C 的 2/5）。
- **关键信息**（每个闸门=当前值+带宽+带内/超带状态）：
  - 回撤分级闸门：当前回撤落位四带之一（仪表呈现）；
  - 波动率带：目标 8%，实测 ±2pp 带内外；
  - 两腿 20 日相关性：对照三档 0.75（筛查线）/0.85（防御降仓）/0.90（提级审查）（R-336 §7.5.4）；
  - 漂移四维 D1–D4：日盈亏偏差 ≤20bp/日、Sharpe 偏差 |Δ|≤0.3、执行率 ≥90%+信号对齐率 ≥95%、滑点 ≤假设×1.5；各维显示在带/超带与**连续超带期数**（连续 2 期=晋升冻结）；
  - 断路器状态：未触发/触发（含触发原因与时间）；触发即页面顶部红条；
  - 退役监视（P1）：RET-1..4 各规则当前距离触发线的余量。
- **交互**：P0=超带项自动置顶+红沿、点任意闸门→事件页定位对应 risk 事件；P1=**待处理事项视图**（聚合断路器触发中/对账失败未解/漂移连续超带/退役 review 中，一屏列全「现在需要老板知道/处理什么」）——四场景之「风险事件响应」的核心载体。数据全部来自既有接口的聚合展示，**建议 R-342 修订时对齐（§3.4 契约无此聚合入口，前端聚合即可，不需新增 API）**；P2=漂移历史曲线。
- 依据：R-342 §4.3 区块⑤（120s 轮询）+ R-336 §4.4/§6.1/§7.2/§7.5.4 阈值。

### 区块 ⑥ 迁移进度（Tab5）——P0

- **回答什么**：迁移（Phase A 审计地基 → B 影子双轨 → C 治理切换 → D 旧件退役）推进到哪、卡在哪（场景 B 的 5）。
- **关键信息**：四阶段卡片列，每阶段内动作 done/doing/todo + 证据链接（点开看报告/产物路径）；**A1/A2 审计项单独置顶**——任一 FAIL=红色「绝对阻塞」语义直接可视（R-336 §5.1：A1 前视偏差/A2 复权口径，FAIL 则一切迁移冻结）；Phase C「指针切换需用户批准」红线项标注。
- **交互**：P0=点动作跳证据文件（只读链接）、手动刷新+600s 轮询；P1=双看板并行对照验收项列入（R-342 §4.7 过渡策略的证据项）；P2=阶段归档收口。
- **下线条件（v1.2）**：Phase D 旧件退役完成后本区块收口归档，新看板不留永久迁移入口。
- 依据：R-342 §4.3 区块⑥ + R-336 §8 迁移总览表。

---

## 4. 每页面可执行验收标准

口径：以下每条都是「打开页面看到 X 即为对」，可独立逐条验收；数值一致性以 HP 侧产物与事件账本重放结果为基准。

### 4.1 总览页（P0）

1. **一屏三问**（390×844 首屏不滚动）：
   - 数据新不新鲜：健康条显示同步滞后秒数，≤阈值绿色；净值日期为最近交易日（非停摆旧值）；
   - 引擎都活着吗：下滑可见全部引擎卡，无一张「状态未知/停更」；
   - 有没有待处理风险：对账徽标绿 + 断路器未触发 + 无超带红点，三项任一异常则首屏有红色指示。
2. 净值曲线末值 = HP 产物最新净值，日变动% 与曲线一致。
3. 回撤带位与风控页显示一致（同一数值同一带色）。
4. 在役版本卡版本号与版本页高亮项一致；权重堆叠条各腿占比合计 100%。
5. 390px 下无横向滚动（全站硬约束，逐页截图）。

### 4.2 风控页（P0）

1. 六组闸门（回撤带/波动带/相关性/漂移四维/断路器/退役监视占位）全部呈现「当前值+带宽+状态」三元组，无一空白。
2. 任一超带项出现在列表顶部且有红沿；与事件页中对应 risk 事件时间吻合。
3. 断路器触发时：页面顶部红条含触发原因与时间；未触发时显示「未触发」而非空白。
4. 漂移各维「连续超带期数」正确显示（例：D1 连续 2 期超带 → 显示 2 并标红=晋升冻结语义）。
5. 阈值口径与 R-336 §4.4/§7.2/§7.5.4 逐条一致（20bp/0.3/90%/95%/×1.5/0.75/0.85/0.90/2%/15%）。

### 4.3 版本页（P0）

1. 在役版本高亮且状态=live；状态机胶囊流当前段与账本重放一致。
2. 版本页前端不渲染 canary 节点（canary 已移出首发主图，见 §3 区块③「未来扩展」与启用契约前置）。
3. 版本列表可按状态过滤；任意版本点开详情抽屉，P1 范围内字段（sleeve/code_hash/data_cut/门禁成绩单/求解留痕含 fallback 原因）非空且可读。
4. 门禁成绩单逐条含「阈值+实测值+pass/fail」，无「仅 pass 无数值」的黑箱条目。
5. 被降级过的版本，详情内可见对应降级事件（时间+规则）。

### 4.4 事件页（P0）

1. 时间线严格倒序；首条为最新事件。
2. 17 种事件类型有着色图例；晋升蓝/风控红/求解绿/对账失败高亮。
3. 过滤「晋升类」后仅见晋升族事件；过滤「执行者=user」仅见人工门事件（批准/拒绝）。
4. 分页翻 3 页无重复无跳漏（对照账本行数）。
5. 任一事件点开可见 payload 摘要（对象/执行者/关键数值）。

### 4.5 迁移页（P0）

1. 当前阶段与实际迁移状态一致（A/B/C/D 与各动作 done/doing/todo）。
2. A1/A2 置顶；构造 FAIL 数据时显示红色「绝对阻塞」。
3. 每个已完成动作有可点开的证据链接。
4. Phase C 红线项带「需用户批准」标注。

### 4.6 全局验收（P0）

1. **零写入口**：全站不存在任何提交按钮/表单/写操作入口；网络面板无 POST/PUT/DELETE（人工门引导至任务中心，见 §5）。
2. 首屏可交互 ≤2s（390px、常规网络）；Tab 切换 ≤1s。
3. 数据非最新时（同步滞后超阈值）健康条变黄+横幅，页面仍显示最后数据及其时间戳，不白屏。
4. 390×844 全页截图基线逐页留档（沿用 dashv5-*-390x844.png 惯例，R-342 §4.5）。

---

## 5. 旧看板能力对照表

盘点基准：agent-dashboard 实际模块（主导航 4 Tab：任务/用量/报告/量化；量化含 6 子页：数据/因子/模型/回测/模拟实盘·灰度/迭代历史；全局：告警/登录/30s 刷新），详见过程笔记。处置口径：**承接**=新看板覆盖；**降级**=旧看板 /legacy 保留承接、新看板不做或后做；**放弃**=随旧看板 Phase D 归档。总验收=**观测能力不断档**：对照表逐项检查，任何一项在新旧两侧都看不到=不合格。

| 旧看板功能 | 处置 | 说明 |
|---|---|---|
| 量化·数据：净值新鲜度/一致性徽标 | **承接（升级）** | 新鲜度→健康条；一致性徽标→三方对账徽标（R-336 §6.3 明确升级语义） |
| 量化·数据：registry/在役引擎状态 | **承接** | 引擎卡片（区块②）+在役版本卡 |
| 量化·数据：数据资产/健康明细/拥挤度/择时配置 | **降级** | 研究运维视角，legacy 保留至 Phase D；新看板 P2 再议数据健康子页 |
| 量化·因子：IC 序列/因子目录/模型清单 | **承接（部分）** | IC+ICIR+3 月趋势进引擎卡片（P0）；目录级浏览/模型明细降级 legacy |
| 量化·模型：模型对比页 | **降级** | 研究工具属性，legacy |
| 量化·回测：回测曲线/择时矩阵/对照批 | **放弃（新看板不做）** | 研究过程工具，留在 legacy 至 Phase D 归档；新看板只承接「成绩单」语义（版本详情 gate_report） |
| 量化·模拟实盘：净值/摘要/运行状态 | **承接** | 驾驶舱净值与状态、引擎卡片、事件流水（run 状态类事件）覆盖 |
| 量化·模拟实盘：成交明细/持仓明细 | **降级** | P2 再议；对账差异抽屉（P1）覆盖关键持仓核对场景 |
| 量化·模拟实盘：shadow 引擎净值对比 | **承接** | 引擎卡片 shadow 状态+天数；曲线对比 P1 |
| 量化·迭代历史：历史版本列表/版本选择器/在役标记 | **承接** | 版本视图（区块③）正是其治理化重造 |
| 量化·迭代历史：决策/账本/生命周期流水 | **承接** | 事件流水（区块④）= 账本/生命周期/决策的事件视角统一 |
| 量化·迭代历史：门禁记录 | **承接** | 版本详情门禁成绩单 + 事件页 gate.evaluated |
| 量化·迭代历史：pending 待办 | **承接（P1）** | 风控页「待处理事项」视图承接其「等着老板处理」语义 |
| 量化·迭代历史：ideas 想法池 / DSR | **降级/放弃** | ideas=研究流程工具降级 legacy；DSR 放弃 |
| 任务 Tab：任务中心（含审核/重试/文档） | **不迁移（留旧）** | 与量化治理无关；旧看板 /legacy 全功能保留，属新旧分工边界——**建议 R-342 修订时对齐（§4.7 只说旧看板降级保留，未写明三大非量化 Tab 的去向）** |
| 用量 Tab：配额/成本/系统指标/agent 监控 | **不迁移（留旧）** | 同上 |
| 报告 Tab：results 扫描浏览 | **不迁移（留旧）** | 同上；新看板迁移页证据链接承担少量「看报告」场景 |
| 全局：告警列表+确认按钮 | **承接展示/不承接写** | 告警**可见性**进新看板（风险角标+待处理事项）；确认动作是写操作，留任务中心 |
| 全局：登录鉴权 | **维持现状** | 单用户+站点认证照旧，不做多用户 |
| 全局：30s 自动刷新 | **承接（改分频）** | 60/120/300/600s 分频轮询（R-342 §4.4） |
| 全部写操作（任务 CRUD/配额刷新/agent 中止/量化动作队列/告警确认） | **明确不承接** | 新看板零写面铁律；对应操作走任务中心/HP 流程（R-342 关键决策 1） |
| 已废弃端点（quant/summary 等 5 组、microcap） | **放弃** | 旧看板已标 deprecated，随 Phase D 归档 |

---

## 6. 非功能需求

### 6.1 数据新鲜度

- 净值/驾驶舱数据：日频口径，当日 mark 完成后 ≤1 个同步周期内可见（同步周期以镜像 cron 为准，健康条如实显示滞后秒数，不假装新鲜）。
- 事件：发生后 ≤5 分钟内可见（120s 轮询+同步周期上限估算）。
- 风控闸门：与事件同频（120s）。
- 净值 mark 停摆 ≥2 交易日：健康条红色并显示「自动决策已冻结」语义（对应 R-336 §6.1 数据陈旧断路条件），不只是黄条。
- 投影校验不一致：显示对账失败状态条，**不静默用旧数据**（R-342 §4.6）。

### 6.2 告警可见性

四类必须不翻页可见（健康条/风险角标级）：断路器触发中、三方对账失败未解、漂移任一维连续超带、退役规则触发待裁决。首屏三问（§4.1）是验收形式。看板不负责推送——主动通知走既有渠道，看板是「打开必见」的兜底呈现层。

### 6.3 只读安全边界（零写面铁律）

- 前端零写入口：无任何提交/编辑/确认类控件；全部数据获取为只读查询。
- 人工门（G-L4 批准、Phase C 指针切换、断路器人工复位）在新看板只做**展示与引导**：「此操作需在任务中心执行」，不提供按钮（R-342 关键决策 1：写路径唯一性是事件账本的结构前提）。
- 数据源视角只读：镜像目录与投影不可回写（R-342 §1.1 三落点分工）。
- 敏感信息口径：单用户内部使用，持仓/权重等全量展示、不脱敏；未来若对外暴露，再启用脱敏+鉴权（字段级设计归架构文档，task-0534 承接）。

### 6.4 加载性能

- 390px 首屏可交互 ≤2s（常规 4G/WiFi）；Tab 切换 ≤1s；事件分页 50 条滚动不卡顿。
- 弱网降级：图表加载失败时降级为关键数字+最后数据时间戳，不白屏不转圈到底。
- 单 VPS 部署、无 CDN 前提下的目标值；月频数据量级（事件千行级/净值千点级）不构成压力。

---

## 7. 明确不做清单（Out of Scope）

1. **一切写操作**：任务管理、审批按钮、配额刷新、告警确认、agent 中止、量化动作队列——不做，走任务中心/HP 流程。
2. **多用户与权限体系**：单用户产品，沿用站点认证；角色/权限/多用户不做（旧看板的多用户规划文档不继承）。
3. **实时推送（SSE/WebSocket）**：轮询制（R-342 §4.4 冻结决策）；接口形态预留未来演进，但 P0 不实现。
4. **不指定 AI 模型**：本 PRD 与后续开发不指定/内置任何 AI 模型；如未来加 AI 摘要类功能，模型由用户自行在 GUI 配置。
5. **研究工具链**：因子目录浏览、回测曲线对比、择时矩阵、想法池——不进新看板（legacy/研究工具承载）。
6. **告警推送通道**：不做推送，见 §6.2。
7. **离线 PWA / 原生 App**：不做。
8. **数据编辑/回填入口**：不存在任何数据修正界面（历史修正走事件追加，R-336 append-only 原则）。

---

## 8. 里程碑：P0 功能集 ↔ R-342 W1-W9 排期映射

R-342 §5.1 排期为唯一主干，本节只映射产品功能落点：

| 批次 | 阶段 | 本 PRD 对应交付 | 验收锚点 |
|---|---|---|---|
| W1-W2 | A 期并行（读层+只读 API） | 无前端交付（前置依赖） | API 契约联调用例就绪 |
| W3 | A 期并行（前端骨架） | **事件页 P0 全量 + 迁移页 P0 全量** + 健康条 | §4.4 / §4.5 逐条 |
| W4 | B 期（卡 vC-0 快照） | **总览页 P0（一屏三问）+ 引擎卡片 P0 + 版本页 P0** | §4.1 / §4.3 逐条 |
| W5 | B 期（卡影子双轨+漂移启用） | **风控页 P0 全量 + 对账徽标** | §4.2 逐条 + §4.1 对账项 |
| W6 | B 期末（双看板验收） | §5 对照表逐项核验 + 390×844 全页截图基线 | 对照表零断档 |
| W7-W8 | C 期（切换，需用户批准） | 无新功能；主域切换、旧看板转 /legacy | 切换后一周内新旧并行可访问 |
| W9 | D 期（收尾） | 迁移页归档态；P1 清单收尾窗口 | 归档可回溯 |

P1 窗口：W5-W6 之间插入版本详情抽屉全量、待处理事项视图、对账差异明细；最迟 Phase C 稳定后补齐。P2 一律进 backlog，Phase D 后随 Qlib 解耦等演进项另议。

---

## 9. 迭代规则（v1.7，2026-08-31 固化，task-0599/task-0601）

> 来源：用户 2026-08-31 12:55 五方向评审固化部分（方向3/4/5 打包）。条文为本 PRD 的编译化落点，适用于本 PRD 覆盖的量化研究与看板迭代全域；本节所有限值为建议值，**待用户确认后生效**，确认前仅作盘点参照不作为硬门禁处罚依据。v1.7 增补来源：用户 2026-08-31 17:23 两条裁定（§9.4 基线标注规则、§9.5 任务触发条件表达式化，task-0601）。

### 9.1 产能比例约束

- 每滚动周期（建议按周盘点）新开任务中，**新 alpha 研究类任务占比 ≥50%，治理/审计/口径类任务合计 ≤50%**（50% 为建议值，待用户确认）。
- 周期复盘时核对当期任务结构（以任务中心 API 台账为准）；超限则**暂停新治理类立项**，直至结构回到带内。
- 目的：防治理/审计/口径整顿挤占研究产能，保证链路主循环（E1 画像 → E2 预注册 → 执行 → 判门）持续有新输入。

### 9.2 判负赛道关停窗口

- 赛道在 E2 判负后，**N 个研究周期内不再预注册同族变体**（N 建议值 2–3 个研究周期，依据 R-384 影子观察 ≥1 个完整月度对账周期的链路节奏、判负死因复核需跨形态观察，**建议值待用户确认**）。
- 同族定义：同执行形态 + 同信号机制的谱系变体。判负先例参照 **R-374** 微盘P2闸 E1 画像——MA60 档 coverage 86.8% ≈ RV 门 85.3% 按纪律判「疑似同构」不予进 E2、月频执行形态判死（maxDD 无改善）；窗口期内若要重启，必须提出与判负死因正交的新形态证据，按新赛道立项。

### 9.3 可重算产物落盘入门禁

- 判门/正式报告类任务的**交付定义必须包含「可重算产物」：计算脚本 + 关键中间产物，落盘至可寻址路径**（results/ 下带 R 号的稳定路径），缺项即验收不通过。
- 作为门禁检查项执行，与 dev 侧 **policy-lint 检查⑥重算血缘断言**衔接（tools/quant-dashboard/scripts/policy-lint.mjs，task-0591）：看板展示层由检查⑥独立重算把守「指标对但基底错」，研究交付层由本条把守「结论可溯源可复现」，两层同一原则。
- 依据：R-377b（原 R-377，2026-08-31 编号治理改名）基底错误判定——F1_quarterly 四指标全部偏乐观险些误导决策，唯一可靠防线是独立可重算；R-384 governance 语义分离原则（主口径翻转≠设计权重翻转，两个语义必须显式分离且各自可重算）。

### 9.4 基线标注规则

- 任何报告/看板/汇报材料**引用 MDD、年化、波动、夏普等基线绩效数字时，必须同行标注三要素**：①组合实例（哪条曲线/哪个权重方案）②统计窗口（起止/频率/n）③口径（净/毛、再平衡频率）；**缺任一项禁止引用**，评审时主 agent 按此 lint。
- 反例：R-386 §5.2 引用 MDD −16.64%→−9.66% 未标注实例与窗口（实为 R-372 两腿/三腿全窗回测），引发基线混淆——评审已打回。
- 正例：R-372 L108「两腿基线全窗参考值（2013-08..2026-08，n=157）：年化 16.02%/MDD −16.64%/Calmar 0.962（2013-2015 a13 高收益段贡献，仅披露）」——实例、窗口、口径、归因披露四项俱全，作为标注范式。
- **实盘/实窗样本 <3 个月的绩效数字必须标注 `insufficient_obs` + 实际样本长度**（如「n=11 收益日」），**禁止与设计口径/长窗回测数字并列同表比较**；确需并列时必须分行并显著标记「无统计意义」。范例：R-388 四基线表 ④a 行（runtime 实仓 08-14 起跑 2 周，单独成行并声明窗口太短）。依据：用户 2026-08-31 18:52 裁定（task-0605/R-389）。

### 9.5 任务触发条件表达式化

- 任务书触发条件**必须写机器可判定表达式（基于任务中心字段）**，例：`after: task-0597.status==done AND user_decision.gold_abc==yes`；**自然语言触发禁止作为自动派发依据**。
- 主 agent 派发前**逐项核验表达式**：字段可查、条件可判定，全部满足才允许派发；表达式含糊或缺字段时视为条件未满足，不得派发。
- 依据：用户 2026-08-31 17:23 裁定（task-0601）；根因=「自然语言规格被按字面执行」同模式第 5 次心跳误启动。

---

## 附：与 R-342 的差异点清单（全部「建议 R-342 修订时对齐」，本 PRD 不改 R-342）

1. **Tab 归并**：R-342 §4.2 信息架构图为导航→六区块平行，未给 ≤5 Tab 的落位方案；本 PRD 定为「引擎卡片并入总览页第二屏」（§2.1）。
2. **引擎卡片刷新**：R-342 §4.3 定 300s 纯轮询；本 PRD 建议事件驱动+300s 兜底（§3 区块②）。
3. **待处理事项聚合视图**：R-342 §3.4 契约无统一聚合入口；本 PRD 在风控页做前端聚合（P1），不需新增 API（§3 区块⑤）。
4. **新旧分工边界**：R-342 §4.7 过渡策略未写明旧看板任务/用量/报告三大非量化 Tab 的去向；本 PRD 定为全功能保留 /legacy 至 Phase D（§5）。
5. **canary 节点首发不渲染**：R-342 §4.3 状态机含 canary 段（灰显「未启用」）；本 PRD 裁决 P0 主图止于 approved→paper→live，canary 移入「未来扩展」，启用前置=先定义启用契约（§3 区块③）。

## 附：验收信息

- 本报告 ≥10KB、8 节齐全；P0 功能均有「看到什么算对」验收标准（§4）。
- 旧看板对照表覆盖 4 主 Tab + 6 量化子页 + 全局功能，与 agent-dashboard 实际模块清单一一对应（盘点过程见 work/task-0532-dashboard-prd-notes.md）。
- 零代码交付；除本报告、过程笔记、README 更新日志一行外零文件改动。

---

## 附：模块对照表（v1.3 收编 R-359 §2）

> 收编自 R-359《新看板未生效模块对照审计》§2 全量对照表，43 模块=全局 6+总览 7+风控 9+版本 7+事件 8+迁移 6（另有 BFF 契约缺口 2 项 /risk/drift、/portfolios/:id/timeline，已由 R-342 v2.0 契约收编裁为后续版本项，不重复列）。**快照 2026-08-29；2026-08-30 task-0587 按看板实况实测刷新（快照日期更新为 2026-08-30），增补 2 行新模块共 45 行，未复测行沿用 08-29 快照原状**（此前 R-360/R-361/R-362/R-363/R-366 等批次已陆续消化其中数据源/管道/前端缺项；08-30 批次 0572/0573/0575/0578/0580/0582/0583/0586 见修订记录 v1.4）。现状口径：✅生效 🟡部分 ❌未生效 🔜排期；处置=补齐动作+优先级。

| 模块 | Tab | 现状 | 根因 | 处置 |
|---|---|---|---|---|
| 健康条·数据新鲜度（滞后秒数+账本尾+刷新） | 全局 | ✅ 五 Tab HealthStrip | — | 无（P0 已达） |
| 健康条·投影校验状态 | 全局 | ❌ /health 已返 projection_sha256_ok，条上不显 | 前端缺 | HealthStrip 加校验状态点（中） |
| 风险角标（风控 Tab+健康条红点） | 全局 | ❌ TabBar 无角标，仅总览页 pending count | 前端缺 | 消费 health.pending_risks.count（中） |
| 390px 无横向滚动 | 全局 | ✅ 五 Tab 实测 scrollWidth=390 | — | 无（P0 已达） |
| 零写入口 | 全局 | ✅ 全站无表单/写控件 | — | 无（P0 已达） |
| 分频轮询 60/120/300/600s | 全局 | 🟡 overview/engines ✓；events 用 60s（定 120s） | 排期微偏 | 顺手对齐 120s（低） |
| 净值曲线+30/90/1Y 切换+日变动% | 总览 | ❌ 0830 复测 nav=null 曲线数值仍缺（如实不伪造）；overview 断供已修（task-0586：active_pv/sleeves/对账在场） | B8 管道 0830 晚已落地（task-0585）：供给切 nav_curves.authoritative.csv:VC0_EQVOL_5842_M（静态 58/42 月度再平衡·无风控层；0830 撤权威标签 task-0590，滚动真解待 HP 日频）；总览接线待复测（版本页已实测 156 点） | 总览净值接线复测后关闭本行；滚动真解翻转走 authoritative_rolling_candidate（P1） |
| 当前回撤+四带带位 | 总览 | ❌ 0830 复测 drawdown_pct 仍 null 无带位 | 数据源缺 | task-0560 一并接；宪章 vs 目标四带双口径 UI 标注（P0 高） |
| 在役版本卡+权重堆叠条（合计 100%） | 总览 | ✅ 0830 实测 vC-0 paper 58.03/41.97+求解日期；0580 契约 v2.1 sleeve 字段+腿徽标核验通过 | — | 无（P0 已达） |
| 三方对账徽标（红/绿+时间） | 总览 | ✅ 绿徽标；差异明细抽屉 🔜 | 排期（P1） | 抽屉复用风控页三视角 diff（P1） |
| 数据新鲜度卡 | 总览 | ✅ 滞后/数据截止/账本尾 | — | 无（P0 已达） |
| 引擎卡（状态/信号日/天数） | 总览 | ✅ 按 08-30 11:10 裁定收敛：单引擎只留逻辑/进展/状态（状态/days ✓）；IC/ICIR 等绩效指标裁定不上单引擎（policy 编译化 task-0583，policy-lint ①实测 PASS） | 用户裁定（原「数据源缺待接入」不再适用） | 无（已裁定收敛；信号明细抽屉仍 P1） |
| 引擎卡→信号明细抽屉 | 总览 | 🔜 未做 | 排期 | P1 窗口（Phase C 后收尾） |
| 回撤分级闸门（四带仪表） | 风控 | ❌ 无渲染；risk/gates 无 portfolio_dd_gate | 数据源+路由+前端三连缺 | HP 产回撤监控 JSON→BFF 补字段→前端仪表；口径双轨标注（P0 高验收硬缺口） |
| 波动率带（8%±2pp） | 风控 | ❌ vol_target=null 无 realized vol 产物 | 同上 | 同上管道，realized vol 20 日（P0 高） |
| 两腿 20 日相关性三档旗（0.75/0.85/0.90） | 风控 | ❌ 无相关性监控产物 | 同上 | HP 日频收益 20 日 rolling corr→投影→BFF→三档旗（P0 高） |
| 漂移四维 D1-D4+连超期数 | 风控 | ✅ D3/D4 带内、D1/D2 如实标注暂不判带 | — | 无（P0 已达） |
| 断路器状态 | 风控 | ✅ 「未触发」显式呈现 | — | 无（P0 已达） |
| 退役监视 RET-1..4 余量 | 风控 | 🔜 无渲染无数据 | 排期（P1） | P1 窗口 |
| 超带项置顶+红沿 | 风控 | ✅ overBandCount 徽标+置顶 | — | 无（P0 已达） |
| 点闸门→事件页定位 risk 事件 | 风控 | ❌ 无此交互 | 前端缺 | hash 路由带 filter 参数（中） |
| 待处理事项聚合视图 | 风控 | 🟡 pending_risks 聚合 ✓；四来源展开未做 | 排期 | P1 窗口 |
| 版本列表+状态过滤+在役高亮 | 版本 | ✅ 全部/paper 过滤+vC-0 卡 | — | 无（P0 已达） |
| 状态机胶囊流（approved→paper→live） | 版本 | ✅ canary 未渲染符合裁决 | — | 无（P0 已达） |
| 详情抽屉 P0 字段 | 版本 | ✅ 全非空可读，fmtID 截断 hash | — | 无（P0 已达） |
| 门禁成绩单（逐条阈值+实测+pass/fail） | 版本 | ✅ 0578 已补门禁成绩单区块渲染（0830 实测 Version.jsx 命中 gate_report）；vC-0 gate_report=null 如实显「未评级」 | —（渲染已补） | vC-1 过门禁后回归验证数据渲染（P0 达口径诚实） |
| 详情·风险控制配置 | 版本 | ✅ 0578 已补 risk_control 区块渲染（0830 实测 Version.jsx 命中；同批含组合构建区块） | —（前端已补） | 无（已修，task-0578） |
| 版本树（parent_version 链） | 版本 | ❌ 当前仅单版本暂无数据 | 排期+数据 | 多版本后 P1 垂直链表降级实装（P1） |
| 两版本 diff 对比 | 版本 | 🔜 backlog | 排期 | P2 |
| 倒序时间线+分页 cursor | 事件 | ✅ 首条=最新，翻页正常 | — | 无（P0 已达） |
| 17 种类型着色图例 | 事件 | ✅ EVENT_TYPES 17 全集五族着色 | — | 无（P0 已达） |
| 按类型/执行者/对象过滤 | 事件 | 🟡 类型过滤 ✓；actor/target 无 UI | 前端缺 | 过滤行加两下拉，数据已在 items（中） |
| payload 摘要 | 事件 | 🟡 常规事件 ✓；recon 类显原始 JSON 串 | 前端缺（体验） | payloadSummary 补 checks/镜像 key 白名单（低） |
| 待决/待决超期标注 | 事件 | ✅ computePendingFlags 35 天周期口径 | — | 无（P0 已达） |
| 点降级事件展开「规则+实测 vs 阈值」 | 事件 | 🔜 未做 | 排期 | P1 |
| 点对账失败展开差异明细 | 事件 | 🔜 未做 | 排期 | P1（复用风控页 recon 三视角） |
| 晋升批准显示批准人+时间 | 事件 | 🔜 账本暂无 promotion.approved 可验 | 排期 | P1 |
| 四阶段卡片列（Phase A-D） | 迁移 | 🟡 仅 A/B 两阶段，C/D 缺 | 管道缺（migration.json 手写投影） | 投影按 R-336 §8 补全 C/D 行（P0 高） |
| 动作 done/doing/todo+证据 | 迁移 | ✅ 机制生效（A1/A2 done 带路径） | — | 无（P0 已达） |
| A1/A2 置顶+绝对阻塞红条 | 迁移 | ✅ 置顶+「无阻塞」判定双保险 | — | 无（P0 已达） |
| Phase C「需用户批准」红线标注 | 迁移 | ✅ 逻辑在，暂无 C 行可显 | 依赖 M1 | 随 M1 生效（P0 达） |
| 投影与实际进度一致 | 迁移 | ❌ B2/B3 显未开始（R-349 已完成留档），Phase B 动作 1-5 未入表 | 管道缺（投影未随任务更新） | 一次投影修订：B2/B3→done+证据，补动作 1-5 行（P0 高数据真实性） |
| 双看板并行对照验收项列入 | 迁移 | 🔜 未做 | 排期 | P1（Phase C 切换窗口前） |
| 候选库视图（Candidates 子页面：vC-0+基准指标、ablation 降级展示） | 子页面（不占 Tab） | ✅ 08-30 新增（task-0572/0573/0582）；0830 实测 policy-lint ③ ablation 禁令 PASS | —（原 43 清单外新增模块） | 迭代按 policy 口径（policy.json，task-0583） |
| 双腿持仓卡（sleeve 腿徽标+逐腿持仓明细） | 版本 | ✅ 08-30 新增（task-0575）；0830 实测 /holdings items 带 sleeve 字段（契约 v2.1，task-0580） | —（原 43 清单外新增模块） | 无（已交付） |

## 修订记录

- v1.0（2026-08-28，task-0532）：初版，二轮外部评审前基线。
- v1.1（2026-08-28，task-0533）：二轮评审产品侧裁决落地——canary 移出首发主图入「未来扩展」（启用前置=启用契约：期限/失效自动降级/G-L 阈值，§3/§4.3/差异清单同步）；§6.3 补敏感信息口径（单用户不脱敏，对外才脱敏+鉴权）；§2.2 补角标计数口径（聚合字段由后端提供，字段设计归 task-0534）。
- v1.2（2026-08-28，task-0536）：21:27 三方交叉评审 P2 残值——区块⑥迁移进度（Tab5）补下线条件：Phase D 旧件退役完成后本区块收口归档，新看板不留永久迁移入口。单点小修零结构变更。
- v1.3（2026-08-30，task-0570，用户 01:18 批准「按此修订」）：附录收编 R-359 §2 全量 43 模块对照表，精简为 模块｜Tab｜现状｜根因｜处置 五列（快照 2026-08-29，状态以看板实况为准；BFF 契约缺口 2 项已由 R-342 v2.0 收编处置，不重复列）。纯增量收编零结构变更。
- v1.4（2026-08-30，task-0587，B6 文档批次）：43 模块对照表按看板实况逐行实测刷新，**快照日期更新为 2026-08-30**（核验：policy-lint 四项 PASS、overview active_pv=vC-0 双腿 58.03/41.97、/holdings items 带 sleeve 字段、Version.jsx risk_control/gate_report/组合构建 12 处命中）；改动 6 行（净值曲线/当前回撤/在役版本卡/引擎卡/门禁成绩单/风险控制配置）+ 增补 2 行新模块（候选库视图、双腿持仓卡），共 45 行，未复测行沿用 08-29 快照原状。落款用户两条裁定：①**10:40 组合模型三层定义**=组合构建层（可插拔求解器：等波动率→风险预算/ERC，MVO 仅对比留档）+组合层（多腿）+风控层（回撤四带/门禁成绩单/单腿风险上限），看板与汇报不得窄化为「双腿持仓」（定义补入 §3 区块③）；②**11:10 单引擎指标禁令**=绩效指标（回测/年化/回撤/夏普等）只在组合模型层展示，单引擎只允许逻辑/进展/状态，已编译化为 policy.json+policy-lint.mjs（tools/quant-dashboard，task-0583），区块②旧「引擎卡含 IC/ICIR」表述按裁定修正注明。已知未修如实保留：nav 供给近似口径 F1_quarterly（R-377 判定，待 B8 权威口径管道，用户待拍板）、腿级 NAV 已裁定不做、cross_check_match 结构缺陷（硬编码 null）待后续。今日批次：0572/0573/0575/0578/0580/0582/0583/0586。纯增量零重写。
- v1.5（2026-08-30，task-0591，dev 线③）：B8 方法偏离（滚动→静态）披露后 PRD 对齐——区块③增补「组合构建层口径演进目标」：v1 当前=静态等波动率 58.03/41.97 月度再平衡·无风控层（R-379 已上线，曲线源 nav_curves.authoritative.csv:VC0_EQVOL_5842_M，task-0590 撤权威标签、policy-lint ⑥重算血缘把守）/v2 目标=滚动等波动率真解+风控层（月频适配末端失配 26.6pp+hindsight 归因未排除；随 HP 日频 sleeve 数据接入翻转 authoritative_rolling_candidate）；当前供给按 08-30 21:23 用户更正红线参数化两段式——目标态（不变）/当前供给（一行可切换·提案轨迹），禁止写死任何口径为「权威」；模块对照表「净值曲线」行根因/处置增量修订（B8 管道已落地，总览接线待复测）；lint 升级 policy-lint ⑥独立重算断言 + 滚动对照四指标同构（tools/quant-dashboard）。纯增量零重写，v1.4 快照与两条用户落款裁定原文不动。
- v1.6（2026-08-31，task-0599）：用户 08-31 12:55 五方向评审固化部分（方向3/4/5 打包）落 PRD——新增 §9 迭代规则三条：①产能比例约束（滚动周期建议按周盘点，新 alpha 研究任务 ≥50%、治理/审计/口径类 ≤50%，复盘核对超限则暂停新治理类立项；50% 建议值待用户确认）；②判负赛道关停窗口（E2 判负后 N=2–3 个研究周期内不再预注册同族变体，同族定义引 R-374 月频/MA60 与 RV 门同构判负先例；N 建议值待用户确认）；③可重算产物落盘入门禁（判门/正式报告类交付定义必须含计算脚本+关键中间产物落盘可寻址路径，作门禁检查项，与 policy-lint 检查⑥重算血缘断言衔接，tools/quant-dashboard/scripts/policy-lint.mjs）。纯增量新章节，其余章节零改动，变更记录见 R-385。
- v1.7（2026-08-31，task-0601，用户 17:23 裁定）：§9 增补两条迭代规则——**§9.4 基线标注规则**（引用 MDD/年化/波动/夏普等基线数字必须同行标注组合实例+统计窗口+口径三要素，缺一禁止引用，主 agent 评审按此 lint；反例 R-386 §5.2 基线混淆、正例 R-372 L108 标注范式）；**§9.5 任务触发条件表达式化**（触发条件必须写基于任务中心字段的机器可判定表达式如 `after: task-XXXX.status==done`，自然语言触发禁止作为自动派发依据，主 agent 派发前逐项核验；根因=自然语言规格被按字面执行同模式第 5 次心跳误启动）。纯增量两条+标题版本号+来源行追加，其余条文零改动。
