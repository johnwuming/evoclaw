# R-322 · 量化迭代全流程节点化与通用模块合并抽象方案

- **任务号**：task-0501（阶段B·正式报告）
- **日期**：2026-08-27
- **输入证据清单**（唯一取材来源，均出自阶段A笔记）：
  - 阶段A证据笔记 `shared/results/work/task-0501-notes.md`（27,433B，下称「笔记」，引用形式如「笔记§1」「笔记§3 表」）
  - 笔记索引的底层证据：HP crontab 实查（2026-08-27，`noname@10.12.192.174:2222`，笔记§1 表）；`evolution_pipeline.py`（HP 80KB，笔记§1）、`risk_patrol.py`（21KB）、`model_manager.py`（VPS 34KB）、`data_validator.py` 解剖；`engines.json` schema_v1（14KB，笔记§2）；`versions-manifest.json`（121KB，118 版本，笔记§0）；`server.js`（718,178B，60 个 /api/quant 路由，行号抽查，笔记§server 实查）；方案文档 R-203/R-205/R-206/R-220/R-223/R-259/R-267/R-300~R-307/R-315/R-316/R-317/R-318/R-319/R-320/R-321（笔记§证据源2、§3、§4、Top10）

---

## 1. 背景与目标

用户 09:25 提出三问（原文概述）：

1. **统一流程图**：把单模型一轮迭代与多引擎中央风控合并成一张完整流程——从数据校验→想法消化→回测→门禁评分→shadow 观察→activate→paper 实盘→risk_patrol 巡检→退出/回退的全生命周期；每节点标注执行者、输入产物、输出产物、失败路径。
2. **节点=通用模块的重构评估**：以「每个流程节点对应一个通用模块」为原则，逐节点评估重复建设（对照 Top10 + R-320 D1-D11 + R-321 矩阵），给出合并抽象方案：职责边界、唯一落点、被合并对象清单；并如实处理阶段A存疑项「influence 二层激活判据」。
3. **逐节点可视化评估**：每节点回答"需要可视化吗 + 现有可视化在哪个 Tab 哪个模块 + 是否与别节点重复 + 处置建议"，输出总表。

## 2. 方法

- **阶段分工**：阶段A（已由 task-0501 阶段A子任务完成）= 证据收集落盘，产物为笔记（HP 代码实查 + 方案文档 + 前端现状三层取证）；阶段B（本任务）= 只从笔记取材撰写正式报告，不回查全量代码、不从记忆复述。
- **证据源三层优先级**：HP 代码实查（crontab/脚本解剖/数据文件）> 方案文档（R-203/R-223/R-320/R-321 等）> 前端现状（server.js 行号、R-321 活 UI 矩阵）。凡结论标注证据出处（笔记节号或文件行号）。
- **存疑项处理**：influence 无实据——如实标注，不臆造定义，列入「待用户确认」。

---

## 3. 问一：统一流程图（结论先行）

**结论**：量化系统存在两套可合并呈现的生命周期——**微盘版本线**（registry 版本对象驱动，全自动：candidate→backtest→EQUIV→evaluate/SCORED→rank==1 自动 activate→shadow_watch 在影观察出影）与**引擎线**（engines.json 引擎对象驱动，半自动：E1 画像→E2 预注册→判门→影子登记→用户批准激活→active_paper）。两者共享同一套外环数据/校验/回测/审计基础设施与内环 paper 运行时，中央风控（risk_patrol/collect_crowding）以"插入点"形式横切在内环之上。合并后全生命周期共 **18 个节点（N0-N18）**，其中 N1-N11 为外环研究进化链、N12-N14 为内环运行与巡检、N15-N17 为横切支撑、N18 为退出回退终点。

### 3.1 文字版流程图

```
【外环·研究进化链】（agent 主导 + HP cron 兜底；微盘版本线 + 引擎线两径汇流）

 N1 数据采集/刷新 ──► N2 数据校验 ──► N3 快照登记 ──► N4 想法消化 ──► N5 候选设计 fork
 (HP cron 周/日/月)   (fail-fast,6项)  (cycle S1)     (cycle S2)      (registry 副本+params_hash)
        ▲                                                              │
        │                                                              ▼
 N18 退出/回退 ◄── N13 risk_patrol ──► N12 paper 实盘(内环) ◄── N11 activate ◄── N10 影子观察
 (降仓/暂停升级/     (工作日16:45,      (daily→rebalance→     (registry+main.json   (N10a 版本级 shadow_watch
  rollback)          charter三规则)     validate)               +字节快照)            N10b 引擎级 cross_engine)
        ▲
        │
 N6 回测(N4无候选可跳过) ──► N7 等价校验 EQUIV ──► N8 门禁评估 ──► N9 评分 SCORED ──┘
 (full+locked双窗)        (patch全关 diffs={})   (g1-g6 / G0-G6两代)   (rank==1)

【内环·模拟盘】（无学习；N12 内联循环 daily→rebalance(月首)→validate）
【横切支撑】N15 记录审计（decision/experiment/switch 三账本）◄─┬─ 全节点
           N16 同步回传（HP→VPS 结果/指标/注册表投影）         │
           N17 通知呈现（notify_hub 双端→微信/dashboard）       ┘
```

### 3.2 节点明细表（执行者 / 输入 / 输出 / 失败路径）

| 节点 | 名称 | 执行者（HP 脚本/cron/API） | 输入产物 | 输出产物 | 失败路径 |
|---|---|---|---|---|---|
| N0 | 触发与编排 | `evolution_pipeline.py cycle`（周六 09:00 cron，笔记§1 表）；用户对话/任务中心 spawn 研究（引擎线 E1 画像→E2 预注册，笔记§2）；`heartbeat_selfheal.sh` */5 自愈兜底 | 时间表 + 任务中心 | 本轮执行计划 | 无（cron 兜底下次重试） |
| N1 | 数据采集/刷新 | `refresh_data.py`（周日 20:00）；`cron_qfq_daily.py`（工作日 18:00）；`collect_qfq_baostock --mode init`+`rebuild_merged.py`（周日）；`w6_collect_delisted.py`（月采）；`fetch_valuation_data.py`（周日 06:30）——均 HP cron（笔记§1 表） | 上游行情/财务/退市/估值源 | 合并前复权面板、估值数据、退市表 | 采集失败→`heartbeat_selfheal` */5 自愈 |
| N2 | 数据校验 | `data_validator.run_all`（cycle Step0 内联 import，fail-fast）；`paper_engine.py --action validate`（周日 20:00，委托同一 validator）——同一实现（笔记§1） | 合并面板 + 校验配置 | 6 项校验结论（kline_freshness/panel_coverage/holdings_kline/price_reasonable/dividend_continuity/selection_count） | 任一失败→fail-fast 中止本轮（cycle Step0），paper validate 阻止开仓日运行 |
| N3 | 快照登记 | `evolution_pipeline.py cycle` Step1（笔记§1） | 校验通过信号 | registry 版本快照 | 失败→本轮中止 |
| N4 | 想法消化 | `evolution_pipeline.py cycle` Step2 消费 `ideas/pool.jsonl`（笔记§1 时序第2步）；另一入口：用户对话/任务中心 spawn（引擎线 idea 类型亦写池，但 VPS action 队列入口为死树 R-320-C，笔记§3 表） | ideas/pool.jsonl | 本轮候选想法集 | 无候选→cycle Step4/5 跳过（笔记§1） |
| N5 | 候选设计 fork | `evolution_pipeline.py fork --from --as --set`（笔记§1） | 候选设计（parent 只改一维度、g5 预埋、ext runner 源码字符串注入零改动，R-223 ①） | registry 副本 + params_hash + decision_log 预写 trigger | 参数冲突→fork 拒绝 |
| N6 | 回测执行 | `evolution_pipeline.py backtest`（两腿端到端选股×择时 + 同口径基线）；**并行旧轨** `p3_3_evolution_standalone.py --rounds 5`（月1/15 cron，笔记§1 表，重复建设 Top1/D6） | 候选 params + 全量池（含退市/成本 v2/一字板不可成交/AUDIT_LOCK clamp，R-223 ②） | full+locked 双窗回测产物（artifacts 移交 leg 目录） | 回测异常→artifacts 留痕，报告可查（笔记§1 第5步） |
| N7 | 等价校验 EQUIV | `evolution_pipeline`（patch 全关复跑 parent，锚点校验同机制，R-223 ③） | 回测产物 + parent 基线 | diffs={} 逐位一致才挂新分支 | 不一致→拒绝挂载，需排查 diff |
| N8 | 门禁评估 | 版本线：`evolution_pipeline.py evaluate` g1-g6（ICIR_IS≥min/ICIR_OOS p>0.05/|ρ|≤0.7 护栏豁免/DSR≥0.95/g5 必填/g6 已禁用只入评分，笔记§1）；引擎线：E2 预注册 G0-G6 判据（gold: corr/G1a/G1b/G2/G4 容量/G5/G6），**按预注册文档手判**（笔记§3 表） | 回测指标 + GATE_CONFIG(oos_split_ym=2021-01) | 门禁 PASS/FAIL（版本线一票否决→转入评分） | FAIL→候选停留 candidate 或 rejected |
| N9 | 评分合成 SCORED | `evolution_pipeline`（R220-#7：五门禁一票否决→综合评分；权重 p .175/dsr .175/oos_calmar .125/oos_sharpe .125/is_calmar .075/is_sharpe .075/dd .10/corr .10/logic .05） | 门禁指标 + `scoring_v12_frozen.json` | SCORED+score，`score_rank_pool rank==1` 才具上岗资格（笔记§1） | 无（仅排序） |
| N10 | 影子观察 | N10a 版本级：`pipeline._shadow_update`（stat_warn→进影清零；clean_evals+1 满 SHADOW_CONFIG.watch_periods 出影，笔记§1）；特例 a12：`a12_monthly_evaluate.sh`（月2日17:10，flock+重试，**L3 守卫：clean_evals≥required-1 停自动评估转人工**，笔记§1）；N10b 引擎级：`engines_shadow_evaluate_gold.py --mode monthly`（月3日09:40，读 B 影子NAV + A 在役 NAV 对比→engines.json 的 shadow.evals，A/A2 字节级零改动，笔记§1） | 候选/引擎 NAV + clean_evals 计数 | 出影判定（clean_evals N/N）或终止 | 连续 stat_warn→进影清零重计（笔记§1） |
| N11 | 激活切换 | 版本线：`evolution_pipeline activate`/`_do_activate`（门禁 PASS 自动，R220#8 移除人工确认；写 registry entry + `build_main_from_registry`→main.json + 冻结 {version}.main.json.snapshot 字节快照 + switch_log.jsonl，笔记§1）；引擎线：engines.json 人工条目变更（gold shadow→active 由用户微信主会话批准，task-0485/R-307，笔记§0/§2） | 通过门禁的版本/引擎 + 注册表 | active 条目 + main.json + 字节快照 | 回退：`rollback --to --reason` 字节级还原；`override` TTL 临时覆盖（笔记§1） |
| N12 | paper 模拟实盘（内环） | `paper_engine.py`：daily（工作日16:30 净值）/rebalance（--check-month-start 工作日15:00，`rebalance_gate.py` 月首判定 task-0347）/validate（周日20:00）；黄金平行链 `paper_engine_gold.py` daily（工作日07:40）/verify（周日03:00），月3日 `engines_shadow_nav_gold.py append` 影子NAV（笔记§1 表） | paper-state.json + 择时系数（q3z×EW-MA200 内化）+ 在役版本参数 | summary/nav/trades/portfolio/state/validation 六件套→rsync push；P0 一致性徽标（paper vs active rules_align） | 数据校验失败→周日 validate 拦截；月首判定错误→rebalance_gate 修正（task-0347 修月首判定） |
| N13 | 中央风控巡检 | `risk_patrol.py`（工作日 16:45，笔记§1 表）：读 config/risk-charter.json 三规则（drawdown_circuit_breaker HWM 回撤25%→降仓至50%；underperform_discipline rolling_6m_excess<-10%→观察+暂停模型升级；live_vs_backtest live sharpe/backtest<0.5 持6月→失效 review） | track_nav + paper_nav + hs300 + microcap_eqw 四净值源（笔记§2） | results/risk-status.json（每规则 current/threshold/color/margin，charter_version=1.0） | 告警→notify_hub 红黄告警 + 前端 P10 色阶；降仓/暂停升级/失效 review 动作落入 N18 |
| N14 | 拥挤度监控 | `collect_crowding.py`（周日 07:00）+ `snapshot_crowding.py`（月度 19:35，笔记§1 表） | 微盘成交/换手数据 | crowding-indicators.json（micro_turnover_share flag=red 等） | flag=red→notify_hub 橙色告警（笔记§0） |
| N15 | 记录审计（横切） | `decision-log.jsonl`（decision_id/trigger/action/backup/params/rollback_condition/expected_impact/phash/data_snapshot）+ `experiment-ledger.jsonl`（experiment_id=IT-批次-序号，n_trials 跨批累计防多重检验）+ `switch_log.jsonl` + engines.json audit.changes[] + registry_backup_*.tar.gz（笔记§1/§0b） | 各节点事件 | 可追溯账本（R-223 ⑤） | 无 |
| N16 | 同步回传 | `auto_sync_notify.py`（VPS pull */30 增量 + 周日 03:00 全量，rsync HP→VPS 04-投资研究 白名单）；`paper_engine.rsync_to_vps()`（L558，六件套 push）；`collect-metrics.sh`（HP→VPS:8055 每分钟）——三通道并存（重复建设 Top4/D4） | HP results/ + model/registry 结果 | VPS 04-投资研究 镜像 + metrics.db | 通道失败→下次 cron 重试；white list 遗漏→R-320 已知 |
| N17 | 通知呈现 | `notify_hub.py` 双端（HP 每小时 + VPS 实例，笔记§0）：监视 risk-status 新 generated_at 红黄/拥挤度红橙/paper 异常 | risk-status.json + crowding-indicators.json + paper 状态 | notifications-queue.jsonl→微信主会话转述；dashboard quantFreshness/quantConsistDot（L7416/7417） | 无 |
| N18 | 退出/回退 | risk-charter 规则触发（N13）+ `rollback --to` 字节级还原 + override TTL + 影子 termination（笔记§1/§2） | 触发信号（降仓/观察/失效/候选劣化） | 降仓至 50% / 暂停模型升级 / 失效 review / 版本回退 | 回退失败→保留字节快照可重试 |

> 两径汇流说明：微盘版本线 N5→N8→N9→N10a→N11 全自动；引擎线 N4(想法)→判门(G0-G6)→N10b 影子→N11 人工批准，两线最终都汇入 N12 paper 运行时 + N13/N14 巡检。证据：笔记§2（引擎生命周期实证）、笔记§1（微盘血统版本线）。

---

## 4. 问二：节点=通用模块重构评估（结论先行）

**结论**：18 个节点可抽象为 **15 个通用模块（GM1-GM15）**。现状重复建设按实据置信排序集中在 6 处：① 进化循环双轨在跑（D6，双 cron 并行，Top1）；② 影子观察三处实现 + 通用版反成孤儿（Top3）；③ 门禁判据两代字段不相容（G0-G6 vs g1-g6，Top6）；④ 同步/指标通道五条仅两条必要（Top4/D3/D4）；⑤ 指标数字 ≥9 渲染点同源 6 处（Top2）；⑥ engines.json 中央表双落盘 + 30 处硬编码 workspace-quant 路径（Top7/D10）。除"影子观察"与"门禁统一"两项需方案评审/分步验证外，其余多为 R-320 P0/P1 或 R-321 已覆盖的低风险项。**「influence 二层激活判据」在全部证据源中无实据**（详见 4.3）。

### 4.1 逐节点重复建设评估（对照 Top10 / R-320 D1-D11 / R-321）

| 流程节点 | 现有实现物 | 重复判定 | 对照条目 |
|---|---|---|---|
| N1 数据采集 | refresh_data + cron_qfq_* + w6 + fetch_valuation | 各管一类**不重复** | 笔记§3 表 |
| N2 数据校验 | data_validator.run_all（pipeline 内联）+ paper_engine validate（委托同一 validator）+ data Tab D1 卡（读产物） | 合理分层**非重复**（1 实现 2 消费） | 笔记§3 表 |
| N4 想法收集 | ideas/pool.jsonl（在用）+ VPS action 队列 idea 类型（入口死）+ M2.7 想法池（前端死岛） | **入口三处一处在用**（死入口待清） | 笔记§3 表 |
| N6 进化循环 | evolution_pipeline cycle（周六）+ p3_3_evolution_standalone（月1/15，旧 registry 前）+ evolution_engine/evolution_review/iter2×4（孤儿） | **严重重复：D6 双 cron 并行**（Top1） | R-320 D6、Top1 |
| N6 回测引擎 | engine.run_backtest（ext 字符串注入零改动）+ run_versioned_backtest（model_manager 旧路径）+ e2_backtest（新赛道一次性） | 两代并存+赛道专用，语义不同**暂可接受** | 笔记§3 表 |
| N8 门禁 A 版 | evolution_pipeline g1-g6 + SCORED | 唯一权威（微盘线） | 笔记§3 表 |
| N8 门禁 B 版 | E2 预注册 G0-G6（文档手判）+ a12_shadow_eval（复核）+ rebalance_gate（月首交易日门） | **多套判据体系并存，字段名不相容（G0-G6 vs g1-g6）**（Top6） | 笔记§3 表、Top6 |
| N10 影子观察 | pipeline._shadow_update（版本级）+ engines_shadow_evaluate_gold（引擎级）+ a12_shadow_eval（a12 专用包装） | **同类逻辑三处实现**；R-259 原设计通用版 engines_shadow_evaluate 反成孤儿被 _gold 替代（Top3） | 笔记§3 表、Top3、R-320 D8 |
| N11 激活切换 | pipeline._do_activate（自动）+ model_manager merge_candidate/reject（pending.json 旧流程，M2.6 卡对应物已过时）+ engines.json 人工条目变更 | **三代机制并存，仅 1/3 全自动** | 笔记§3 表 |
| N12 paper 运行 | paper_engine（A）+ paper_engine_gold（B 冻形态）+ F6 组合账本（规划中） | **有意隔离不合并**，抽公共层即可（D7） | R-320 D7、笔记§3 表 |
| N13 风控退出 | risk_patrol（在用）+ risk_control.py（孤儿，paper_engine 已内联同功能） | 孤儿实证（D 类清理） | 笔记§3 表 |
| N16 结果同步 | auto_sync_notify pull（在用）+ paper_engine rsync push（在用）+ sync_to_vps.sh + hp_api_server /sync（死） | **D4 三套一套在用 + 2 死**（Top4） | R-320 D4、Top4 |
| N16 指标上报 | collect-metrics.sh push + pull-hp-metrics.sh pull + fetchHpStats SSH 实时 | **D3 双写同库**（Top4） | R-320 D3、Top4 |
| N16 中央注册表 | model/registry/*.json + main.json（pipeline 读写）+ engines.json（双落盘）+ versions-manifest（只读投影）+ factor_pool/sota/history | 投影合理；**engines.json 双落盘冗余**（Top7） | 笔记§3 表、Top7、D10 |
| 版本展示 API | /api/quant/active（活）+ /api/quant/models（死岛）+ version-options/history（活） | **D1/D2 两代 UI**（死树清理后自然收敛） | R-320 D1/D2、Top10 |
| 指标数字渲染 | v5MetricCardsHtml 组件，M3/B5/H3×2×6 卡 + B1 徽标行 + F6 脚注 + B7 排行 4 列 + H1 行内 + B8 台账（≥9 渲染点，同源 6 处） | **B1 回退指标与 M3/B5 完全同值=最高优去重**（Top2） | R-321 §2.2 表、Top2 |
| 净值绘制 | v5DrawNav/v5DrawPos/v5F6CurveHtml/renderNavWithPosChart 多绘制器；A 曲线端点 B6/B3/P5 各画一遍 | **多绘制器未共用组件**（Top5） | R-321 §2.1 表、Top5 |
| 影子进度 clean_evals | B8 版本级 lifecycle.shadow_watch（L12963）+ P5 引擎级 engines.evals（L13560）+ L10151 引擎评估徽标 | 数字三处消费，**粒度两级建议保留但以 B8 为准** | R-321 §2.3、笔记§4 |

### 4.2 通用模块合并抽象方案（GM1-GM15）

| 通用模块 | 对应节点 | 职责边界 | 唯一实现落点 | 被合并对象清单 |
|---|---|---|---|---|
| GM1 数据管道 | N1 | 行情/财务/退市/估值采集，只负责"取数落盘" | HP 脚本组（refresh_data/cron_qfq/w6/fetch_valuation，维持现状不合并） | 无（各管一类，合理） |
| GM2 数据校验器 | N2 | 6 项校验统一入口，返回 fail-fast 信号 | HP `data_validator.py`（唯一权威） | pipeline Step0 内联调用与 paper_engine validate 已委托同一实现——保持，前端 D1 只读产物 |
| GM3 想法/候选簿记 | N4-N5 | 想法的唯一登记入口与候选生命周期簿记 | HP `ideas/pool.jsonl` + registry 条目 | 删 VPS action 队列 idea 入口（死树 R-320-C）；删 M2.7 想法池死岛前端（或改只读列表） |
| GM4 回测执行器 | N6 | 版本两腿回测 + 基线，双窗口径唯一实现 | HP `evolution_pipeline.py backtest` | **停用 p3_3_evolution_standalone 双轨（需用户批准动 cron）**；孤儿 evolution_engine/evolution_review/iter2×4 归 P0 死码清理；run_versioned_backtest 旧路径标注废弃 |
| GM5 等价校验器 | N7 | EQUIV patch 全关复跑 diff 判定 + 锚点校验 | HP `evolution_pipeline` 内嵌（唯一，无重复） | 无 |
| GM6 门禁引擎 | N8 | 判据以注册式 spec 定义（字段名标准化），g1-g6 与 G0-G6 统一进同一 gate 框架 | HP `evolution_pipeline` gate 段独立为 `gates.py`（或同文件独立类） | 统一 G0-G6（E2 预注册）与 g1-g6 字段映射表；rebalance_gate/a12 L3 守卫作独立门部件调用同框架；SCORED 为唯一评分器 |
| GM7 影子观察引擎 | N10 | clean_evals 计数状态机唯一实现，版本级/引擎级/赛道特例全走配置 | HP 通用 `engines_shadow_evaluate`（**复活 R-259 原设计通用版**）或扩展 pipeline._shadow_update 为引擎感知 | 三处实现合一：_shadow_update（版本级）、engines_shadow_evaluate_gold（引擎级，保留 A/A2 零改动原则）、a12_shadow_eval（a12 特例降级为配置参数，含 L3 守卫语义） |
| GM8 激活/回退写入器 | N11 | registry entry + main.json 生成 + 字节快照 + switch_log 唯一写手 | HP `evolution_pipeline._do_activate`/`rollback`（唯一权威） | model_manager merge_candidate/reject 旧 pending 流程废弃；引擎线人工批准后也走同一写入器（批准=门，写入=同一实现） |
| GM9 paper 运行时内核 | N12 | 净值计算/调仓/校验/六件套 push 的公共内核 | HP `quant_common.py`（R-320 P2 方向） | paper_engine 与 paper_engine_gold 抽公共层（**不合并脚本**，D7 有意隔离保留）；F6 组合账本以公共内核扩展 |
| GM10 中央风控引擎 | N13 | charter 规则解释 + risk-status.json 唯一生产者 | HP `risk_patrol.py`（唯一活） | risk_control.py 孤儿归 P0；underperform_discipline 反作用回进化层保持 |
| GM11 市场环境监控 | N14 | 拥挤度指标 + 月度快照 | HP `collect_crowding.py`+`snapshot_crowding.py`（两脚本职责分明不合并） | 无 |
| GM12 审计日志层 | N15 | 三账本 + audit.changes 写入公共函数，字段规范沿用 R-223 ⑤ | HP `append_record()` 公共函数（低优先级整合） | decision-log/experiment-ledger/switch_log 统一写入器；registry_backup 归档策略 |
| GM13 同步总线 | N16 | HP→VPS 结果同步唯一通道 + 指标唯一通道 | VPS `auto_sync_notify.py`（结果，*/30+全量）+ 指标二选一（见待确认②） | **五条通道收敛为两条**：删 sync_to_vps.sh + hp_api_server /sync（P0）；D3 push/pull 双写二选一；paper_engine rsync push 与 auto_sync pull 取舍见待确认④ |
| GM14 通知中心 | N17 | 告警规则 + 队列 + 微信转述唯一代码库 | 单一 `notify_hub.py` 代码（双端部署可保留，但逻辑同源） | HP/VPS 双实例抽同一代码；quantFreshness/quantConsistDot 语义保留 |
| GM15 注册表与投影 | 横切 | model/registry 唯一权威；main.json/versions-manifest 只读投影 | HP `model/registry/` + `gen_versions_manifest.py` | **engines.json 双落盘收敛为 model/registry 单份**（改 server.js 读路径 L2120/L2711）；factor_catalog v1/v2 降级链收敛 v3（L1860-1864，见待确认⑤）；版本展示 API 死岛清理（D1/D2） |

> 前端组件层（不属于"流程节点"但属重复重灾区，一并给唯一落点）：**v5MetricCardsHtml** 为唯一指标卡渲染组件（B1 徽标行去重）；**新增/抽取 NavChart 公共绘图器**（v5DrawNav/v5DrawPos/v5F6CurveHtml/renderNavWithPosChart 合并）；**跨 Tab 端点重复拉取 6 组**（registry/active-curves/engines/version-options/shadow-nav/data-health）收敛为前端统一 fetch 层 + 会话缓存（R-321）。

### 4.3 「influence 二层激活判据」存疑项（如实说明）

阶段A对 `influence` 关键字在 R-290/R-314/R-29x 系列及 `engines.json` 中 **grep 无实据**（笔记§2 末段）。现有证据中最接近的两个候选概念，**均不等于用户所问的"influence 二层激活判据"**，无法自行推定：

1. A2 叠加臂的 `w_source=scoring_v12_frozen migration`（R-267 §4.2 组件型建议值，笔记§2）；
2. F6 中央风控层2 的 wi 计算框架（R-259 §6.1 战略配置器，笔记§2）。

**处置**：如实标注无实据，纳入「待用户确认」节（§7 第 1 条），请用户明确所指后再补证。

---

## 5. 问三：逐节点可视化评估（结论先行）

**结论**：18 节点中 **15 个需要可视化**（判据：该节点信息有时效性或异常需人审），3 个不需要（N5 fork 设计过程、N7 EQUIV、N15 审计——分别是"过程性/二元性/已由详情抽屉覆盖"）。现有可视化与 R-321 矩阵核对后，**无新增高优需求，重点在去重与死岛清理**：唯一真正必须动手的高优去重是 **B1 徽标行回退指标与 M3/B5 完全同值**（R-321 最高优项）；其余重复多为粒度/视角差异，按 R-321 建议保留。可视化全部落在 5 个活 Tab（model/btlc/hist/paper），data/factor Tab 各只服务 1-2 个节点。

### 5.1 节点×可视化×处置总表

| 节点 | 需要可视化？ | 判定理由 | 现有可视化（Tab/模块） | 与别节点重复？（引 R-321 矩阵） | 处置建议 |
|---|---|---|---|---|---|
| N1 数据采集 | 是 | 时效性（cron 自愈兜底，但数据陈旧需人审） | data Tab D1 健康校验卡（读产物，笔记§4） | 唯一，无重复 | 保留（复用 D1，不新增） |
| N2 数据校验 | 是 | 异常需人审（6 项校验结果时效性） | data Tab D1（PIT + 健康 4 卡，唯一） | 唯一，无重复 | 保留 |
| N3 快照登记 | 否 | 过程性，无独立时效异常 | 无独立视图（registry 状态经 B7/H1 反映） | — | 不新增 |
| N4 想法收集 | 是 | 想法状态需人审（但当前无活视图） | M2.7 想法池=死岛（pending 卡对应旧流程已过时，R-206 编号 M2.7） | 无（死岛） | **删除死岛**（P0）；若需看 ideas/pool.jsonl 改只读列表并入 B8 台账 |
| N5 fork 设计 | 否 | 过程性 | 无 | — | 不新增 |
| N6 回测 | 是 | 结果时效性 + 人审核心 | v5btlc 五模块：B6 策略vs基准主图/B3 F6 组合图/B5 指标卡/B7 全版本排行/B8 生命周期折叠；A 曲线三处（B6/B3/P5 parent 线，R-321 §2.1） | **B1 徽标行与 M3/B5 同值**（最高优去重）；A 曲线端点 3 图各画一遍（Top5） | B1 徽标行去重（P1，R-321 已定）；净绘制抽公共组件（P2） |
| N7 EQUIV | 否 | 二元结果（PASS/FAIL）且只在换代时刻重要 | 无独立视图（drift signature/phash 在 R-223 ⑤ 记录） | — | 建议并入 H3 详情抽屉字段展示（低风险，可选） |
| N8 门禁 | 是 | PASS/FAIL 人审核心 | H3 详情抽屉 Gate 评估区（唯一活）；「五门禁面板」+「DSR 折扣曲线」为假数据死岛（L12806-12816 + 8月16日中性态假数据，R-321 §3.4） | 无重复（死岛不参与） | **死岛删除不复活**（P0）；H3 保留为唯一 Gate 视图 |
| N9 评分 SCORED | 是 | 排名决定上岗资格 | B7 排行（4 列，含 score）+ B8 生命周期面板 | B7 与 B8 指标同源（R-321 判定保留） | 保留 |
| N10 影子观察 | 是 | clean_evals 进度/出影异常需人审 | B2 影子对比图（图形）+ B8 lifecycle 进度（L12963 版本级）+ P5 跨引擎影子卡（L13560 引擎级）+ L10151 引擎评估徽标 | clean_evals 数字 3 处消费（R-321 §2.3） | 保留两级粒度（版本级以 B8 为准、引擎级 P5）；L10151 徽标并入 B1 去重一并处理 |
| N11 激活 | 是 | 切换事件需留痕人审 | B8 qLifecycleTimeline 全局 + H3 单版本详情（两处粒度不同） | 非实质重复（R-321 判定保留） | 保留；M2.5/M2.6 旧设计概念随 P0 清理 |
| N12 paper 实盘 | 是 | 净值/持仓/交易时效性 + 一致性 | paper Tab P0-P11 十二模块：P4 净值唯一、P6 持仓唯一、P7 交易唯一、P0 一致性徽标、P3 运行状态条、P8 运行版本卡、P11 参数 | **paper 实际净值仅 P4 一处**（无重复）；P6/P7 无重复（R-321 §2.5） | 全部保留（唯一性已实证） |
| N13 中央风控 | 是 | 退出纪律异常必须人审 | P10 退出纪律卡 renderRiskCharterCard（L11887，读 risk-status.json，唯一前端）；另走 notify_hub 微信告警 | P10 内嵌 crowding_reference 与 P9 语义重叠（风险规则引用 vs 监控现状） | 保留 P10 主色阶；crowding_reference 行折叠为详情（P1 低风险前端调整） |
| N14 拥挤度 | 是 | flag=red 异常需人审 | P9 微盘拥挤度卡 renderCrowdingCard（L11797，读 crowding-indicators.json，唯一主卡） | 与 P10 内嵌 crowding_reference 轻微重叠（如上） | 保留 P9 主卡；P10 内嵌行折叠 |
| N15 审计 | 是 | 可追溯需人审 | H3 详情 + B8 台账（decision/experiment 记录） | 两处粒度不同（R-321 判定保留） | 保留，不新增全局审计页 |
| N16 同步 | 是 | 同步陈旧是运维异常 | quantFreshness 横幅（L7417）+ quantConsistDot 自检点（L7416）+ P3 mtime 红绿灯 | 三处定位不同（R-321 判定保留） | 保留 |
| N17 通知 | 是 | 告警时效性 | notifications-queue→微信转述 + dashboard 横幅 | 与 P10/P9 语义不同（事件流 vs 状态卡） | 保留 |
| N18 退出/回退 | 是 | 退出动作/回退事件需留痕 | switch_log 事件入 B8 时间线 + P10 当前色阶 | 与 N13 共享 P10 | 保留 |

> 关键行号：L7416/7417（横件）、L9964（引擎评估徽标）、L10151（clean_evals 徽标）、L11797（P9）、L11887（P10）、L12806-12816（假 DSR 曲线）、L12963（B8）、L13560（P5）——均笔记§server 实查或 R-321 记录。

---

## 6. 实施建议分期表

| 分期 | 内容 | 风险/前提 | 涉及清单 |
|---|---|---|---|
| **P0 死码清理（可直接做，R-320 已授权）** | 死 UI 树 ~1500 行（L11377-12836 + L14029）+ 29 死后端路由 + 五门禁/DSR 假数据死岛 + sync_to_vps.sh + hp_api_server /sync + 孤儿脚本（evolution_engine/evolution_review/iter2×4/risk_control.py）+ M2.6/M2.7 概念死岛 | 低；git 快照可回滚；registry active/paper_engine/HP cron 在役项零改动 | Top10-10、R-320 D1/D2/D4/D5/D9 |
| **P1 纯前端低风险（R-321 已覆盖，可直接做）** | B1 徽标行指标去重（最高优）；P10 crowding_reference 行折叠；跨 Tab 6 组端点重复拉取收敛（fetch 层 + 会话缓存）；H3 增加 EQUIV/drift 字段展示 | 低；只动 server.js 前端 + 少量样式，390x844 回归照 R-321 验收 | Top2、R-321 全案 |
| **P2 中风险需方案评审/分步验证** | GM7 影子观察三实现合一（复活通用 engines_shadow_evaluate）；GM6 门禁判据统一 schema（G0-G6 vs g1-g6 映射）；GM9 quant_common paper 公共层；GM15 engines.json 单落点（改 server.js 读路径 L2120/L2711）；净值绘图组件统一；metrics 双写二选一；factor_catalog 收敛 v3 | 中；影子合一与门禁统一会触碰判定时机/字段语义，需先出迁移方案 + 影子窗口对齐验证；engines.json 收敛需先确认 server.js 消费点 | Top1/3/4/5/6/7、R-320 P2、R-321 组件化 |
| **P3 需用户批准（AGENTS 红线：crontab/registry active 不可自动改）** | D6 双轨停用：p3_3_evolution_standalone 移出 crontab；任何 HP cron 时间表变动；registry active 条目变更；GM13 同步通道取舍落地；influence 机制任何落地 | 动 cron/active 必须用户批准；停 p3_3 前需确认月1/15 无历史依赖 | D6、D3、D4、Top1、influence |

---

## 7. 待用户确认清单

1. **「influence 二层激活判据」所指**：阶段A grep R-290/R-314/R-29x 及 engines.json **无 influence 实据**。最接近候选为 A2 叠加臂 `w_source=scoring_v12_frozen migration`（R-267 §4.2）与 F6 层2 wi 计算框架（R-259 §6.1），均不能自行等同。请用户明确所指机制与出处。
2. **D6 双轨裁决**：p3_3_evolution_standalone（月1/15 cron，939 行旧版）是否停用？（动 crontab 需批准；停用后孤儿 evolution_engine/evolution_review/iter2×4 一并归 P0）
3. **D3 指标通道二选一**：collect-metrics.sh push 与 pull-hp-metrics.sh pull 双写同 metrics.db，保留哪个？（建议保留 push 收敛 pull，方向待确认）
4. **D4 同步通道取舍**：auto_sync_notify pull（统一、*/30 有延迟）vs paper_engine rsync push（即时、六件套、L558）——是否允许停 push 改由 pull 全量覆盖，或保留 push 为 paper 专属即时通道？（影响 N12 实时性与 N16 单通道目标，取舍点）
5. **GM7 影子合一后特例语义**：a12 L3 守卫「clean_evals≥required-1 停自动评估转人工评审」是否作为通用配置保留？（通用化可能抹平特例守卫，冲突取舍点）
6. **factor_catalog v1/v2 消费者确认**：三代降级链共存（L1860-1864，Top7）收敛到 v3 前，需确认无外部消费者仍引用 v1/v2。
7. **engines.json 双落盘收敛方向**：以 model/registry/engines.json 为主源并改 server.js 读路径（L2120/L2711），或保留 results/ 副本为只读镜像？（删除任一份前需核对 60 路由全部读点）

---

## 8. 来源清单（文件 + 行号，关键数字可溯源）

| 证据 | 文件/出处 | 关键数字/行号 |
|---|---|---|
| HP crontab 全表（22 条） | HP `noname@10.12.192.174:2222` 实查（2026-08-27），笔记§1 表 | evolution_pipeline 周六09:00；risk_patrol 工作日16:45；p3_3 月1/15 02:00；paper daily 16:30 / rebalance 15:00 / validate 周日20:00；gold 系列 07:40/周日03:00/月3日 09:38-09:40；crowding 周日07:00 + 月19:35 |
| 进化状态机 | `evolution_pipeline.py`（HP 80KB） | cycle 七步（Step0 校验→Step0b 新鲜度→S1 快照→S2 想法→S3 因子→S4/5 回测+evaluate→S6 通知→S7 自动 activate）；g1-g6 门禁；SCORED 权重（p .175/dsr .175/oos_calmar .125/oos_sharpe .125/is_calmar .075/is_sharpe .075/dd .10/corr .10/logic .05）；_shadow_update 状态机；oos_split_ym=2021-01；GUARD_CORR_CONFIG 护栏 |
| 风控 | `risk_patrol.py`（HP 21KB） | 三规则：drawdown_circuit_breaker（HWM 25%→降半仓）/underperform_discipline（rolling_6m_excess<-10%→观察暂停升级）/live_vs_backtest（live/backtest<0.5 持6月→review）；run_replay 2015 演练；风险章程 charter_version=1.0（risk-status.json 2026-08-26 16:45:02，overall=green） |
| 引擎影子 | `engines_shadow_evaluate_gold.py` + `engines_shadow_nav_gold.py` | 月3日 09:40 评估 / 09:38 影子NAV；A/A2 字节级零改动；gold shadow_nav 157 月（笔记§0） |
| a12 特例 | `a12_monthly_evaluate.sh` | 月2日 17:10；L3 守卫 required-1 停自动转人工；通知走 notifications-queue.jsonl |
| 旧模型层 | `model_manager.py`（VPS 34KB） | pending.json/factor_pool.json/switch_log.jsonl/history.jsonl；merge_candidate/reject 旧流程 |
| 中央注册表 | `model/registry/engines.json`（14KB schema_v1）+ `model/registry/` 74 文件 + `versions-manifest.json`（121KB，118 版本） | A=active/A2=shadow w=0.5/gold=active_paper；candidate=a12_s2_reb/a14_crowdf2/a15_csad_resid；registry_backup ×7+；main.json 快照 ×N |
| 数据通道 | `server.js`（718,178B，60 路由） | QUANT_REPORTS_DIR L2120（engines/risk-status/f6-curves/shadow-nav）；QUANT_MODEL_REGISTRY_DIR L2711；decision-log L2562；ideas L2522；factor_catalog 三代 L1860-1864；results/model 别名 L2853/L1866；engines.json 双落盘（model/registry + results，同日期 08-25）；五门禁消费 L2470-2513；verdict L2596-2606；clean_evals 三处 L10151/L11381/L11978；P9 L11797；P10 L11887；徽标 L9964；横件 L7416/7417；死树 L11377-12836+14029；假 DSR L12806-12816；shadow-nav 双路由 L3756/L3800；paper_engine rsync L558 |
| 方案文档 | R-203（五层架构）/R-206（模块编号 M1-M4 系）/R-220（五门禁→评分制、#7/#8 移除人工）/R-223（六环节、EQUIV、locked=2006-01-01~2024-06-30、DSR n_trials）/R-259（§6.1 F6 层2 wi 框架）/R-267（§4.2 组件型建议值）/R-300~R-307（E1-E2 判门/影子/激活链）/R-316/R-317/R-318（F6/F7 层2）/R-319（F6 实施）/R-320（D1-D11 重复矩阵 + P0/P1/P2 分期）/R-321（活 UI 矩阵 + 六端点重复拉取 + 死岛评估） | 笔记§证据源2/§3/§4/Top10 |
| 阶段A笔记 | `shared/results/work/task-0501-notes.md` | §1（时序16步 + cron + 脚本解剖）/§0b（状态字典）/§2（多引擎视角 + influence 存疑）/§3（节点对照表）/§4（可视化矩阵）/Top1-Top10 |

---

*本报告为纯方案文档（零代码改动），与 R-320/R-321 同属"先方案后批准再改造"链路。红线执行：涉及 crontab/registry active 的任何落地均在 P3 待批准；本报告不含任何实施动作。*
