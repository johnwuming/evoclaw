# R-362 月频挂点方案B实装（task-0559）

> 用户 2026-08-29 14:01 批准 R-355 提案方案B。本任务按提案落 HP crontab 幂等重跑挂点并验证。

| 项 | 内容 |
|---|---|
| 结论 | **方案B挂点已落 HP crontab 并全绿验证：仅新增 1 条目（+注释行），幂等双跑 md5 逐位相同，与 VPS 现役产物零 diff；引擎/registry/账本/live-data 零改动** |
| 变更面 | HP crontab 39→41 行（注释+条目）；备份 `~/crontab.bak-task0559-20260829` |

## 落地内容

**新增 cron 条目（HP，noname@10.12.192.174）：**

```
# --- task-0559 performance 导出挂点 方案B (R-355 用户批准 2026-08-29): 每月1-7日16:00 幂等重跑, 兜底月首交易日调仓后1小时 ---
0 16 1-7 * * cd /home/noname/quant-evolve && /home/noname/miniconda3/envs/quant/bin/python portfolio_v1/governance/export/hp_export_metrics.py --out-dir /tmp/perf_export >> logs/perf_export_$(date +\%Y\%m).log 2>&1
```

- 表达式严格按提案方案B：`0 16 1-7 * *`（不用 `1-7 * * 1-5`，规避 vixie cron「日+星期 OR」陷阱）。
- 时序对齐：在役 `0 15 * * 1-5` rebalance --check-month-start（月首交易日 15:00 实际执行）后 1 小时；与 16:30 paper daily 错峰 30 分钟，无冲突。
- 1–7 日每日兜底重跑，覆盖「首个交易日碰上周一/节假日顺延」等日历抖动；导出器幂等，无新月末数据时输出字节相同，零副作用。
- 日志 `logs/perf_export_$(date +%Y%m).log` 按月分文件自然轮换（% 已按 cron 规则转义）；脚本自带 makedirs，/tmp/perf_export 无需预建。
- 部署副本实际路径为 `~/quant-evolve/portfolio_v1/governance/export/hp_export_metrics.py`（任务书简写少一层目录，以实存为准）；两端 md5 `43f3932a` 一致。

## 验证记录

| 验证点 | 结果 |
|---|---|
| crontab 改前备份 | `~/crontab.bak-task0559-20260829`（5435B，39 行） |
| diff 核对 | 仅 +2 行（注释+条目），其余 39 行逐行零改动 |
| 幂等双跑 | performance.json `e959d21a` / nav_curves.csv `9704a300`，两跑 md5 逐位相同 |
| `--check` | CHECK-SAME，exit 0 |
| 指标一致性 | ann=0.135702 / vol=0.094679 / sharpe=1.4333 / maxDD=-0.090794，与 R-355 在役值一致 |
| vs VPS 现役 | nav_curves.csv md5 逐位相同；performance.json 逐字段零 diff（live/data 已是固化脚本产物） |
| cron 在位 | `crontab -l` 第 40–41 行在位；`crontab -` 安装时 vixie 已语法校验通过；不真等触发（下次自然触发 2026-09-01 16:00） |

## 降级路径（按提案）

- 导出失败仅记日志（`~/quant-evolve/logs/perf_export_YYYYMM.log`），BFF 无感知，继续用旧文件。
- 产物落 `/tmp/perf_export`（tmp+rename 原子写），本任务未接 VPS 同步环节，不存在半途文件进 live/data 的可能。
- 提案落地物中的「VPS 拉取包装脚本（scp -O + tmp+mv + md5 判定 + 日志）」不在本任务书交付物清单，**待后续任务接线**；接线前管道为「HP 侧自动重导出」，VPS 侧仍为现役文件不变。

## 回滚方法

1. crontab 回滚：HP 上 `crontab ~/crontab.bak-task0559-20260829` 一键还原（或单删第 40–41 两行）。
2. 数据面：本次未触碰 VPS `live/data/`；如需回滚数据用 R-355 备份 `/tmp/task0553-bak-performance.json`、`/tmp/task0553-bak-nav_curves.csv`（或由 HP 源同口径重导）。

## 修改文件清单

- HP crontab：+注释 1 行 +条目 1 行（唯一变更）
- 报告/笔记：`shared/results/05-量化投资/R-362-月频挂点方案B实装.md`、`work/task-0559-notes.md`
- 零改动：VPS crontab、live/data、BFF、引擎、registry、paper_engine、evolution_pipeline、账本

## 来源

- R-355 Performance指标持续供给管道（方案B节）
- HP 实查：crontab、`portfolio_v1/governance/export/hp_export_metrics.py`、双跑+check 实测
- VPS 实测：`tools/quant-bff/live/data/` 两文件 md5/字段比对
