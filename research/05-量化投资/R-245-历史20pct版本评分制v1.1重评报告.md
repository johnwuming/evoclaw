# R-245 历史20%+版本评分制v1.1集体重评报告

- 任务号：task-0386（用户 2026-08-19 19:41 拍板推进）
- 日期：2026-08-19
- 产物：HP `~/quant-evolve/scripts/rescore_20pct_v11.py` + `results/rescore_20pct_v11_summary.json` + registry 11 条目 + `results/versions-manifest.json` 重生成

## 一、背景与目标

量化版本库中存在一批"历史年化 20%+"的老版本（v0~v4 系 seedB 变体为主），其高分产生于旧口径（五门禁一票否决时代/无统一评分）。用户拍板按评分制 v1.1（SCORE_CONFIG，R-225）集体重走流程：

- g6 硬门禁已禁用（D-20260819-G6DEL），dd 数值保留入评分；新血统线废弃不用于 REJECT
- g3 含护栏豁免修正（D-20260819-G3CORR，向后兼容）
- 重评的 oos/dd 分量以当前在役 **a13_rsraw_e1f10dz** 为基准
- 本任务只标注 + 给建议，**不自动激活**；registry 批量补 score + rescored 标记 + manifest 重生成已获用户例外授权

## 二、方法与数据来源

1. **筛选**：registry `backtest_refs.metrics`（locked 审计锁口径）中 `annual_return ≥ 0.20`，共 12 个；除在役基准 a13 外 **11 个**进入重评。
2. **评分**：新写 `scripts/rescore_20pct_v11.py`，逐版本调用已部署函数 `gate_icir / gate_max_corr / deflated_sharpe / gate_mdd_vs_parent / score_composite / compute_holdout_metrics / score_rank_pool`（不改 evolution_pipeline.py 本体，调用方式参考 a13/a15_score 脚本）。
3. **holdout 对照**：`compute_holdout_metrics` 从既有 full nav（holdout_start_ym 起）分段计算，不重跑回测；locked 口径取 registry metrics。
4. **回写**：先 tar 备份 `model/registry.bak.20260819_t0386.tar.gz`，逐条目写 `gate.score / score_components / score_flags / stat_warn / rank_in_pool / score_holdout / ic_coverage / rescored{task,at,prior_*}`；单条失败即停；最后 `gen_versions_manifest.py` 重生成 manifest。全程未触碰 status、在役条目、evolution_pipeline.py、paper_engine、crontab。

## 三、核心发现（结论先行）

**结论：在 v1.1 评分制、以在役 a13 为基准的统一口径下，历史年化 20%+ 老版本无一过线（0 个晋升候选），全部维持原状；"20%+ 集体重走"疑虑可以关闭。**

### 3.1 重评结果总表（按分排序）

| 版本 | score | 池内rank | stat_warn | p单侧 | DSR | corr | dd恶化pp | holdout(年化/mdd) | IC覆盖 |
|---|---|---|---|---|---|---|---|---|---|
| a9_ranksum_raw (sota) | 0.7715 | 6* | 否 | 0.4033 | 0.9999 | 0.7555 | 0.0 | 0.2584/-0.1613 ✓ | 5/5 |
| v1g_ivw | 0.2988 | 8 | 是 | 0.0714 | 0.6433 | 0.9437 | 36.2 | 0.2292/-0.2068 ✓ | 4/4 |
| v1h_buf | 0.2979 | 9 | 是 | 0.0714 | 0.6426 | 0.9437 | 35.4 | 0.2691/-0.2118 ✓ | 4/4 |
| v0_seed (retired) | 0.2908 | 10* | 是 | 0.0714 | 0.5640 | 0.9437 | 35.9 | 0.2626/-0.2116 ✓ | 4/4 |
| v1c_liq | 0.2896 | 10 | 是 | 0.0714 | 0.5573 | 0.9437 | 36.8 | 0.2626/-0.2116 ✓ | 4/4 |
| v1b_mvq | 0.2877 | 11 | 是 | 0.0714 | 0.6101 | 0.9437 | 33.3 | 0.2720/-0.2114 ✓ | 4/4 |
| v3a_peg | 0.2861 | 12 | 是 | 0.0714 | 0.6499 | 0.9437 | 38.6 | 0.2992/-0.1328 ✓ | **4/5** |
| v1a_score | 0.2690 | 13 | 是 | 0.0714 | 0.5599 | 0.9437 | 36.6 | 0.2515/-0.2109 ✓ | 4/4 |
| v4d_mfu_raw | 0.2657 | 14 | 是 | 0.0714 | 0.4906 | 0.9437 | 35.7 | 0.2556/-0.2108 ✓ | **4/6** |
| v3b_glm | 0.2651 | 15 | 是 | 0.0714 | 0.4055 | 0.9437 | 34.2 | 0.2716/-0.2108 ✓ | **4/5** |
| v1d_cv | 0.2611 | 16 | 是 | 0.0552 | 0.4782 | 0.9437 | 38.8 | 0.2405/-0.2109 ✓ | 5/5 |

\* a9 状态 sota、v0_seed 状态 retired，均不在正式排名池（部署口径仅 candidate/pending/active 入池），其 rank 为"假设入池"参考值。

### 3.2 老版本低分分解（0.26~0.30 的构成）

- **dd 恶化 33~39pp**（MDD -0.66~-0.72 vs 在役 -0.3355）→ dd 分量 0
- **DSR 0.41~0.65**（<0.90 警戒线）→ dsr 分量 0，且触发 stat_warn
- **corr 0.9437**（与在役因子集最高|ρ|，div 系老因子 vs a13 因子集高度冗余）→ corr 分量 0
- **oos_calmar 负增量超 -40% 满档**（calmar ~0.36 vs 在役 0.6562）→ oos_calmar 分量 0
- 仅 p（0.0714→0.74）、is 分量（0.50~0.77）、logic（1.0）贡献分数

**反差点**：这批版本 holdout 段全部通过（近段年化 0.23~0.30，mdd 收敛至 -0.13~-0.21）——"近段表现好"无法弥补全期深回撤 + 因子冗余 + DSR 不足的评分劣势。历史 20%+ 年化主要是旧口径（深回撤不计价、无在役对照）的产物。

### 3.3 a9_ranksum_raw 0.867 → 0.7715 的原因（基准切换，非策略退化）

- oos 分量：对照基准从 v5h_xsub（task-0390 时点在役）换为 a13，Δcalmar/Δsharpe 增量收窄（oos_calmar 0.7844→0.4853）
- corr 0.6249→0.7555：a9 的新增因子 ret120 与在役 a13 的护栏替身 mom_pen_dz 相关 0.7555；G3CORR 豁免只覆盖"在役持护栏列、候选持替身"方向，**不覆盖"a9 持原始护栏列、在役持替身"的对称方向** → 建议（另派任务）：GUARD_CORR_CONFIG 增加对称豁免，属 pipeline 改动需用户批准
- holdout 复算与旧 summary 完全一致（0.2584/-0.1613）✓，重评流程可信

### 3.4 重评后排名池格局（top）

a13_rsraw_e1f10dz 0.8781（在役居首）> v4a_mf0_trr 0.8088 > v5k_nh10 0.80 > v5i_comb 0.7985 > v5j_bl30 0.7811 > v5b_amt55 0.7537 > v5c_amt73 0.7486 >> 老版本（≤0.2988）。在役地位不受重评影响。

### 3.5 数据完整性如实标注

- v3a_peg 缺 peg_np、v4d_mfu_raw 缺 roc/ev_ebit、v3b_glm 缺 graham_score 的月度 IC 列：部署函数 `gate_icir` 按可用因子子集合成复合 IC（向后兼容口径），覆盖度已写入 registry `gate.ic_coverage` 与 summary，未编造数据；三分量对总分影响有限（其低分主因 dd/DSR/corr，与缺失列无关）
- 其余 8 个版本 IC 覆盖完整；全部 11 个版本 holdout 数据齐备，无 partial

## 四、结论与建议

1. **无晋升候选**：rank1+holdout+无警示的过线者为 0 → 不建议任何激活动作；老版本（prior_verdict 多为 REJECT）维持原状，不摘 legacy（本次补 score 后它们已非"未评分 legacy"，但状态/裁决变更无依据）。
2. **建议清单**（均留给用户决策）：
   - a9 对称护栏豁免修正（pipeline 改动，另派任务 + 用户批准）
   - server.js legacy 例外清理（已另派，不在本任务）
   - HP→VPS 的 versions-manifest 同步确认走既有机制（本任务仅 HP 侧重生成，rc=0，97 版本，active=a13）
3. **可复现**：`python scripts/rescore_20pct_v11.py`（干跑）／`--write`（备份+回写+manifest）；两次干跑分数逐位一致（确定性验证 ✓）。

## 五、验收记录

- 抽查 v1g_ivw / v3b_glm / a9_ranksum_raw：registry `gate.score` 与 summary json 逐一匹配（MATCH×3）✓
- registry 变更范围 = 11 个目标条目 + 备份 tar + manifest + summary，无无关文件改动 ✓
- 在役 a13 条目未动（无 rescored 块，score 0.8781 保持）✓
- 备份：`model/registry.bak.20260819_t0386.tar.gz`（35.7KB）

## 六、来源清单

- HP `~/quant-evolve/model/registry/*.json`（11 目标条目 + a13 在役基准）
- HP `~/quant-evolve/results/{a2*,seedB_*,a4d_*,mf_*,a9_*}_{full,locked}_nav.csv`（holdout/DSR 数据源）
- HP `~/quant-evolve/scripts/evolution_pipeline.py`（部署评分函数，未修改）
- HP `~/quant-evolve/results/rescore_20pct_v11_summary.json`（本次汇总产物）
- 参考：R-225（SCORE_CONFIG v1.1）、R-241/R-242（a13/a9 评分与口径复核）、a13_score_summary.json / a15_score_summary.json
