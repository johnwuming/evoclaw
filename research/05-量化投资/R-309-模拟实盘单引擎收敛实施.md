# R-309 模拟实盘收敛单引擎：旧链 paper_trade.py 退役实施报告

- **任务号**：task-0487（任务中心 127.0.0.1:8055）
- **日期**：2026-08-25（执行窗口 05:50–06:00 GMT+8；HP 主机为 UTC，执行时当地 2026-08-24 21:50–22:00）
- **性质**：R-308（task-0486）待决策清单第 1 项，用户 2026-08-25 05:46 批准「旧链整体退役」
- **改动范围**：HP crontab 恰好移除 2 行；旧链产物归档移动；2 个 cron 脚本头部注释标记；零引擎代码改动

## 背景

R-308 核查定案：paper 层存在双链——在役 `paper_engine.py`（baseline-paper-* 产物，model_version=a13_rsraw_e1f10dz，下游消费者 model_manager / collect_crowding / cron_qfq_daily 三家）与旧链 `paper_trade.py`（paper-nav.csv / paper-summary.json，无任何下游消费者）。旧链三重问题：①价格查找静默失败致 NAV 长期冻结；②与在役引擎共享 paper-state.json 整字典覆盖写，有状态互踩风险；③旧链 rebalance cron（月首交易日）按 v5h 选股，将在 2026-09-01 覆盖在役 a13 引擎持仓。

R-308 已完成应急止血（rebalance 脚本内置 exit 0 冻结、行日期纠正口径、禁止写共享 last_daily）。本轮 R-309 为退役收尾：让 baseline-paper-* 成为唯一模拟实盘链路。

## 方法（按序执行，全程边查边写笔记）

1. **crontab 退役（先备份后写回）**
   - 备份：`crontab -l > ~/quant-evolve/results/archive/crontab.bak.r309-$(date +%Y%m%d%H%M)`，落得 `crontab.bak.r309-202608242151`（5103B，36 行）。
   - 移除方式：`crontab -l | grep -v "scripts/paper_trade.py --action daily" | grep -v "scripts/cron_paper_rebalance.sh" > /tmp/crontab.new.r309 && crontab /tmp/crontab.new.r309`。全程未触碰 `crontab -r`。
   - 目标行定位：全 crontab 恰好 2 行匹配（L3 paper_trade daily、L5 cron_paper_rebalance.sh），grep -v 不会误伤其他行。
2. **旧链产物归档（移动而非删除）**：mv 前用 `lsof` 确认无进程持有；`results/paper-nav.csv`（329B）、`results/paper-summary.json`（1307B）移入 `results/archive/` 并加 `.r309-retired` 后缀；archive/ 内写 `README-r309.md`（1251B）说明退役原因并指向 R-308 / R-309 报告。
3. **cron 脚本留档标记**：`cron_paper_daily.sh`、`cron_paper_rebalance.sh` 不删除，首行插入 `# RETIRED by task-0487 R-309 2026-08-25: crontab 已移除引用，旧链退役留档，勿再启用`，其余内容一字未动。
4. **在役链冒烟验证**：`paper_engine.py --action validate` 全量校验 + `--action daily` 幂等测试（HP 为 UTC，2026-08-24 行已存在，重跑应不追加不重复）。
5. **VPS 侧同步面核查**：确认旧链文件在 VPS 的 mtime 冻结、不再出现新同步。

## 核心发现与验证结果

### 1. crontab 退役 ✅
- 写回后 `crontab -l | grep -c "paper_trade\|cron_paper_rebalance"` = **0**
- before（36 行）/ after（34 行）干净落盘 diff：**恰好 2 删（3d2、5d3）0 增**，其余 34 行逐字不变（初测 diff 出现 2 个"新增"行系抓取命令表头污染，重新干净落盘后 add=0 确认）

### 2. 归档三件套 + README ✅
`~/quant-evolve/results/archive/` 下：`paper-nav.csv.r309-retired`（329B）、`paper-summary.json.r309-retired`（1307B）、`crontab.bak.r309-202608242151`（5103B）、`README-r309.md`（1251B）。results/ 原路径已不存在。lsof 确认移动时无进程持有。

### 3. validate 6/6 PASS（超预期）✅
`--action validate` 于 21:52 UTC 执行，退出码 0：
- K线新鲜度 PASS（最新交易日 2026-08-24，距今 0 天——任务书预期此项 FAIL 为已知环境问题，实际当日 K 线已采集，环境已自愈，优于预期）
- 因子面板覆盖率 PASS（2026-08 面板 5028 只，div_yield_ttm/roe_ttm/roa_ttm 非空率全 1.0）
- 持仓股当日 K 线完整性 PASS（持仓 8 只，异常 0）
- 价格合理性 PASS（抽样 209 只，异常 0）
- 分红数据连续性 PASS（近 12 月 4461 条，涉及 3659 只）
- 调仓日选股结果≥5 PASS（最新交易日选股 20 只）

### 4. daily 幂等 ✅
- pre 基线：baseline-paper-nav.csv 8 行，末行 `2026-08-24,0.98319`；paper-state.json last_daily=2026-08-24、model_version=a13_rsraw_e1f10dz。
- 21:54 UTC 执行 `--action daily`：日志出现「spot 收盘价拉取失败（回退 parquet 口径）」——引擎按设计优雅降级，用 parquet 重算，NAV 0.983190 与既有值逐位一致；「rsync 到 VPS 完成」；退出码 0。
- post：仍 8 行（未追加、无重复），末行、last_daily、model_version 三项逐字不变 → 幂等正确（HP 为 UTC，2026-08-24 行已存在，拒绝追加即正确行为）。
- 重算总资产 ¥98,319.00 / NAV 0.983190 / 持仓 8 只 / 模型 a13_rsraw_e1f10dz，与 R-308 验收值一致。

### 5. VPS 侧旧链文件不再同步 ✅
- VPS `shared/results/04-投资研究/paper-nav.csv`（329B）mtime = 2026-08-25 01:40:02 +0800，为 R-308 纠错时最后一次同步，早于 R-309 开始（05:50）。
- 证据：paper_engine.py `rsync_to_vps()`（L1080–1085）仅同步 SUMMARY/NAV/TRADES/PORTFOLIO/STATE/VALIDATION 六个在役文件（baseline-paper-* + paper-state.json），源列表不含旧链文件；且 HP 侧源文件已归档移走 → VPS 旧链文件 mtime 永久冻结。旁证：同目录在役 baseline-paper-nav.csv 于本次 daily 被 rsync 重推（内容幂等，rsync 保留原 mtime），属在役链正常行为。
- VPS 侧旧链残留文件（paper-nav.csv 329B、paper-portfolio.json 4792B、R-308 留下的 paper-nav.csv.bak-task0486-20260825）为历史快照，无消费者引用（R-308 已确认旧链零下游），按「可留待说明」保留不动。

### 6. 收敛后系统状态
模拟实盘收敛为**单引擎单链**：`paper_engine.py` → baseline-paper-*（NAV 末行 2026-08-24,0.98319）+ paper-state.json（last_daily=2026-08-24，model_version=a13_rsraw_e1f10dz），唯一 rsync 目标为 VPS shared/results/04-投资研究/。2026-09-01 月首交易日原"v5h 覆盖 a13 持仓"事故源已物理拆除（rebalance cron 行已移除 + 脚本 RETIRED 标记双保险）。

## 结论

R-309 六项验收全部通过：crontab 退役精确到行（2 删 0 增）、归档三件套齐备、validate 6/6 PASS、daily 幂等、在役链 NAV/state 三字段不变、VPS 侧旧链同步面冻结。旧链 paper_trade.py 正式退役，模拟实盘层自 2026-08-25 起收敛为单引擎。若未来需回滚：archive/ 内 crontab 备份可直接 `crontab < crontab.bak.r309-202608242151` 恢复，旧链产物可移回原路径。

未尽事项（不在本任务范围）：VPS 侧旧链历史残留文件（04-投资研究/ 下 paper-nav.csv 等）未清理，建议后续随数据治理统一处置；spot 拉取偶发失败（本次已优雅回退 parquet，R-308 待决策清单中的备用源事项仍待用户决策）。

## 来源
- 过程笔记：`shared/results/work/task-0487-notes.md`（边查边写，7 节完整记录）
- 前置报告：`shared/results/05-量化投资/R-308-模拟实盘超期修复与数据完整性核查.md`（task-0486）
- HP 现场证据：`~/quant-evolve/results/archive/`（三件套+README）、crontab before/after 落盘文件（VPS /tmp/hp-crontab-before.txt、/tmp/hp-crontab-after.txt）
