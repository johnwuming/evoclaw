# R-370 · 文档修订批次：R-342 v2.0 契约收编 + R-344 v1.3 模块附录 + R-336 v1.6 术语增补 + spawn 模板 Dashboard 纪律（task-0570）

- 日期：2026-08-30 ｜ 任务：task-0570 ｜ 依据：用户 2026-08-30 01:18 批准「按此修订」
- 性质：纯文档增量修订，零新建文档（报告 R-370 本身除外），零代码改动，HP 零接触

## 背景与目标

契约与事实散落：BFF 端点契约散在 R-355~R-361 各实施报告；R-359 的 43 模块对照表未进 PRD；R-336 附录 A GLOSSARY 缺运行态/治理实施语汇；Dashboard 开发事故教训（2026-08-29 VITE_API_BASE）未沉淀进 spawn 模板。本批次四项修订全部收编归位，**以代码实况为唯一事实源**。

## 修订明细

### ① R-342 v1.3 → v2.0（46.3KB → 55.4KB）
- 文末新增「契约总表（v2.0 收编，2026-08-30）」：13 条已注册路由逐端点列 方法+路径｜参数｜响应 schema 要点｜降级语义｜数据源｜首次实装。端点清单以 `tools/quant-bff/src/app.js` 实际注册为准（grep `app.get(\`${p\}` 计 13，与表格行数一致）：
  `/health`、`/events`、`/migration`、`/overview`、`/engines`、`/portfolios`、`/portfolios/:id`、`/portfolios/:id/holdings`、`/portfolios/:id/trades`、`/portfolios/:id/navseries`、`/risk/gates`、`/perf-history`、`/perf-history/:id`（全 GET，前缀 `/api/v1`）。
- 通用降级语义冻结：账本门卫 503 四态（INITIALIZING/LEDGER_REFRESH_TIMEOUT/LEDGER_CORRUPTED/PROJECTION_MISMATCH）、5s 超时 503 UPSTREAM_TIMEOUT、404 两级、id 白名单校验；「绝不返回空数据假装正常」。
- 废弃/后续版本项标注：`/risk/drift`、`/portfolios/:id/timeline`（沿文末「契约与现实对齐（2026-08-29）」节）。
- 新增「构建与部署规约」节：VITE_API_BASE=/quantv6 构建期陷阱（2026-08-29 事故）、零新依赖、390x844 bodyScrollW=390、fmtID、`systemctl restart quant-bff`（quant-bff.service，127.0.0.1:8180）、只读三源清单。
- 修订记录加 v2.0 行；§3.4 原表格零删改保留作历史承诺记录。

### ② R-344 v1.2 → v1.3（26.6KB → 33.4KB）
- 附录收编 R-359 §2 全量 43 模块对照表，精简为 模块｜Tab｜现状｜根因｜处置 五列；标注「快照 2026-08-29，状态以看板实况为准」。行数核验：43（awk 区间计数）。
- BFF 契约缺口 2 项（B1/B2）不重复列表，注明已由 R-342 v2.0 收编处置。修订记录加 v1.3 行。

### ③ R-336 v1.5 → v1.6（60.0KB → 63.3KB）
- 附录 A 增补「运行态与治理术语」表（15 条，标准名｜一行定义｜出处）：registry 版本线、Engine、Sleeve、PV/vC、Shadow、Paper、Canary、Live/Gated、复现门、4 维漂移 D1-D4、三方对账、断路器、事件账本、镜像、Watermark。
- 出处全部实核：状态机串行线 §1.2⑤/§1.2⑥（含反向降级触发）、4 维漂移阈值表 §7.2、断路器 §6.1、三方对账 §6.3、事件账本 §3.1-3.3、镜像 §3.2（v1.5 运行态镜像组）+§3.6、复现门 §4.1+R-342 §3.1。
- **Watermark 如实收录**：R-336/R-342/BFF 代码均无此术语，仅 R-320（task-0434 HP metrics 增量拉水位）出现，故定义限定为「增量同步游标」并注明出处，不臆造账本语义。
- 修订记录表加 v1.6 行（保持 v1.1→v1.6 升序）；原三列对照表与维护责任条目零改动。

### ④ spawn-task.md（6.3KB → 7.3KB）
- 「量化任务附加纪律」段后新增「Dashboard 开发附加纪律」段（同格式 code block，六条）：①构建必须 VITE_API_BASE=/quantv6；②交付前无头浏览器 390x844 bodyScrollW=390 抽查；③fmtID 渲染+零新依赖；④报告编号=ls 最大 R-xxx+1、占用冲突顺延并报告头注明；⑤README 顶部更新日志每报告一行；⑥migration.json 等投影修正 .bak 备份+断链核验。

## 自检结果

| 检查项 | 结果 |
|---|---|
| R-342 含「契约总表（v2.0 收编）」 | ✅ |
| R-344 含对照附录 | ✅ 43 行 |
| R-336 含 GLOSSARY 增补 | ✅ 15 条 |
| spawn-task.md 含「Dashboard 开发附加纪律」 | ✅ |
| R-342 端点行数 = app.js 注册数 | ✅ 13 = 13 |
| 各修订记录行（v2.0 / v1.3 / v1.6） | ✅ 齐全，R-336 表序升序 |
| 增量修订（无全文重写） | ✅ 四文件均为插入/加行，wc -c 增量合理 |
| 未动四份修订文件之外的文件 | ✅（R-370 报告、笔记、README 更新日志一行、.task-completions 为任务规定交付物/常驻约定） |

## 结论/建议

- R-342 v2.0 契约总表自此为 BFF 契约唯一权威；后续端点变更须同批修订该表。
- R-359 中已被后续批次（R-360/R-361/R-362/R-363/R-366）消化的模块，状态以看板实况为准（附录头已标注快照口径）。

## 来源清单

- `tools/quant-bff/src/app.js`（路由注册 L381-393）、`src/config.js`（apiPrefix）、`src/server.js`（监听/优雅关闭）、`src/risk-gates.js`、`src/perf-history.js`、`src/nav-series.js`（响应 schema）
- R-359 §2（43 模块对照）、R-355/R-356/R-357/R-358（实施报告）、R-361/R-362/R-366（risk_gates 扩展）、R-350（部署验证）、R-320（watermark）
- R-336 §1.2⑤⑥/§3/§4/§6/§7.2、R-342 §3.1/§3.4/§4.1/§4.5（术语与规约出处）
- 过程笔记：`work/task-0570-notes.md`
