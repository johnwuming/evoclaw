# R-360 看板小修包 P1（task-0562）

- 日期：2026-08-29 ｜ 类型：前端小修 + 数据投影修正 + 契约文档增补 ｜ 执行：主会话 spawn 子代理
- 来源：R-359 审计新发现 2/3/4/5 项（前端缺渲染 4 + migration 投影 1 + 契约缺口 1）
- 范围红线：BFF 代码零改动；migration.json 属数据修正（.bak 备份先行）；R-342 只增不删；零新依赖
- 证据笔记：`shared/results/work/task-0562-notes.md`；截图 `shared/results/work/q562-shots/`（390×844）

## 0. 一句话结论

前端 4 项缺渲染全部补齐并经无头浏览器逐项验证可见；migration.json 投影 5→12 行与实际进度对齐（BFF 实测返回更新内容）；R-342 增补「契约与现实对齐（2026-08-29）」一节将 /risk/drift 与 /portfolios/:id/timeline 降为后续版本项；构建与双包测试全过，五 Tab 390px 无横向滚动。

## 1. 前端缺渲染 4 项（R-359 §2 G3/G2/V5/E3）

| # | 项 | 改动 | 验证（无头 390×844） |
|---|---|---|---|
| 1 | Tab 风险角标红点（G3） | `App.jsx` 全局 usePoll 60s 拉 /health；`TabBar.jsx` 接收 riskCount，count>0 时「风控」Tab 渲染 `.tab-risk-dot`（8px 红点，title/aria 带数量）；`styles.css` 加样式 | 自然态 pending=0 无红点（正确空态）；mock health count=2 → 红点可见 title「待处理风险 2 项」（overview_badge.png） |
| 2 | 健康条投影校验位（G2） | `HealthStrip.jsx` 内部 usePoll 120s 拉 /health，`projection_sha256_ok` true→「投影✓」false→「投影✗」null→不渲染（空态降级不变） | 五 Tab 健康条全部出现「投影✓」 |
| 3 | 版本详情 risk_control 区块（V5） | `Version.jsx` 新增 `RiskControlSection`：在役宪章回撤闸门（减半/清仓/宪章版本/口径来源）+ drawdown_gates.note + vol_target（null 显「未设定」）+ backfill_rule；复用 model-card 样式 | 详情展开后区块渲染，「减半 25% · 清仓 35%（宪章 v1.0）」+ 口径 note +「未设定」全部可见（version_detail.png） |
| 4 | 事件页 actor/target 过滤（E3） | `Events.jsx` 新增执行者/对象两个下拉（选项由已载入 items 去重导出，长 ID fmtID 截断），与类型过滤三条件叠加前端过滤（沿 W3 全量前端过滤口径） | 3 下拉就位（类型 16/执行者 3/对象 5）；选执行者后 16→2 条且 actor 一致 |

轮询代价：新增 2 条低频 GET（角标 60s + 投影位 120s，均为 /health 轻响应），其余端点零变化。

## 2. migration.json 投影修正（R-359 §2 M5/M1）

- 备份：`tools/quant-bff/live/data/migration.json.bak-20260829`（改前原样 5 行版）。
- 内容变化（5→12 行）：
  - B2「MVO 带约束对照跑批」、B3「协方差估计对比留档」：todo→**done**，evidence_ref 指向 R-349（依据 R-349/task-0544，2026-08-29 留档完成）；
  - 新增 B4-B8 = R-336 §8 Phase B **动作 1-5**（vC-0 快照落地 / 等波动率求解器 v1 / 选择器化+vC-0 复现门 / 影子双轨对账 / 4 维漂移监控），全部 done，证据 R-346/R-347/R-348；
  - 新增 **C1 治理切换**（done，证据 R-354，2026-08-29 10:43-10:58 执行完毕）；
  - 新增 **D1 旧看板退役与归档**（todo，规划中）；
  - `blocking` 原样保留；B1 保持 doing（R-359 审计未标记该项，超出本任务范围，未擅改）。
- 投影头：按 projection.json 既有约定补 `meta {generated_at, items_sha256}`，其中 items_sha256 = canonicalJson(items) 的 sha256（复用 BFF `src/replay.js` 同一实现计算）。注：BFF `migrationHandler` 只透传 phase/items/blocking，meta 为文件级留痕，不影响 API 响应结构。
- **判断项（可回退）**：`phase` "B"→"D"。理由：A/B/C 三段全部完成、仅 D1 规划中，保留「当前阶段 B」会延续「看板说谎」问题（本任务要消除的正是这类失真）；迁移页 Phase D 段随之挂「进行中」标。若用户认为应保持 B，改回一个字段即可（备份在 .bak）。
- `projection_sha256_ok` 不受影响：BFF 完整性校验只覆盖 projection.json（registry/engines/composites）。
- 实测：`GET /api/v1/migration` 返回 12 items / phase=D / B2B3 done（快照 /tmp/q562/migration.json）。

## 3. R-342 契约修订记录（R-359 §2 B1/B2）

`R-342-R336实施级架构设计含Dashboard.md` 文末（修订记录之后）增补一节**「契约与现实对齐（2026-08-29）」**，只增不删：

- `/api/v1/risk/drift`（§3.4 原表 209 行）：**降为后续版本项，当前版本不实现**。漂移 D1-D4 与连超期数已由 `/api/v1/risk/gates` 直接返回且前端已消费（R-359 R4 已达）；独立拆分留待 P2 漂移历史曲线需求出现再评估。
- `/api/v1/portfolios/:id/timeline`（§3.4 原表 206 行）：**降为后续版本项，当前版本不实现**。单版本状态链已由 detail 的 `status_history` 承接、全量流水由 `/api/v1/events` 承接（R-359 E1/E3 已达）。
- 影响面说明：消费方仅 quantv6 前端，实测未调用两端点（R-359：404 且页面功能无缺）；属承诺范围收敛，非已发布端点下线，不触发 v1.1⑨ Breaking 策略。未新增 v1.4 修订条目行（避免代拟版本号；新节自带日期与依据，引用 R-359 §2 B1/B2 行）。

## 4. 构建与验证

- `VITE_API_BASE=/quantv6 npm run build`：✓ 43 modules，dist/ 产物正常（未发布到生产，部署不在本任务范围）。
- 测试：dashboard `npm test` 39 断言全过；BFF `npm test` 38 pass / 0 fail（migration.json 数据变更不破坏 BFF 契约/降级测试）。
- 无头浏览器（本地 dist + /quantv6 反代 127.0.0.1:8180 BFF，等价生产路径结构）：
  - 五 Tab + 版本详情展开态 scrollWidth 全部 =390，无横向滚动；
  - 角标/投影✓/风控块/过滤器/迁移新行逐一可见（§1 表 + §2），截图 8 张见 `shared/results/work/q562-shots/`；
  - 空态降级保持：角标 count=0 不渲染、投影位 null 不渲染、C1 done 后「需用户批准」标消失、D1 显「未开始」。
- 清理：本地验证用代理进程已停止（8899 端口关闭），HP 零接触。

## 5. 遗留与建议（不阻塞验收）

1. B1「事件账本管线与读侧 API」仍标 doing：客观上 BFF 已部署服务（R-350），建议下次投影修订时置 done（本任务未动，避免超范围）。
2. 角标数量语义：当前仅红点+title 带数；若要常显数字徽标，等 pending_risks 出现非零真实值后按视觉复核再开。
3. P1/P2 排期项（对账抽屉/退役监视/版本树/diff 等）沿 R-359 §4 顺序另行排班。
