# task-0554 修订笔记（R-336 运行态事件枚举 + 运行数据供给管道）

## 0. 关键偏差记录（2026-08-29 10:31）
- 任务书假设当前版本 v1.3，**实际文档已是 v1.4**（头部第 4 行「当前版本：v1.4（2026-08-28，task-0536 修订）」，修订记录表已有 v1.4 行）。
- 处置：本次修订版本号取 **v1.5**；备份按实际版本命名 R-336.bak-v1.4-20260829.md，同时保留任务书指定路径 R-336.bak-v1.3-20260829.md（内容相同，备注防混淆）。
- 修订内容三条不变（§3.2 运行态镜像组 / 新增 §3.6 / §8 Phase C 追加动作+退出条件），仅版本号修正。

## 1. 结构勘察（wc -c = 56644，>30KB 未全读）
- §3.2 事件枚举位于 L142-170，四组：对象生命周期/门禁与晋升/风控与退役/数据与审计。确认无 paper 运行态事件（trade/nav/holding 均无）→ 缺口成立。
- §3.3 结束于 L188「不引入消息队列…」段，其后即 ## 4 → §3.6 插入点。
- §8 Phase C 动作 4 条（L370-373），动作 2=paper 指针语义切换；退出条件「切换后首个调仓日三方对账全绿；断路器/checkpoint 干跑验证通过；重放重建状态 vs 旧 JSON 状态 diff=0」。
- 修订记录表 L16-21（v1.1~v1.4）。
- 未动 §4/§7 阈值，未碰其他章节。

## 2. 编辑清单（edit 增量，无全文重写）
1. 头部版本行 v1.4 → v1.5（2026-08-29，task-0554 修订）
2. §3.2 「# 风控与退役」组后、「# 数据与审计」组前插入「# 运行态镜像（v1.5）」组：trade.fill / nav.daily / holding.snapshot（v1 取舍=并入 checkpoint.created payload）+ 镜像语义注释
3. §3.3 之后新增「### 3.6 paper 运行数据供给管道（v1.5 新增）」：写入时机（钩子追加、失败不阻塞、对账兜底）/ BFF 投影路径（holdings/trades/fee，衔接 task-0553）/ 看板口径
4. §8 Phase C 动作 2 后插入动作 3「运行态镜像接线」，原 3/4 顺延为 4/5；退出条件追加首运行日镜像与 CSV 逐字段一致
5. 修订记录表追加 v1.5 行（2026-08-29，动机+三条改动）

## 3. 校验结果（修订后回填）
- 待回填
